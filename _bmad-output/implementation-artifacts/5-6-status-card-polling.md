---
story_id: 5.6
epic: 5
title: Story 5.6 — Status Card & Polling Bridge
Status: done
frs: [FR-27]
ads: [AD-5]
baseline_commit: 4c4ebddec9e5aef862ceb1772bb1aa688e5a22f5
---

# Story 5.6: Status Card & Polling Bridge

Status: done

## Story

As a **user returning from Chargily checkout**,
I want **to see a status card on `/billing` that polls for up to 60 seconds and tells me whether my payment succeeded or needs waiting**,
So that **I don't have to wonder if my payment went through or contact support**.

## Acceptance Criteria

**Given** the Status Card on `/billing`
**When** a user returns from Chargily checkout
**Then** a status card appears inline on `/billing` with `role="status"`

**Given** the polling state (≤60s)
**When** payment is pending
**Then** the card shows:
- {colors.info-container} / {colors.info-on-container}
- Spinner + "Confirming payment…" (localized)

**Given** the success state
**When** the payment is confirmed within the polling window
**Then** the card flips to:
- {colors.success-container} / {colors.success-on-container}
- "{n} credits added — pack credits never expire" (localized)
- A success toast appears
- The polling stops

**Given** the timeout state (60s exceeded)
**When** payment status is still pending after 60s
**Then** the card shows:
- Info-toned (same as polling, not an error)
- "Payment received — credits will post shortly" (localized)
- Non-blocking — the rest of `/billing` stays interactive
- The credits are granted out-of-band via the idempotent webhook reconciliation

**Given** the polling mechanism
**When** the card is in polling state
**Then** it calls `GET /api/billing/status/{txnId}` every 5 seconds for 60 seconds
**And** once the payment_transactions status is 'succeeded', the card flips to success

**Given** accessibility
**When** the status changes (polling → success or timeout)
**Then** the `role="status"` change is announced before the toast appears
**And** the user is never asked to retry or re-pay

**Given** a failed settlement [AMENDED 2026-08-11, John PM verdict — a fourth card state: failed. When the polled transaction reports status 'failed' (a charge settled failed by the grant task, or a rejected charge), the card flips to {colors.danger-container} / {colors.danger-on-container} showing the 5.5 failed-row surface verbatim — "Payment failed. Contact {support} if you were charged." (history.failed_note, mailto SUPPORT_EMAIL); polling stops; the card's live role flips to role="alert" (Sally); no retry or re-pay CTA is rendered — the "never asked to retry or re-pay" clause prohibits re-payment prompts, and a support contact link is not a re-payment prompt. A 'refunded' row observed in the polling window renders the same terminal failed state (refunds are ops-manual, cannot occur meaningfully in 60s — a mapping ruling, not an amendment)]

**Given** the success copy [AMENDED 2026-08-11, John PM verdict — the success copy is type-flavored: a pack grant renders the AC/D15 verbatim "{n} credits added — pack credits never expire"; a subscription grant (200 monthly credits, which expire at cycle end) renders a Sally-approved subscription flavor ("{n} credits added" — Sally's verbatim map) that never claims the credits never expire; the flavor is selected from the polled transaction's raw type; {n} renders in latn numerals (AD-8); the success toast carries the same type-flavored string]

**Given** the card trigger [AMENDED 2026-08-11, John PM verdict — the card trigger has two paths: (a) primary — a pending-checkout entry {checkout_id, started_at} stashed in sessionStorage by useCheckoutRedirect before the Chargily redirect; (b) fallback — when no entry exists (cleared storage, another tab or device, deep link) but the return URL carries the settings-pinned ?status=success or ?status=failure, a static non-polling card renders: success shows the retained billing.status.succeeded copy, failure shows the failed state (the failed-state amendment); the fallback renders once and the param is stripped from the URL after first render; the entry path always wins when both are present]

## Tasks / Subtasks

- [x] **Task 1: Backend — `GET /api/billing/status/{txnId}/`** (AC polling mechanism; Winston Q1-Q3; the 5.2 deferred-work "shaped-metadata checkout_id" ruling — never key the lookup on chargily_metadata.checkout_id until the docs gate pins payload semantics)
  - [x] 1.1 RED: `backend/apps/billing/tests/test_status_view.py` — 401 anonymous; `since` REQUIRED (absent/malformed → 400 — never a silent default that would let a stale succeeded row flip the card); txnId = the Chargily checkout id, non-empty string ≤255 (400 on oversized; no regex pin — D11 envelope risk); user-scoped (cross-user isolation — user B never sees user A's row); `filter(user, created_at__gte=since)` ordered -created_at,-id; no row in range → 200 `{status:'pending'}` with nulls (same shape — the FE schema stays constant; absence is a state, never 404 mid-poll); pending row → pending; succeeded → `{status:'succeeded', type, credits_granted, id, date}` RAW codes; failed and refunded rows report RAW (the card maps refunded→failed-family FE-side); an OLD succeeded row older than `since` must NOT match (the false-success regression guard); parse via `parse_datetime` + `make_aware` (naive→UTC — SQLite/PG TZ consistency).
  - [x] 1.2 GREEN: StatusView (user-scoped, since-bounded, no metadata reliance, no new table/index — payments_user_created_idx covers; read-only, no locks); route `status/<str:txn_id>/` + docstring pinning "txnId = Chargily checkout id (5.2 D14)" + the V1.5 exact-match upgrade path (docs gate → column → webhook write).
  - [x] 1.3 Backend gates — pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean.

- [x] **Task 2: Backend — create-checkout `started_at` echo** (Winston Q6 amendment — the FE's since-bound needs the server's checkout start so client-ahead/behind skew cannot widen the old-row window)
  - [x] 2.1 RED: `test_checkout_view.py` extend — the create-checkout response gains an additive `started_at` (server ISO); the existing contract shape {checkout_url, checkout_id} preserved (additive only).
  - [x] 2.2 GREEN: CreateCheckoutView response + started_at.
  - [x] 2.3 Backend gates — pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean.

- [x] **Task 3: FE — data layer** (Winston Q2 contract; polling via TanStack refetchInterval per AD-20)
  - [x] 3.1 RED: `billing-service.test.ts` extend + `use-payment-status.test.tsx` (new) — `status(txnId, since)` typed; CheckoutResult gains started_at; the polling hook: enabled only with entry+user, refetchInterval 5000, refetchInterval function returns false on terminal statuses (succeeded/failed/refunded — stop-on-first-terminal also shrinks the concurrent-checkout window, John V5) and after the 60s deadline (checkout start + 60s), cardState derivation 'polling'|'success'|'timeout'|'failed' with refunded→failed mapping, retry:false, refetchOnWindowFocus:false.
  - [x] 3.2 GREEN: billingService.status + billingKeys.status + usePaymentStatus.
  - [x] 3.3 FE gates — vitest / lint 0 / tsc 0 / check:i18n clean.

- [x] **Task 4: FE — checkout stash + fallback trigger** (John V3 amendment — entry path primary, ?status= fallback static card)
  - [x] 4.1 RED: `use-checkout-redirect.test.tsx` extend — on redirect the hook stashes `{checkout_id, started_at}` in sessionStorage before navigator.assign; a later /billing mount can read it back. Plus the fallback: no entry + `?status=success` → static success card (retained billing.status.succeeded copy, NO polling); no entry + `?status=failure` → static failed card; param stripped from the URL after first render (no re-trigger on refresh); entry wins when both present.
  - [x] 4.2 GREEN: sessionStorage write on redirect (cleared by the card on terminal states); BillingPage fallback handling.
  - [x] 4.3 FE gates — vitest / lint 0 / tsc 0 / check:i18n clean.

- [x] **Task 5: FE — StatusCard component** (AC card states; Sally anatomy mandates; DESIGN.md L335)
  - [x] 5.1 RED: `status-card.test.tsx` — role="status" on the card root; polling state renders info-container/info-on-container + Loader2 spinner (aria-hidden, motion-reduce:animate-none) + "Confirming payment…"; success flips to success-container/success-on-container + lucide Check + type-flavored copy (pack → D15 verbatim, subscription → "N credits added" flavor; {n} via numerals() — AR pin: '75' never '٧٥'); timeout state info-toned static text (no spinner, no icon) "Payment received — credits will post shortly"; failed state danger-container/danger-on-container + role=alert + history.failed_note verbatim with the support mailto (link in on-container color + underline + font-medium — never text-destructive on the danger fill) + polling stops + no retry/re-pay CTA; refunded → failed family; card is a plain inline block (no overlay/trap; polling state has zero focusable elements — non-blocking); entry-wins-vs-fallback precedence.
  - [x] 5.2 GREEN: StatusCard (four states, one message-map consumer of history.failed_note/support_link).
  - [x] 5.3 FE gates — vitest / lint 0 / tsc 0 / check:i18n clean.

- [x] **Task 6: FE — BillingPage integration + toast values + pill baseline + i18n** (AC success toast + announcement order; Sally mandates 3/5/6; the 5.5 handoff — billing.status.* keys are 5.6's)
  - [x] 6.1 RED: `billing-page.test.tsx` extend + `billing-messages.test.ts` extend + `toast-provider.test.tsx` extend — the card renders on /billing when an entry exists; success: card flip commits FIRST, the toast fires from an effect AFTER the card state commit (frame separation — the test asserts the card's success text is in the DOM before the toast element appears), polling stops, entry cleared, billingKeys.plan/packs/history invalidated + session refreshed + CreditProvider baseline reset (the false-decrease guard — no announceDecrease after the grant-announced balance update); the billing.status.* rewrite (Sally's verbatim map: polling/success_pack/success_subscription added, pending/failed deleted, succeeded retained for the fallback card, timeout AR noun fix) — trilingual pins + the AR latn-numerals pin; ToastProvider `toast(key, values?)` backward-compatible extension (the success toast carries {n} as a pre-formatted Latin string).
  - [x] 6.2 GREEN: BillingPage + StatusCard wiring, ToastProvider values, CreditProvider baselineNonce + CreditsPill consume, messages ×3.
  - [x] 6.3 FE gates — vitest / lint 0 / tsc 0 / check:i18n clean.

- [x] **Task 7: UX doc fixes** (Sally mandate 7; the 5.5-flagged EXPERIENCE.md L130 staleness was due for a 5.6 doc fix)
  - [x] 7.1 EXPERIENCE.md L130 best-value badge `{colors.success}` → `{colors.primary}` solid (DESIGN.md L182-183 pins primary); L132 status-card row → success-container/success-on-container pair; the mockup chargily-return.html timeout line → the authoritative FR key ("…seront crédités sous peu" — the mock's "dans une minute" is superseded); DESIGN.md L185-190 status-card token block gains failed-background/failed-foreground (danger pair) + padding 14px + spinner/icon spec + timeout-visual note.
  - [x] 7.2 Doc-only change — gates: FE vitest/lint/tsc/i18n re-run (docs untouched by code gates but the suite must stay green).

- [x] **Task 8: Real-stack verification (PG16) + full regression** (Winston Q7 read-only hazard-free; 5.2 real-PG precedent)
  - [x] 8.1 Docker PG16 E2E — status endpoint on real PG: 401; since-bound (stale row excluded, fresh row found, malformed since → 400); user-scoping; pending → succeeded flip path; failed row raw; refunded row raw; latest-row-wins; create-checkout started_at echo present; users cleaned + container removed.
  - [x] 8.2 Full regression — backend pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean; FE vitest / lint 0 / tsc 0 / check:i18n clean.

## Dev Notes

- **Source of truth — the planning spec**: `_bmad-output/planning-artifacts/epics/epic-05-billing-subscriptions/story-06-status-card-polling.md` (ACs verbatim). FR-27 in the PRD `4-features.md` (webhook idempotent on event id; ≤60s polling + explanatory toast; final reconciliation corrects a missed grant). Epic context in the epic-5 `index.md`. UX: EXPERIENCE.md L132 (status-card component pattern: role="status", polls ≤60s, success flips + toast, timeout note, non-blocking) + L101-102 (copy: confirming / credits-posted toast) + L289-292 (UJ-2 return flow); DESIGN.md L335 (status-card anatomy: info-container/info-on-container polling with spinner; success-container/success-on-container + toast; timeout stays info-toned; never-expires note for packs); mockups/chargily-return.html (State A polling / State B success — "75 crédits ajoutés — les crédits de pack n'expirent jamais").
- **Contracts this story honors** (pre-consultation scan; D-notes amended after the persona phase):
  - D1: `POST /api/billing/create-checkout/` returns `{checkout_url, checkout_id}` — the Chargily checkout id is the 5.6 polling key (5.2 D14/Winston Q4); the 5.5 useCheckoutRedirect discards checkout_id — 5.6 must capture it. [AMENDED 2026-08-11 Winston Q6: response gains additive `started_at` (server ISO) — the FE echoes it back as `since` so the bound is exact; client-ahead skew only degrades to the graceful timeout, client-behind skew widens the old-row window.]
  - D2: Deferred-work 5.2 (B13): the webhook's shaped metadata `checkout_id` stores `data.id` (the same value normalized into `chargily_event_id`) — the REAL Chargily checkout id is the 5.6 polling key; the status lookup must NOT rely on the metadata `checkout_id` field until the payload semantics are pinned at the docs gate. The status endpoint therefore cannot map checkout id → row directly at V1; the honest lookup is user-scoped + REQUIRED since-bounded latest row (Winston Q1: (a) over (b)/(c); since absent/malformed → 400 via the _validation_response pattern — never a silent default that IS option (c)). The exact checkout-id match is V1.5 behind the docs gate (column + webhook write + exact lookup).
  - D3: The status contract mirrors HistoryView (Winston Q2): `{id, status, type, credits_granted, date}` RAW codes (pending/succeeded/failed/refunded — the ledger precedent, FE localizes per AD-8); no-row → the SAME shape with `status:'pending'` and nulls (constant FE schema — absence is a state, never 404 mid-poll); refunded reports raw (the card maps refunded→failed-family FE-side — John V1). `type` ships so the success card picks the flavor; `credits_granted` ships so the card can render "{n} credits added" (pack) vs the subscription flavor (John V2/Sally map).
  - D4: `payment_transactions` has `payments_user_created_idx` (user, -created_at) — the user-scoped lookup is index-covered (equality user + range created_at — index-scan-able on PG16 AND SQLite); ordering `-created_at, -id` (the P8 deterministic-ordering precedent); no new index, no migration (Winston Q4/Q6).
  - D5: The 60s window is FE-owned (spine L631: polls every 5s for 60s; timeout note is the degrade — never an error, never a re-pay prompt). The backend endpoint is stateless and read-only (Winston Q7: grant_credits updates status under the txn-row lock in its own transaction; the sweep only re-enqueues/settles; a poll sees pre- or post-commit state, never torn; observations monotonic pending→succeeded/failed). The FE deadline is `checkout_start + 60s`.
  - D6: The 5.5 handoff: `billing.status.*` keys were retained for 5.6. Sally's verbatim map (2026-08-11): ADD `polling` ('Confirming payment…' ×3), ADD `success_pack` (D15 verbatim ×3), ADD `success_subscription` ('{n} credits added' ×3), UPDATE `timeout` (AR noun fix only), DELETE `pending`/`failed` (the failed card REUSES `history.failed_note` + `history.support_link` so failure copy can never diverge), KEEP `succeeded` (the fallback static card's consumer — John V3).
  - D7: ToastProvider.toast(messageKey) takes a key only — the success toast needs `{n}` interpolation → a backward-compatible `toast(messageKey, values?)` extension (ToastItem carries values, t(key, values); existing callers untouched). In scope (John V6 — the toast slot is 5.6's per the 5.4 D15/D18 handoff).
  - D8: Sally's 5.3 note (pill trap handoff): the success flow must reset the CreditsPill diff baseline — a renewal grant can announce a false DECREASE (pool 250 → 200). Mandate: a provider-exposed baseline reset the pill consumes (baselineNonce + resetBaseline on CreditProvider; the pill's prevBalanceRef resets when the nonce changes — grant-announced balance updates are treated mount-like: no announceDecrease, no common.credits.updated).
  - D9: The return URLs are settings-pinned: CHARGILY_SUCCESS_URL = /billing?status=success, CHARGILY_FAILURE_URL = /billing?status=failure. Dual trigger (John V3): entry path primary (sessionStorage {checkout_id, started_at} stashed by useCheckoutRedirect before assign); fallback = the ?status= param with NO entry → static NON-polling card (success → retained succeeded copy; failure → failed card); param stripped after first render (history.replaceState); entry wins when both present.
  - D10: FE conventions (AD-20/21): TanStack Query v5 (refetchInterval for polling — the AD-20 precedent names the billing status-card), typed service methods on HttpClient, queryKeys factory, retry:false, next-intl messages, AD-8 latn numerals via the numerals() helper (pre-formatted Latin string passed as a value — next-intl applies locale numerals only to numeric values; string values insert verbatim — no bdi needed for the success strings), logical properties. Anatomy: rounded.lg, 14px padding, no border, Loader2 spinner (aria-hidden, currentColor, motion-reduce:animate-none), lucide Check success icon, timeout = info tones + no spinner/icon.
  - D11: Announcement order (Sally mandate 3): DOM order alone is NOT sufficient — two polite live-region mutations in one frame can merge/reorder. The card's success content commits in frame 1 (single atomic text swap, node stays mounted); the toast inserts in frame 2 (fired from an effect after the card state commit, never inline with the poll handler). NO aria-busy on the live region. Failed flips the card's live role to role="alert" (assertive).
  - D12: Non-blocking (Sally mandate 4): plain inline block above the plan card; no overlay/scrim/aria-modal/inert/focus-trap/focus management; polling/success/timeout states contain ZERO focusable elements; the failed state's support link is a normal in-flow focusable rendered in the on-container color + underline + font-medium (text-destructive FAILS small-text contrast on the danger-container fill — ~4.0:1 vs the 4.5:1 AA floor).
  - D13: Concurrency (John V5): latest-row-wins + since-bound can misattribute a second tab's terminal row into the first tab's window — acceptable V1 limitation, recorded (the ledger is webhook-driven/idempotent/per-row correct; harm is cosmetic card narrative); mitigation: stop polling on the FIRST terminal state; real fix (checkout-id-keyed polling) blocked by B13 until the docs gate. Deferrals: failure EMAIL (V1.5, 5.5 precedent), abandoned-vs-delayed copy (V1.5 candidate), concurrent-checkout keying (V1.5/docs gate).
- **Persona consultation record (2026-08-11, parallel subagents — Winston architect + John PM + Sally UX; Sally INCLUDED — the whole story is user-facing)**:
  - Winston (architect): APPROVED with rulings — (Q1) resolution (a): user-scoped latest-row bounded by a REQUIRED `since` ISO param (the FE echoes the server-issued checkout start); (b) is structurally dead (the metadata checkout_id stores the EVENT id — a column could never be populated pre-docs-gate) and (c) lets a stale succeeded row flip the card; since absent/malformed → 400 via the _validation_response pattern (never a silent default); no-row → 200 `{status:'pending'}` (absence is a state — never 404 mid-poll, an error branch in the 5s loop is worse). (Q2) contract mirrors HistoryView: `{id, status, type, credits_granted, date}` RAW codes; no-row returns the SAME shape with pending+nulls (constant FE schema); refunded reports raw (all four statuses possible; the card must not fabricate pending for a settled-failed charge). (Q3) txnId = non-empty string ≤255, NO regex pin (chargily.py takes data.get('id') bare — D11 envelope risk); malformed → 400, well-formed-but-unmatched → 200 pending. (Q4) payments_user_created_idx covers the lookup (equality user + range created_at — index-scan-able PG16/SQLite); no new index; no migration. (Q5) portability: plain ORM read; parse via `django.utils.dateparse.parse_datetime` + `timezone.make_aware(naive, UTC)` (3.10-safe, handles Z, SQLite-naive vs PG-aware consistency); -id tiebreak (the P8 Windows precedent); NO select_for_update (silent no-op on SQLite, pointless for a read). (Q6) resolution (a)+since is the right V1; migration DEFERRED — the exact checkout-id match is V1.5 behind the docs gate (column + webhook write + exact lookup, one migration one view change); AMENDMENT: create-checkout gains an additive `started_at` (server ISO) echoed back as `since` (client-ahead skew only degrades to the graceful timeout; client-behind skew widens the old-row window — the echo makes the bound exact). (Q7) concurrency hazard-free: grant_credits updates status under the txn-row lock in its own transaction; the sweep only re-enqueues/settles; a poll sees pre- or post-commit state, never torn; observations are monotonic (pending→succeeded/failed); read-only endpoint needs no lock.
  - John (PM): RATIFIED with THREE AC amendments recorded verbatim in the AC section (failed-state card, type-flavored success copy, dual card trigger) + rulings — (V1) failed card = option (b): danger-container/danger-on-container + the 5.5 failed-row surface verbatim (history.failed_note, mailto SUPPORT_EMAIL), polling stops, role=status announces, NO retry/re-pay CTA (the AC ban covers re-payment prompts; a support contact is not one); the timeout copy is a PROMISE ("credits will post shortly") — applying it to a terminal failed row would be actively false; a refunded row observed in-window renders the same terminal failed state (ops-manual, cannot occur meaningfully in 60s — mapping ruling, not an amendment). (V2) success copy type-flavored: pack → D15 verbatim; subscription → Sally's flavor (never claims never-expire — the 200 monthly credits DO reset at cycle end; false copy on a paying subscriber is a correctness issue, not taste); flavor from the polled row's raw type (the D3 key already ships it); same flavor in the toast. (V3) card trigger dual-path: entry primary (useCheckoutRedirect stashes {checkout_id, started_at}); fallback = settings-pinned ?status=success|failure with NO entry → STATIC non-polling card (sessionStorage is per-tab — another tab/restart/deep-link is a real hole; a polling card without an id is theater); ?status=success → retained billing.status.succeeded ("Payment successful!" — finally a real consumer for the retained key); ?status=failure → the failed card; param stripped after first render; entry wins. (V4) abandoned checkout: AC-literal acceptable, NO amendment — the 60s window cannot distinguish paid-but-delayed from abandoned; the abandoner KNOWS they didn't pay; info-toned non-blocking entry-scoped; self-corrects (no charge ever occurred); V1.5 candidate (neutral phrasing) recorded, not a 5.6 change. (V5) concurrent checkouts: acceptable V1 limitation, recorded — the ledger is webhook-driven/idempotent/per-row correct; harm is cosmetic card narrative; the real fix (checkout-id-keyed polling) is blocked by B13 until the docs gate; mitigation: stop polling on the FIRST terminal state (shrinks window overlap). (V6) ToastProvider values extension IN SCOPE (toast(messageKey, values?) backward-compatible; implementation detail of "a success toast appears" — no amendment). (V7) scope sanity: all six AC clauses deliverable; no dead endpoints/stubs (the failed card ships WITHOUT a re-pay CTA — dead-UI risk; billing.status.succeeded + failed get real consumers via the fallback card + history.failed_note reuse — no new failed keys); deferrals recorded: failure EMAIL (V1.5, 5.5 precedent), abandoned-vs-delayed copy (V1.5), concurrent-checkout keying (V1.5/docs gate), metadata checkout_id as trusted key (B13).
  - Sally (UX): CONFIRMED the surfaces and MANDATED the copy contracts — the billing.status.* rewrite (VERBATIM map): ADD `polling` EN 'Confirming payment…' FR 'Confirmation du paiement…' AR 'جارٍ تأكيد الدفع…' · ADD `success_pack` (D15 verbatim) EN '{n} credits added — pack credits never expire' FR '{n} crédits ajoutés — les crédits de pack n'expirent jamais' AR 'أُضيف {n} رصيدًا — أرصدة الحزم لا تنتهي صلاحيتها أبدًا' · ADD `success_subscription` EN '{n} credits added' FR '{n} crédits ajoutés' AR 'أُضيف {n} رصيدًا' · UPDATE `timeout` AR ONLY: 'تم استلام الدفع — سيتم إضافة الرصيد قريبًا' (noun alignment: EN/FR already use the same noun on card+toast; AR used 'النقاط' on the card but 'رصيدًا' in the D15 toast — kill the term switch; EN/FR unchanged — they match the AC verbatim) · DELETE `pending`/`failed` (no consumers; the failed card REUSES `history.failed_note` + `history.support_link` so failure copy can never diverge across surfaces) · KEEP `succeeded` (the fallback static card's consumer — John V3). Anatomy: rounded.lg (8px), padding 14px (the mock's exact proportions), NO border (tonal container register), inline flex row gap 10px, copy at {typography.small}; spinner = lucide Loader2 size-4 animate-spin aria-hidden currentColor (the on-container token — no hex) + motion-reduce:animate-none; success icon = lucide Check size-4 aria-hidden at inline-start (on-container carries text AND icons); {n} latn (AD-8): pass numerals(n) from billing-service as a PRE-FORMATTED Latin string in both card and toast — never the raw number (next-intl applies locale numerals only to numeric values; string values insert verbatim); NO bdi needed for these two strings (the digit run sits between Arabic words with no adjacent neutrals — if a string ever gains a neutral char next to {n}, it converts to rich text with bdi); timeout = same info pair as polling, NO spinner, NO icon, static text. A11y: role="status" on the card root; failed flips the live role to role="alert" (assertive — the EXPERIENCE.md L180 payment-failed precedent); announcement order — DOM order alone is NOT sufficient (two polite mutations in one frame can merge/reorder): the card's success content commits in frame 1 (single atomic text swap — node stays mounted, content never appended char-by-char) and the toast inserts in frame 2 (fired from an effect after the card state commit, never inline with the poll handler); test = after the success poll resolves, the card's success text is in the DOM BEFORE the toast element appears; NO aria-busy on the live region (suppresses the mount announcement + adds a third racing state); non-blocking = plain inline block, never a Dialog/Sheet/Popover (no overlay/scrim/aria-modal/inert/focus-trap/focus management); polling/success/timeout contain ZERO focusable elements; the failed state's support link is a normal in-flow focusable; card renders ABOVE the plan card; the 5s poll stays isolated to the card's own state (no page re-render). Toast: reuses the SAME success keys (one string per flavor, card+toast can never diverge); ToastProvider gains `toast(messageKey, values?)` (ToastItem carries values, t(key, values) — existing callers untouched). Pill baseline: the 5.6 success flow MUST reset the CreditsPill diff baseline as part of the flip (a provider-exposed baseline reset the pill consumes — the grant-announced balance update is treated mount-like: no announceDecrease, no common.credits.updated). Failed-link contrast catch: on the danger-container card, text-destructive FAILS small-text contrast (~4.0:1 vs 4.5:1) — the link renders in the on-container color + underline + font-medium (9.2:1). Doc staleness (mandate 7): EXPERIENCE.md L130 best-value badge {colors.success} → {colors.primary} solid; EXPERIENCE.md L132 status-card row → success-container/success-on-container pair; DESIGN.md L185-190 status-card token block gains failed-background/failed-foreground + padding 14px + spinner/icon spec + timeout-visual note + copy-contract references; mockup timeout line ('dans une minute') superseded by the FR key ('…seront crédités sous peu'). AR subscription flavor + AR timeout noun fix carry the working-draft caveat per EXPERIENCE.md L85 (pre-launch native review).
- **AC amendments**: 3 clauses amended verbatim by John (2026-08-11) — failed-state card, type-flavored success copy, dual card trigger — all recorded in the AC section above in the 5.5 style.

## Dev Agent Record

### Agent Model Used

opencode-go/deepseek-v4-flash

### Debug Log References

- Phase 0: story resolved from the epic-5 planning spec (5-6-status-card-polling); sprint-status key `5-6-status-card-polling` was `backlog` → flipped `in-progress` (UTF-16 LE preserved); baseline 4c4ebdd (story 5.5 review follow-up).
- Phase 2 context: ACs verbatim; FR-27 (idempotent webhook reconciliation + ≤60s polling + explanatory toast); spine L481 (GET /api/billing/status/{txnId}/) + L629-631 (Polling Bridge: every 5s for 60s, success flip + toast, timeout note); UX DESIGN.md L335 + EXPERIENCE.md L132/L101-102/L289-292 + mockup; deferred-work 5.2 B13 (metadata checkout_id untrusted — polling key = the create-checkout response id); 5.5 record (status.* retained for 5.6; toast slot; pill baseline handoff; ToastProvider key-only signature).
- Phase 3 persona consultations (2026-08-11, parallel subagents): Winston Q1-Q7 (user-scoped + REQUIRED since-bound; HistoryView-mirror contract; txnId ≤255 no-regex; no migration/index — payments_user_created_idx covers; parse_datetime + make_aware portability; started_at echo amendment; read-only concurrency-free). John V1-V7 (failed-state card AC amendment; type-flavored success AC amendment; dual-trigger AC amendment; abandoned-checkout AC-literal no-amendment; concurrent-checkout V1 limitation; ToastProvider values in scope; scope sanity — no dead endpoints, deferrals recorded). Sally mandates 1-7 (verbatim trilingual key map — polling/success_pack/success_subscription added, pending/failed deleted, succeeded retained, timeout AR noun fix; anatomy tokens; role=alert failed flip; frame-separated card→toast; no aria-busy; non-blocking inline; numerals()-only {n} latn; pill baseline reset mandate; failed-link on-container contrast; doc fixes). All recorded in Dev Notes verbatim.
- Task 1 (StatusView): RED 13 tests → GREEN 13/13; the isolation test needed email_verified_at (the 5.5 login lesson); mypy: TextChoices `.value` typing quirk → raw string literal (the test suite precedent). Gates: 949 pytest / ruff 0 / mypy 0 / check clean / makemigrations clean.
- Task 2 (started_at echo): RED 1 (exact-shape assertion updated to the additive contract) → GREEN; gates 950 pytest / ruff 0 / mypy 0 / clean.
- Task 3 (FE data layer): billingService.status + CheckoutResult.started_at + TERMINAL_PAYMENT_STATUSES/PAYMENT_POLL_* constants + billingKeys.status + usePaymentStatus. RED: the vi.mock must export the constants (vitest strict-export error — the mock fallback test); the deadline-flip needed an explicit timeout alarm (the interval's next tick can lag the deadline — state derivation is render-time only); fake-timer tests need a RELATIVE started_at (a fixed constant is interpreted against the faked clock); the terminal-state assertion needed waitFor (the flip render lags the poll). Gates: 823 vitest / lint 0 / tsc 0 / i18n clean.
- Task 4 (stash + fallback): checkoutStorage (read/stash/clear + PendingCheckout re-export) + useCheckoutRedirect stash-before-assign (stash only on success — no phantom card); 4 tests (stash, round-trip read, failure-no-stash, second-checkout-overwrites). Gates: 827 vitest / lint 0 / tsc 0 / i18n clean.
- Task 5 (StatusCard): the component needed the Task-6 provider extensions (toast values + resetBaseline) — landed together (the card is their first consumer); message keys landed early (the card's copy — polling/success_pack/success_subscription ×3, pending/failed deleted, succeeded retained, timeout AR noun fix); the 5.5 rich-text mock pattern (zero-arg support fn + support_link key) reused for the failed card. RED learnings: findByRole resolves before the query flips (wait for the flip TEXT); renderCard typing. Gates: 833 vitest / lint 0 / tsc 0 / i18n clean.
- Task 6 (integration + providers + i18n): BillingPage reads the stash (useState initializer) + ?status= fallback (readFallbackStatus) + stripStatusParam (history.replaceState — no re-trigger on refresh) + entry-wins precedence; ToastProvider toast(key, values?) backward-compatible; CreditProvider baselineNonce/resetBaseline + CreditsPill consumes (renewal grants never announce a false DECREASE — pinned test); billing-messages pins extended (status map + failed_note ×3); announcement-order test (card success copy commits before the toast fires — frame separation); the billing-page StatusCard mock honors null props. Gates: 857 vitest / lint 0 / tsc 0 / i18n clean.
- Task 7 (UX docs): EXPERIENCE.md L130 badge → primary solid; L132 status-card row → container pairs + failed state; DESIGN.md status-card token block → failed pair + 14px padding + spinner/icon/timeout notes; mockup timeout line → the authoritative FR key. Doc-only.
- Task 8 (Phase 5 real-stack): Docker PG16 (host 5433) — full billing suite on real PG 283/283; focused status E2E 11/11 (401; no-row pending shape; missing-since 400; stale-row-since-exclusion; fresh pending raw; pending→succeeded flip; failed raw; refunded raw; user-B isolation; started_at echo; echoed-since round-trip); users 0 rows + container removed + temp script deleted. Final regression: backend 950 pytest / ruff 0 / mypy 0 / check clean / makemigrations clean; FE 857 vitest / lint 0 / tsc 0 / i18n clean.

### Completion Notes List

- Story 5.6 implemented end-to-end (TDD red→green per task): backend — `GET /api/billing/status/{txnId}/` (StatusView: user-scoped + REQUIRED since-bound — a row created before the checkout started can never flip the card; HistoryView-mirror contract `{id, status, type, credits_granted, date}` RAW codes; no-row → 200 pending shape; failed/refunded reported raw; txnId ≤255 no-regex; parse_datetime + make_aware TZ-consistent; read-only/lock-free; index-covered — zero migration) + create-checkout additive `started_at` echo (Winston Q6 — the exact since-bound); route + docstring pinning the V1.5 exact-match upgrade path. FE — billingService.status + CheckoutResult.started_at + poll constants; billingKeys.status; usePaymentStatus (refetchInterval 5000, stop-on-first-terminal, deadline alarm → timeout flip, refunded→failed); checkoutStorage + useCheckoutRedirect stash-before-assign; StatusCard (4 states — polling info pair + Loader2, success container pair + Check + type-flavored {n} copy, timeout info pair static, failed danger pair + role=alert + the 5.5 failure surface with on-container support link; role=status root; no aria-busy; zero focusables in non-failed states); BillingPage integration (entry path + ?status= static fallback + param strip + entry-wins); ToastProvider toast(key, values?) backward-compatible; CreditProvider baselineNonce/resetBaseline + CreditsPill consume (no false DECREASE); the billing.status.* message rewrite (Sally's verbatim map ×3) + 6 new trilingual pins + the AR timeout noun fix; announcement-order frame separation (card before toast).
- Deferred-work items: the 5.2 B13 "shaped-metadata checkout_id" ruling is HONORED BY DESIGN (the lookup never touches chargily_metadata; the exact checkout-id match stays deferred behind the docs gate — Winston Q6 records the V1.5 upgrade path: column + webhook write + exact lookup). New records: the concurrent-checkout V1 limitation (John V5 — latest-row-wins within a since-bound can misattribute a second tab's terminal row; mitigation stop-on-first-terminal; the real fix is checkout-id-keyed polling behind the docs gate); the abandoned-checkout timeout copy (AC-literal — the 60s window cannot distinguish paid-but-delayed from abandoned; V1.5 neutral-phrasing candidate); failure EMAIL stays V1.5 (5.5 precedent).
- Persona decisions all implemented: Winston Q1-Q7 (since-required 400s, raw contract, no migration, started_at echo, read-only), John's 3 AC amendments verbatim + V4-V7 (abandoned AC-literal, concurrent limitation, toast values in scope, scope sanity), Sally's verbatim copy map + anatomy/a11y mandates (role=alert, frame separation, no aria-busy, numerals-only {n}, pill baseline, on-container failed link) + the 7 doc fixes.
- Gates: backend 950 pytest / ruff strict 0 / mypy strict 0 / check clean / makemigrations clean; FE 857 vitest / lint 0 / tsc 0 / check:i18n clean; real-PG16 billing suite 283/283 + focused status E2E 11/11.

### File List

- backend/apps/billing/views.py (StatusView + CreateCheckoutView started_at echo)
- backend/apps/billing/urls/billing.py (status route)
- backend/apps/billing/tests/test_status_view.py (new), test_checkout_view.py (extend)
- frontend/src/lib/api/billing-service.ts (status method + types + CheckoutResult.started_at)
- frontend/src/lib/queryKeys/billing.ts (status key)
- frontend/src/hooks/usePaymentStatus.ts (new — polling hook)
- frontend/src/hooks/useCheckoutRedirect.ts (checkout stash)
- frontend/src/components/billing/StatusCard.tsx (new)
- frontend/src/components/billing/BillingPage.tsx (StatusCard wiring + fallback trigger)
- frontend/src/components/providers/ToastProvider.tsx (values support)
- frontend/src/components/providers/CreditProvider.tsx (baselineNonce/resetBaseline) + CreditsPill.tsx (consume)
- frontend/messages/{en,fr,ar}.json (billing.status.* rewrite)
- frontend/src/__tests__/status-card.test.tsx (new), use-payment-status.test.tsx (new), use-checkout-redirect.test.tsx (extend), billing-page.test.tsx (extend), billing-messages.test.ts (extend), toast-provider.test.tsx (extend)
- _bmad-output/planning-artifacts/ux-designs/ux-profitable-saas-service-2026-07-19/{EXPERIENCE.md,DESIGN.md,mockups/chargily-return.html} (doc fixes)

### Change Log

- 2026-08-11: Story created (in-progress) from the epic 5.6 planning spec; sprint-status 5-6 → in-progress (UTF-16 LE preserved); epic-5 already in-progress. Baseline commit 4c4ebdd.
- 2026-08-11: Persona consultations (parallel subagents — Winston architect + John PM + Sally UX; Sally INCLUDED — the whole story is user-facing): Winston Q1-Q7 (user-scoped + REQUIRED since-bound resolution; HistoryView-mirror raw contract incl. refunded; txnId ≤255 no-regex; no migration/no index; parse_datetime + make_aware portability; started_at echo amendment — additive create-checkout field; read-only concurrency-free). John's THREE AC amendments recorded verbatim in the AC section (failed-state card with the 5.5 failure surface + no re-pay CTA; type-flavored success copy; dual card trigger with the static ?status= fallback) + V4-V7 rulings (abandoned-checkout AC-literal; concurrent-checkout V1 limitation with stop-on-first-terminal mitigation; ToastProvider values in scope; scope sanity — no dead endpoints, deferrals recorded). Sally's verbatim billing.status.* map (polling/success_pack/success_subscription added, pending/failed deleted — failed reuses history.failed_note, succeeded retained for the fallback card, timeout AR noun fix) + anatomy/a11y mandates (role=alert failed flip, frame-separated card→toast, no aria-busy, numerals()-only {n} latn, non-blocking inline, pill baseline reset, failed-link on-container contrast) + doc-fix mandate 7. Dev Notes + AC amendments added.
- 2026-08-11: Implemented (TDD) — Task 1 StatusView (13 tests), Task 2 started_at echo (1 test), Task 3 FE data layer (10 tests: 3 service + 8 hook — the deadline-alarm learning), Task 4 stash (4 tests), Task 5 StatusCard (7 tests incl. the frame-separation announcement-order test), Task 6 BillingPage + ToastProvider values + CreditProvider baseline + i18n (billing-page fallback matrix 7 new tests, credits-pill baseline-reset test, toast values test, 5 new message pins ×3 — failed_note + 4 status keys; the frame-separation announcement-order proof lives in status-card.test.tsx, not billing-page.test.tsx (the page mocks StatusCard)), Task 7 UX doc fixes (EXPERIENCE.md L130/L132 + DESIGN.md token block + mockup timeout line). Gates green after every task (backend 949 → 950; FE 823 → 857; lint/tsc/i18n clean throughout). Phase 5 real-stack: PG16 (host 5433) clean migrate; billing suite on real PG 283/283; focused status E2E 11/11 (401; no-row pending shape; missing-since 400; stale-row-since-exclusion; fresh pending raw; pending→succeeded flip; failed raw; refunded raw; user-B isolation; started_at echo; echoed-since round-trip); E2E rows cleaned (0 users, 0 txns), container removed, temp script deleted. Status → review (sprint-status 5-6 → review; epic-5 stays in-progress).

## Review Findings

Full-mode review 2026-08-11 (parallel: Blind Hunter 8 + Edge Case Hunter 5 + Acceptance Auditor — 6/6 AC groups SATISFIED, amendments 1-2 SATISFIED, amendment 3 PARTIAL (P3, resolved); review subagents ran on the same model family — the "different LLM" preference was not available and is noted). Triage: 7 patches + 4 dismissed + 3 record fixes. All 7 patches applied + re-verified: 951 backend pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean; FE 862 vitest / lint 0 / tsc 0 / check:i18n clean; full billing suite on real PG16 284/284 (incl. the new P5 cap test).

- [x] [Review][Patch] P1 (HIGH) — pill baseline reset captures the PRE-GRANT balance (Blind 2 / Edge 1): the StatusCard bumps the baseline nonce and refreshes the session in the same effect — the grant-announced balance lands a FRAME LATER via the async /me probe; capturing the baseline at the nonce frame diffs the grant balance against the pre-grant value and fires the exact false DECREASE the D8 handoff was built to prevent (the credits-pill test batched reset+delta into one click — production never batches a network result with the effect that called the reset). Now: the nonce branch sets a `pendingResetRef` and the capture lands on the NEXT balance-change frame (the grant balance itself); a real subsequent spend still announces. New split-frame test (reset in frame A, balance in frame B, real spend in frame C announces). [frontend/src/components/layout/CreditsPill.tsx] + credits-pill.test.tsx.
- [x] [Review][Patch] P2 (MEDIUM) — corrupt stash → NaN deadline → infinite polling (Blind 3 / Edge 4): `started_at` typed-string-but-unparseable (or empty checkout_id) passed the storage type checks; `new Date(...).getTime()` → NaN → the deadline alarm fired at 0ms once (never re-armed), `Date.now() >= NaN` stayed false, the interval never stopped, and the server 400'd every poll — the spinner ran forever with the stash never cleared. Now: `readPendingCheckout` rejects non-ISO started_at and empty checkout_id (clears the corrupt entry — the ?status= fallback still applies); the hook treats a NaN deadline as deadline-passed (belt-and-braces — degrades to the timeout note instead of spinning). New tests (unparseable + empty-id rejection, stash cleared). [frontend/src/lib/billing/checkoutStorage.ts, hooks/usePaymentStatus.ts] + use-checkout-redirect.test.tsx.
- [x] [Review][Patch] P3 (MEDIUM) — `?status=` param never stripped on the entry-wins path (Blind 4 / Edge 3 / Auditor amendment-3 PARTIAL): the strip effect ran only when the fallback rendered; an entry-wins session left the param in the URL, so after the stash cleared (terminal state) a refresh re-triggered the static card — the exact re-trigger the strip was designed to prevent, and a genuine ?status=success return could be suppressed by a stale entry. Now: the strip runs unconditionally on mount (both paths); the fallback state still reads the param before the strip. New test (entry-wins path strips). [frontend/src/components/billing/BillingPage.tsx] + billing-page.test.tsx.
- [x] [Review][Patch] P4 (MEDIUM) — succeeded + null credits_granted skipped ALL success side effects (Edge 2): the toast/invalidate/refresh/baseline effect was gated on `creditsGranted === null` — a succeeded row with a null count (legacy/manual data) stopped the polling, cleared the stash, and left plan/packs/history/pill permanently stale with no recovery. Now: only the TOAST needs the count; invalidation + session refresh + baseline reset always run on success. [frontend/src/components/billing/StatusCard.tsx]
- [x] [Review][Patch] P5 (MEDIUM) — no server-side `since` cap (Blind 5): the false-success guard ("a row created before the checkout began can never be that checkout's row") held only while the client echoed the server-issued started_at honestly — a tampered stash with an ancient `since` made the mount fetch return the user's OLDEST row in range (an old succeeded row flips the card green with the wrong credits). Self-scoped but it defeated the endpoint's headline contract exactly like Winston's rejected option (c). Now: `since` older than 24h → 400 invalid_since (the polling + reconciliation horizon is minutes; 24h is generous for any realistic return while bounding the lookup to this checkout's era). New test (48h-old since → 400). [backend/apps/billing/views.py] + test_status_view.py.
- [x] [Review][Patch] P6 (LOW) — StrictMode double-fire of the success side effects (Blind 6): the handledRef guarded only the storage-clear effect; the toast/baseline/invalidate/refresh effect had no dedupe — dev StrictMode double-invokes effects on mount-commit (two toasts, double refresh probes). Now: a successHandledRef guards the whole success-effect block (the state is terminal, so a ref can never block a legitimate re-fire). [frontend/src/components/billing/StatusCard.tsx]
- [x] [Review][Patch] P7 (LOW) — orphan stash when the navigation itself throws (Edge 5): create-checkout succeeded and stashed, then `navigator.assign` threw (blocked popup/sandboxed iframe) — the catch swallowed it, leaving an entry that would make a later /billing visit poll a checkout that never began and promise credits on timeout. Now: the assign failure clears the stash + sets the error state. New test (assign throws → no orphan stash). [frontend/src/hooks/useCheckoutRedirect.ts] + use-checkout-redirect.test.tsx.
- [x] [Review][Dismiss] txn_id ignored by the lookup (Blind 1): the user-scoped + since-bounded resolution cannot key on the checkout id at V1 — the exact match is blocked by the 5.2 B13 docs gate (metadata checkout_id stores the event id); the misattribution window requires two deliberate concurrent checkouts in one 60s span and the harm is the card narrative (both grants still land — the ledger is webhook-driven/idempotent). This is the RECORDED D13 V1 limitation (John V5 verdict); the real fix is checkout-id-keyed polling behind the docs gate. Blind Hunter's severity note (a false-failure card for a still-pending checkout) recorded for the V1.5 fix priority.
- [x] [Review][Dismiss] Static fallback never refreshes data (Blind 8): the ?status= fallback is a one-shot announcement by design (John V3 amendment — no toast, no polling, "static"; the entry path owns the success side effects). A deep-link return with cleared storage gets the confirmation card; the plan/pill refetch on the next window-focus pass.
- [x] [Review][Dismiss] Success card "0 credits added" fabrication on null credits_granted (Blind 7 half): unreachable via the app — the grant task writes status + credits_granted + reconciled_at ATOMICALLY under the row lock (tasks.py), the reconcile sweep only re-enqueues, and the settle paths write `failed` not `succeeded`; succeeded+null is legacy/manual DB data only. The P4 patch independently fixes the invalidation gap for that legacy case.
- [x] [Review][Record fix] Task 6.1 wording (Auditor 2): the record claimed billing-page.test.tsx asserts card-before-toast DOM order — the StatusCard is mocked there; the frame-separation proof lives in status-card.test.tsx (card success copy in the DOM before the toast fires). Record wording corrected.
- [x] [Review][Record fix] "6 new trilingual pins" vs 5 (Auditor 3): failed_note + 4 status keys = 5 pins. Record corrected.
- [x] [Review][Record fix] Task 8 claims (Auditor 4): the PG16 E2E results rest on the record — re-verified in this review by the real-PG billing suite 284/284 after the P5 backend patch.

### Change Log (review additions)

- 2026-08-11: Full-mode code review (Blind Hunter 8 + Edge Case Hunter 5 + Acceptance Auditor — 6/6 AC groups SATISFIED, amendments 1-2 SATISFIED, amendment 3 PARTIAL (P3), no VIOLATIONS). Triage: 7 patches + 4 dismissed + 3 record fixes (user-approved: apply all 7). Patches applied + re-verified: 951 backend pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean; FE 862 vitest / lint 0 / tsc 0 / check:i18n clean; billing suite on real PG16 284/284 (incl. the P5 since-cap test). Status → done (sprint-status 5-6 → done; epic-5 stays in-progress).