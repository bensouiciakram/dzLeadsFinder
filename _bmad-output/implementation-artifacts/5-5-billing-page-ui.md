---
story_id: 5.5
epic: 5
title: Story 5.5 Ã¢â‚¬â€ Billing Page UI
Status: review
frs: [FR-24, FR-25, FR-26, FR-28]
ads: [AD-7, AD-8]
baseline_commit: c9815a6ef08afd0c687ff9b60ff51ec6910d14cb
---

# Story 5.5: Billing Page UI

Status: review

## Story

As a **user managing my account**,
I want **a billing page that shows my current plan, available add-on packs, payment history, and the danger zone for cancellation**,
So that **I have full visibility and control over my subscription**.

## Acceptance Criteria

**Given** the Billing Page (`/billing`)
**When** an authenticated user navigates to it
**Then** it has four stacked sections: [AMENDED 2026-08-10, John PM verdict Ã¢â‚¬â€ the four-section shape applies to a user with an ACTIVE subscription; for free users and all non-active states the Danger Zone section is omitted (see the Danger Zone amendment)]

1. **Plan Card** Ã¢â‚¬â€ current plan, tier, renewal date, credits left this cycle
2. **Pack Cards** Ã¢â‚¬â€ two side-by-side cards (500 DZD/75 and 1,500 DZD/250)
3. **Payment History** Ã¢â‚¬â€ Chargily receipts table
4. **Danger Zone** Ã¢â‚¬â€ cancel subscription with confirmation

**Given** the Plan Card
**When** a free user views it
**Then** it shows "Free tier" with an "Upgrade" CTA that opens the Upgrade Dialog [AMENDED 2026-08-10, John PM verdict Ã¢â‚¬â€ the UpgradeDialog is a 5.7 deliverable (epic index); the 5.5 'Upgrade' CTA performs the direct Chargily redirect via `POST /api/billing/create-checkout/` (type=subscription Ã¢â‚¬â€ the live, tested 5.3 FR-24 path), never a dead stub; 5.7 re-points this CTA slot into the single UpgradeDialog]
**When** a Starter user views it
**Then** it shows "Starter Ã¢â‚¬â€ 1,500 DZD/mo Ã¢â‚¬â€ renews on {date}"
**And** shows remaining subscription credits [AMENDED 2026-08-10, John PM verdict Ã¢â‚¬â€ the 5.3 plan contract has no balance field (5.3 D12); the 5.5 plan view EXTENDS with the cycle balance so the card can render "credits left this cycle": `credits_left_cycle` = the remaining subscription-pool balance (the combined pill stays the header surface); two AC-unspecified states ship with the card: `failed_renewal` = read-only "access until {date}" + retry-payment CTA via create-checkout (the banner copy stays 5.7's); `expired` = "Subscription ended" + re-subscribe CTA via create-checkout (the same slot 5.7 re-points into the dialog)]

**Given** the Pack Cards
**When** they render
**Then** they are {colors.card}, 1px {colors.border}, {rounded.lg}, {spacing.gutter-desktop} padding
**And** the 1,500 DZD card has a "Best value" badge ({rounded.full}, {colors.primary} / {colors.primary-foreground}, top-inline-end)
**And** hover shifts border to {colors.primary}; focus-visible gets the standard ring

**Given** the Payment History table
**When** it renders
**Then** it shows past Chargily transactions with date, amount, type, status [AMENDED 2026-08-10, John PM verdict Ã¢â‚¬â€ ALL of the user's transactions, newest-first, no pagination at V1 volumes (user-scoped append-only table, ~15-25 rows/year per subscriber; pagination is a V1.5 concern); ALL statuses render Ã¢â‚¬â€ pending, succeeded, and failed; a failed row is visible with a localized explanatory state plus a support-contact line (the user decision's 5.5 failure surface Ã¢â‚¬â€ the deferred-work "Failed-charge user notification" RESOLVED for the visible surface; failure EMAIL deferred V1.5; the 5.7 banner is subscription-state-driven and unaffected); type and status labels localize ar/fr/en with latn numerals per AD-8 (exact copy: Sally R1d)]

**Given** the Danger Zone
**When** a Starter user clicks "Cancel subscription" [AMENDED 2026-08-10, John PM verdict Ã¢â‚¬â€ cancellation applies only to an ACTIVE subscription: `failed_renewal`/`expired`/no-subscription rows are NOT cancellable (exit paths are re-pay or auto-expire; the 5.7 banner owns that messaging) and return 409 `subscription_not_active`; an already-cancelled row returns an idempotent 200 Ã¢â‚¬â€ the intent is already satisfied, a repeat click must not error; the Danger Zone section renders only for the active state Ã¢â‚¬â€ free users and all non-active states omit the section entirely, with the Plan Card carrying the state communication]
**Then** a confirmation dialog states: "You will keep access until {date}. No refund for the current cycle."
**And** on confirm: subscription status = 'cancelled', cancelled_at = now
**And** access continues until current_period_end
**And** no auto-renewal occurs [AMENDED 2026-08-10, John PM verdict Ã¢â‚¬â€ interpretation recorded verbatim: 'the user's subscription will not renew without their payment' Ã¢â‚¬â€ the system performs no renewal without a paid Chargily webhook event (renewals are webhook-triggered, never time-based) and never re-bills the user by itself; a payment the user authorizes after cancellation is always honored (paid-event-never-skipped) and re-activates the subscription Ã¢â‚¬â€ the promise is about our behavior, not Chargily's retry behavior. NO backend change to the renewal/grant handler is required in 5.5; the cancel endpoint only writes status='cancelled' + cancelled_at]

**Given** the cancellation result
**When** a user cancels
**Then** the Plan Card updates to show "Cancelled Ã¢â‚¬â€ access until {date}"
**And** a "Reactivate" button is available (which resumes subscription from next billing date) [AMENDED 2026-08-10, John PM verdict Ã¢â‚¬â€ Reactivate is a PAID path: it routes through `POST /api/billing/create-checkout/` (type=subscription) to Chargily Ã¢â‚¬â€ no free reactivation exists (no auto-renewal machinery); on paid webhook the same cancelled row re-activates anchored at `max(current_period_end, now)` + 1 month (the renewal anchor-preservation pattern Ã¢â‚¬â€ a small `_grant_creation` anchor amendment; no future-dated grant machinery, no proration in V1), with the no-rollover credits reset landing at payment (the early-renewal precedent: 200 fresh credits on payment, remaining old-cycle credits zeroed then); the button renders on the Plan Card; `failed_renewal` re-pay remains now-anchored as shipped in 5.3 Ã¢â‚¬â€ unchanged by this story]

## Tasks / Subtasks

- [x] **Task 1: Backend Ã¢â‚¬â€ `GET /api/billing/packs/`** (AC Pack Cards; the 5.4 RP7 handoff; Winston Q1)
  - [x] 1.1 RED: `backend/apps/billing/tests/test_packs_view.py` Ã¢â‚¬â€ 401 anonymous; exact contract `{packs: [{amount, credits, description, unit_price, never_expires, best_value}], never_expires: true}`; unit_price strings '6.7'/'6.0' verbatim from the module constants; descriptions exact; best_value computed as amount == max(PACK_PRICES) (250 pack true, 500 pack false Ã¢â‚¬â€ never hardcoded literals).
  - [x] 1.2 GREEN: PacksView serving the pricing.py table (module-level imports Ã¢â‚¬â€ the views.py precedent).
  - [x] 1.3 Backend gates Ã¢â‚¬â€ 910 pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean.

- [x] **Task 2: Backend Ã¢â‚¬â€ `GET /api/billing/history/`** (AC Payment History; Winston Q2; John V4 Ã¢â‚¬â€ ALL statuses)
  - [x] 2.1 RED: `backend/apps/billing/tests/test_history_view.py` Ã¢â‚¬â€ 401 anonymous; user-scoped (no cross-user leakage); newest-first (-created_at, -id); cap 50 (51 rows Ã¢â€ â€™ 50); all statuses INCLUDED (pending/succeeded/failed Ã¢â‚¬â€ no filter); raw codes; `{results: [{id, date, amount_dzd, type, status, credits_granted}]}`; empty list for a fresh user; `refunded` rows render too.
  - [x] 2.2 GREEN: HistoryView (read-only, cap 50, no pagination params).
  - [x] 2.3 Backend gates Ã¢â‚¬â€ 917 pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean.

- [x] **Task 3: Backend Ã¢â‚¬â€ `POST /api/billing/cancel/`** (AC Danger Zone; Winston Q3; John V2)
  - [x] 3.1 RED: `backend/apps/billing/tests/test_cancel_view.py` Ã¢â‚¬â€ 401 anonymous; ACTIVE sub Ã¢â€ â€™ 200 {status: 'cancelled', cancelled_at ISO}; already-cancelled Ã¢â€ â€™ idempotent 200; failed_renewal Ã¢â€ â€™ 409 'subscription_not_active'; expired Ã¢â€ â€™ 409 'subscription_not_active'; no row Ã¢â€ â€™ 409 'subscription_not_found'; user.tier untouched (5.7 cancel-sync deferral); cancelled_at set iff cancelled (constraint); concurrent cancel vs grant re-activation serializes (userÃ¢â€ â€™sub lock order Ã¢â‚¬â€ REVIEW: delivered as a real-PG two-thread race, P8 of the review, 8/8 deterministic); cancel does NOT create a renewal/ledger row.
  - [x] 3.2 GREEN: CancelView (userÃ¢â€ â€™sub lock order, status re-check under the sub lock, update_fields=['status', 'cancelled_at'], no tier write, no Chargily call).
  - [x] 3.3 Backend gates Ã¢â‚¬â€ 927 pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean.

- [x] **Task 4: Backend Ã¢â‚¬â€ PlanView balances extension + reactivation anchor + orphan pin** (AC Plan Card credits + Reactivate; Winston Q4/Q5/Q6; John V3)
  - [x] 4.1 RED: `test_plan_view.py` extend Ã¢â‚¬â€ additive `balances` key {subscription_balance, pack_balance, display_balance} (from user_balances); the 5.3 shape {tier, status, renews_on} preserved (the 5.7 consumers untouched Ã¢â‚¬â€ 2 pin tests updated to the additive key, the documented contract extension); `test_tasks.py` Ã¢â‚¬â€ cancelled-row re-activation anchors at max(current_period_end, now) (billing date preserved when reactivating before period end; now when after); failed_renewal re-pay stays now-anchored (5.3 pin untouched); orphan pin: renewal webhook for a CANCELLED row Ã¢â€ â€™ orphan path Ã¢â€ â€™ ACTIVE row + grant + ERROR alarm (Winston Q6 Ã¢â‚¬â€ no 5.5 change, the test pins the ruling; passed on the existing behavior by construction).
  - [x] 4.2 GREEN: PlanView balances + `_grant_creation` cancelled-branch anchor amendment (`anchor = max(current_period_end, now)` for cancelled; failed_renewal/expired keep the now-anchor).
  - [x] 4.3 Backend gates Ã¢â‚¬â€ 934 pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean.

- [x] **Task 5: FE Ã¢â‚¬â€ data layer** (Winston Q9; Sally R2/R3 Ã¢â‚¬â€ billing-service + billingKeys + useBilling)
  - [x] 5.1 RED: `billing-service.test.ts` Ã¢â‚¬â€ plan/packs/history typed responses; cancel with 409-code narrowing ('subscription_not_active'/'subscription_not_found'); SUPPORT_EMAIL constant; formatBillingDate helper (dateStyle medium + latn; withTime variant = the CreditsPage formatTimestamp precedent); no new formatting code (numerals() reuse).
  - [x] 5.2 GREEN: billing-service.ts + queryKeys/billing.ts + hooks/useBilling.ts (3 useQuery + 1 useMutation; retry:false, refetchOnWindowFocus:false; cancel onSuccess invalidates billingKeys.plan).
  - [x] 5.3 FE gates Ã¢â‚¬â€ 700 vitest / lint 0 / tsc 0 / check:i18n 485x3 clean.

- [x] **Task 6: FE Ã¢â‚¬â€ PlanCard** (AC Plan Card; Sally R1b/R3a Ã¢â‚¬â€ 5 states)
  - [x] 6.1 RED: `plan-card.test.tsx` Ã¢â‚¬â€ 5 states (free/active/failed_renewal/cancelled/expired) driven by subscription status never user.tier; headline strings pinned verbatim (Ã¢â€“Â² trilingual); credits line label + tabular-nums value (subscription pool for starter/cancelled, display balance for free/expired); Upgrade/Reactivate/Retry/Resubscribe CTAs Ã¢â€ â€™ create-checkout redirect; cancelled-state role="status" wraps the headline text only; <bdi> around the date in AR.
  - [x] 6.2 GREEN: PlanCard + the i18n rewrite landed early (Sally's verbatim billing.* map in en/fr/ar; `billing.upgrade_stub` RESTORED Ã¢â‚¬â€ live-consumed by the 4.5 ExportModal until 5.7 replaces the stub toast with the UpgradeDialog, deviation recorded; `billing.currency` added for the pack price line, D25).
  - [x] 6.3 FE gates Ã¢â‚¬â€ 712 vitest / lint 0 / tsc 0 / check:i18n clean.

- [x] **Task 7: FE Ã¢â‚¬â€ PackCards** (AC Pack Cards; Sally R3b Ã¢â‚¬â€ D15 contracts verbatim)
  - [x] 7.1 RED: `pack-cards.test.tsx` Ã¢â‚¬â€ two cards side-by-side Ã¢â€°Â¥md / stacked below; D15 copy pinned verbatim Ã¢â€“Â² ("Never expires"/"Best value"/"Buy" via common.actions.buy); unit price from the SERVER strings ('6.7 DZD/credit' shape via packs.unit_price, '.' fixed in all locales); best-value badge on the 250 pack ({rounded.full}, primary/primary-foreground, top-inline-end); credit counts headline + tabular-nums; buy Ã¢â€ â€™ create-checkout {type:'pack'} redirect; busy state label swap + disabled; checkout failure Ã¢â€ â€™ inline error box (role=alert, no toast).
  - [x] 7.2 GREEN: PackCards (grid-cols-1 md:grid-cols-2; hover border-primary; badge top-4 end-4; Ã¢Å“â€œ aria-hidden success check; buy label via common.actions.buy).
  - [x] 7.3 FE gates Ã¢â‚¬â€ 721 vitest / lint 0 / tsc 0 / check:i18n clean.

- [x] **Task 8: FE Ã¢â‚¬â€ PaymentHistoryTable** (AC Payment History; Sally R3c Ã¢â‚¬â€ all statuses + failed sub-row)
  - [x] 8.1 RED: `payment-history-table.test.tsx` Ã¢â‚¬â€ 4 columns (date/amount/type/status); ledger-anatomy table (table-fixed, px-3, scope=col); status dot + label (never color-only; dot colors pass 1.4.11; refunded = muted slate dot); type/status labels localized with raw-code fallback; failed rows render the explanatory sub-row with the mailto support link (rich {support}); empty state; pending/succeeded/failed/refunded all render; latn numerals + <bdi> date cells.
  - [x] 8.2 GREEN: PaymentHistoryTable (Fragment-keyed rows; failed sub-row colSpan=4; SUPPORT_EMAIL mailto; dot classes per status).
  - [x] 8.3 FE gates Ã¢â‚¬â€ 742 vitest / lint 0 / tsc 0 / check:i18n clean.

- [x] **Task 9: FE Ã¢â‚¬â€ DangerZone** (AC Danger Zone; Sally R3d Ã¢â‚¬â€ single-step dialog, safe-control initial focus)
  - [x] 9.1 RED: `billing-danger-zone.test.tsx` Ã¢â‚¬â€ renders ONLY for the active state (John V2 amendment; REVIEW P1 Ã¢â‚¬â€ my draft wording "active and failed_renewal" contradicted the verbatim amendment; failed_renewal is non-cancellable by design Ã¢â‚¬â€ the backend 409s it, the CTA would be dead UI; the 5.7 banner owns that state); hidden for free/expired/failed_renewal/cancelled; dialog states the AC copy "You will keep access until {date}. No refund for the current cycle." Ã¢â€“Â² trilingual; initial focus on "Keep my subscription"; confirm Ã¢â€ â€™ cancel mutation Ã¢â€ â€™ dialog closes on success only (REVIEW P2) + plan query invalidated; error Ã¢â€ â€™ in-dialog role=alert with the dialog staying open; danger-on-container description color (never muted on the danger fill).
  - [x] 9.2 GREEN: DangerZone (Base UI Dialog; single confirm step; initialFocus via the data-billing-keep selector Ã¢â‚¬â€ Base UI schedules focus via rAF, tests flush with an rAF round-trip; description in danger-on-container per Sally Ã¢â‚¬â€ the settings component's muted-on-danger latent AA failure flagged for a drive-by fix, out of scope).
  - [x] 9.3 FE gates Ã¢â‚¬â€ 742 vitest / lint 0 / tsc 0 / check:i18n clean.

- [x] **Task 10: FE Ã¢â‚¬â€ BillingPage + route + i18n** (AC page structure; Sally R1/R5)
  - [x] 10.1 RED: `billing-page.test.tsx` + route Ã¢â‚¬â€ four stacked sections (active user); Danger Zone omitted for free/cancelled/expired; h1 + meta; the billing.* message namespace rewrite (Sally's verbatim map; deleted keys: free_plan/starter_plan/starter_description/free_description/upgrade_stub/packs.pack*_*/packs.buy/history.credits/cancel.*/dzone.delete_*; retained: title/current_plan/renewal_date (5.7 handoff)/failed_renewal (5.7 handoff)/status.* (5.6's Ã¢â‚¬â€ untouched); packs.buy Ã¢â€ â€™ common.actions.buy reuse); check:i18n clean. Plus `billing-messages.test.ts` Ã¢â‚¬â€ the Ã¢â€“Â² trilingual verbatim pins (23 keys Ãƒâ€” 3 locales = 63 assertions; the ASCII-apostrophe message ground truth aligned).
  - [x] 10.2 GREEN: BillingPage + app/[locale]/billing/page.tsx (metadata per the settings precedent; dir comes from the root layout) + messages.
  - [x] 10.3 FE gates Ã¢â‚¬â€ 808 vitest / lint 0 / tsc 0 / check:i18n clean.

- [x] **Task 11: Real-stack verification (PG16) + full regression** (Winston Q8 Ã¢â‚¬â€ the 4.1/5.3 E2E-race precedent)
  - [x] 11.1 Docker PG16 E2E Ã¢â‚¬â€ 27/27 (host 5433; docker daemon was DOWN at start Ã¢â€ â€™ Docker Desktop started, the 5.3 lesson; clean migrate; the 5.3 login lesson applied Ã¢â‚¬â€ email_verified_at required for the JWT cookie path; PYTHONIOENCODING=utf-8 Ã¢â‚¬â€ the console codec chokes on 'Ã¢â€ â€™' in check names): packs contract (amounts/unit prices/best_value/descriptions/never_expires/401), history (all statuses, newest-first, raw codes, 50-cap), cancel (200 + body, status+cancelled_at written, access-until preserved, idempotent re-cancel, tier untouched, zero ledger rows, constraint re-check, 409 not_found/not_active), plan balances key, cancelled-row reactivation anchored at current_period_end on real PG (billing date preserved) + cancelled_at cleared + 200 grant; users self-cleaned (0 rows) + container removed + temp script deleted.
  - [x] 11.2 Full regression Ã¢â‚¬â€ backend 934 pytest / ruff 0 / mypy strict 0 / check clean / makemigrations clean; FE 808 vitest / lint 0 / tsc 0 / check:i18n clean (both suites re-run after the last change each side).

## Dev Notes

- **Source of truth Ã¢â‚¬â€ the planning spec**: `_bmad-output/planning-artifacts/epics/epic-05-billing-subscriptions/story-05-billing-page-ui.md` (ACs verbatim). FR-24/25/26/28 in the PRD `4-features.md` (FR-26 L244-250 Ã¢â‚¬â€ self-serve cancel with access-until-date confirmation; refunds V1 = documented-payment-errors only, ops-manual). Epic context in the epic-5 `index.md`. UX: EXPERIENCE.md L130-132 + L250-253 (billing sections, pack cards, status-card is 5.6), DESIGN.md L334 (pack-card anatomy) + L288 (headline) + L335 (status-card Ã¢â‚¬â€ 5.6).
- **Contracts this story honors** (pre-consultation scan; D-notes amended after the persona phase):
  - D1: `GET /api/billing/plan/` contract {tier, status, renews_on} (5.3 D12) Ã¢â‚¬â€ 200 always incl. the free shape; the 5.5 Plan Card consumes it. "Credits left this cycle" is NOT in the contract Ã¢â‚¬â€ the 5.3 D12 comment says balances are "the pill endpoint's job" but no such endpoint exists (the pill reads `user.credits_balance` from the session); a plan-view extension vs a new endpoint is a Winston verdict.
  - D2: The 5.4 pack contract (pricing.py Ã¢â‚¬â€ PACK_PRICES {500:75, 1500:250}, PACK_DESCRIPTIONS, PACK_UNIT_PRICES '6.7'/'6.0', PACK_NEVER_EXPIRES) is Python-side; RP7 recorded the HTTP surface as a 5.5 deliverable ("the FE cannot import the Python module"). The spine API table lists `GET /api/billing/packs/` Ã¢â‚¬â€ not yet implemented.
  - D3: `POST /api/billing/create-checkout/` contract (5.3/5.4): {type: 'subscription'|'pack', amount}; 400 price_mismatch; 409 active_subscription_exists (subscriptions only); response {checkout_url, checkout_id}. PackCards Buy and the Reactivate/Upgrade CTAs route through it. The 5.6 return flow + status card + toast are 5.6 Ã¢â‚¬â€ 5.5 redirects to checkout_url.
  - D4: D15 copy contracts (5.4 Sally, verbatim): "Never expires" EN 'Never expires' / FR "N'expire jamais" / AR 'Ã™â€žÃ˜Â§ Ã˜ÂªÃ™â€ Ã˜ÂªÃ™â€¡Ã™Å  Ã˜ÂµÃ™â€žÃ˜Â§Ã˜Â­Ã™Å Ã˜ÂªÃ™â€¡Ã˜Â§ Ã˜Â£Ã˜Â¨Ã˜Â¯Ã™â€¹Ã˜Â§' with a {colors.success} check icon; "Best value" EN 'Best value' / FR 'Meilleur rapport qualitÃƒÂ©/prix' / AR 'Ã˜Â£Ã™ÂÃ˜Â¶Ã™â€ž Ã™â€šÃ™Å Ã™â€¦Ã˜Â©' Ã¢â‚¬â€ {colors.primary} solid, rounded-full, top-inline-end; unit price '6.7 DZD/credit' / '6.0 DZD/credit' Ã¢â‚¬â€ decimal '.' fixed in ALL locales (AD-8), localized credit noun, formatted from raw price/credits via Intl.NumberFormat('en', {maximumFractionDigits: 1}) Ã¢â‚¬â€ never hand-typed; buy button EN 'Buy' / FR 'Acheter' / AR 'Ã˜Â´Ã˜Â±Ã˜Â§Ã˜Â¡' Ã¢â‚¬â€ {colors.primary} fill.
  - D5: PaymentTransaction = the history source (user, amount_dzd, type, status, credits_granted, created_at; ordering -created_at; payments_user_created_idx). No pagination pattern exists for billing; V1 volumes fine (Winston ruling pending).
  - D6: Subscription states active/failed_renewal/cancelled/expired; `subscriptions_cancel_state_check` (cancelled_at set iff status='cancelled'); `subscriptions_active_unique` (one active per user). Cancel writes status='cancelled' + cancelled_at=now under a row lock.
  - D7: The "no auto-renewal" AC vs `_grant_renewal`'s orphan-creation for a paid renewal on a cancelled row (5.3 Winston Q8 Ã¢â‚¬â€ a paid event is never silently skipped): a Chargily-side renewal charged AFTER cancellation would re-activate via the orphan path. Persona verdict needed (paid money Ã¢â€ â€™ grant + reactivate is arguably correct; "no auto-renewal" = our system never renews for free).
  - D8: Tier split-brain deferral (5.1 Ã¢â€ â€™ 5.7 cancel sync): user.tier stays 'starter' after cancel; the Plan Card "Cancelled Ã¢â‚¬â€ access until {date}" state must read the SUBSCRIPTION status, never user.tier.
  - D9: Reactivate: FR-24 allows a new subscription when no ACTIVE row exists (cancelled qualifies Ã¢â‚¬â€ the create-checkout 409 blocks only 'active'). `_grant_creation` re-activates the SAME cancelled row (status active, cancelled_at cleared Ã¢â‚¬â€ constraint-compliant) anchored at NOW with reset_pool=True (the 5.3 P1 last-payment-wins precedent). The AC phrase "resumes subscription from next billing date" vs the now-anchor is a John/Winston verdict (option A: keep the 5.3 machinery, interpret as the cadence resuming; option B: anchor cancelled-row reactivation at max(current_period_end, now) Ã¢â‚¬â€ a small _grant_creation amendment).
  - D10: The AC's free-user "Upgrade" CTA "opens the Upgrade Dialog" Ã¢â‚¬â€ the UpgradeDialog is a 5.7 deliverable (epic index). No dead stubs (John): options Ã¢â‚¬â€ (a) 5.5 CTA redirects to create-checkout directly (functional, 5.7 re-points it into the dialog); (b) CTA defers entirely to 5.7. John verdict.
  - D11: DESIGN.md L334 pack-card anatomy + L288: credit count in {typography.headline} (Western numerals), DZD price in {typography.title}, unit price in {typography.caption} {colors.muted-foreground}, never-expires note with a {colors.success} check icon, buy button {colors.primary}; hover borderÃ¢â€ â€™{colors.primary}; focus-visible standard ring. EXPERIENCE.md L131: four stacked sections.
  - D12: FE conventions: TanStack Query v5 (AD-20/21 Ã¢â‚¬â€ useQuery/useMutation with queryKeys factory, retry:false, refetchOnWindowFocus:false), typed service classes extending HttpClient, next-intl messages (billing.*), Base UI Dialog (the settings DangerZone precedent), AD-8 latn numerals everywhere, dir/rtl handled by the layout.
  - D13: Existing messages: `common.nav.billing` ("Billing") links to /billing in the Header already (Header.tsx:36) Ã¢â‚¬â€ the route must exist at `app/[locale]/billing/page.tsx`. Existing draft billing.* keys (title/current_plan/free_plan/starter_plan/renewal_date/packs.*/history.*/status.*/cancel.*/dzone.*) contain a corrupted character in starter_description (`Ã¯Â¿Â½?` Ã¢â‚¬â€ likely an em-dash encoding break) Ã¢â‚¬â€ Sally rules on replacing vs fixing; the 5.5 copy needs the D4/D15 contracts verbatim.
  - D14: `expire_failed_renewals` (daily beat) touches only active/failed_renewal rows Ã¢â‚¬â€ a cancelled row is never auto-expired or auto-renewed by our tasks; cancellation is one-way except the paid re-activation path (D9).
- **Persona consultation record (2026-08-10, parallel subagents Ã¢â‚¬â€ Winston architect + John PM + Sally UX; Sally INCLUDED Ã¢â‚¬â€ the whole story is user-facing surfaces)**:
  - Winston (architect): APPROVED with rulings Ã¢â‚¬â€ (Q1) `GET /billing/packs/` authenticated, contract `{packs: [{amount, credits, description, unit_price, never_expires, best_value}], never_expires}`; best_value server-computed as amount == max(PACK_PRICES) (no hardcoded literals); unit_price ships the exact server string '6.7'/'6.0' (FE never divides/re-formats). (Q2) `GET /billing/history/` Ã¢â‚¬â€ `{results: [...]}` wrapper (forward-compatible with pagination), cap 50 (LEDGER_PAGE_SIZE precedent), order -created_at,-id (P8 deterministic-ordering), raw codes (ledger precedent Ã¢â‚¬â€ FE localizes), refunded status renders (ops-manual). (Q3) cancel Ã¢â‚¬â€ ACTIVE-only cancellable (failed_renewal/expired Ã¢â€ â€™ 409; cancelling failed_renewal would strand the pool past period end and hide the 5.7 banner state); already-cancelled Ã¢â€ â€™ idempotent 200; lock order user Ã¢â€ â€™ sub with status re-check UNDER the sub lock (the grant D8 discipline; no ABBA cycle with grant/expiry); update_fields=['status','cancelled_at'] (constraint-compliant); NO tier write (5.7 cancel sync); NO Chargily call (the pinned envelope has no cancel surface; renewals are webhook-driven). (Q4) PlanView EXTENDS with additive `balances` {subscription_balance, pack_balance, display_balance} from user_balances() (one fetch for the Plan Card; the 5.3 D12 "pill endpoint" comment is stale Ã¢â‚¬â€ no such endpoint exists; additive key leaves the 5.7 consumers untouched; one aggregate is trivial at V1). (Q5) reactivation anchor Ã¢â‚¬â€ Option B scoped to the CANCELLED branch only: `anchor = max(latest.current_period_end, now)`, period `anchor.._add_month(anchor)`; failed_renewal/expired keep the 5.3 now-anchor (expired degenerates via max()); reset_pool=True identical; the P1 last-payment-wins ACTIVE-row logic untouched. (Q6) keep the orphan-renewal behavior Ã¢â‚¬â€ "no auto-renewal" holds by construction (renewals are webhook-triggered; no beat touches cancelled rows); a paid renewal on a cancelled row Ã¢â€ â€™ orphan ACTIVE row + grant + ERROR alarm (paid-event-never-skipped; settling would turn collected money into an unpaid outcome needing an ops refund); pin the ruling with a test. (Q7) NO schema change Ã¢â‚¬â€ zero migrations (makemigrations --check clean gate). (Q8) portability clean Ã¢â‚¬â€ ORM-only, filtered SUM aggregates SQLite-tested, no SERIALIZABLE (D8 deviation), no new dependencies (requests exists; FE = axios + TanStack v5). (Q9) FE data layer Ã¢â‚¬â€ one typed billing-service.ts + billingKeys factory + 3 useQuery + 1 useMutation; cancel invalidates billingKeys.plan only (no balance change on cancel Ã¢â‚¬â€ the CreditsPill is untouched); key factory now so 5.6's polling success invalidates the same keys. (Q10) Upgrade CTA Ã¢â‚¬â€ option (a): direct create-checkout redirect, 5.7 re-points the slot into the dialog (a dead stub is the anti-pattern).
  - John (PM): RATIFIED with EIGHT amendment clauses recorded verbatim in the AC section (page-structure conditional, free-user Upgrade CTA, Starter credits + the two AC-unspecified states failed_renewal-retry/expired-resubscribe, Payment History all-rows-all-statuses + failed-row surface, Danger Zone active-only + 409/200 semantics + conditional section, no-auto-renewal interpretation, Reactivate paid-path + anchor semantics + Plan Card placement). (V2) failed_renewal NOT cancellable; 409 'subscription_not_active' for failed_renewal/expired/no-row (Winston's split into 'subscription_not_found' for no-row adopted as the implementation detail); Danger Zone renders ONLY for active Ã¢â‚¬â€ free/non-active users omit the section entirely (no "nothing to cancel" dead surface). (V3) Reactivate = PAID create-checkout path; literal anchor semantics (max(current_period_end, now) + 1 month Ã¢â‚¬â€ the renewal anchor-preservation pattern); button on the Plan Card per the AC; failed_renewal re-pay stays now-anchored (deliberate asymmetry recorded Ã¢â‚¬â€ nobody fixes it silently). (V4) history = ALL rows newest-first no pagination at V1 (~15-25 rows/year per subscriber); ALL statuses render Ã¢â‚¬â€ pending/succeeded/failed/refunded; failed rows are the deferred-work failure surface (explanatory state + support line). (V5) no-auto-renewal = "the user's subscription will not renew without their payment" Ã¢â‚¬â€ verbatim interpretation; no handler change. (V6) deferred-work "Failed-charge user notification" RESOLVED for the visible surface by 5.5 (failed-status rendering in Payment History + support contact per FR-26 ops-manual refunds); failure EMAIL deferred V1.5; the 5.7 banner is subscription-state-driven and unaffected. (V7) the /refund-policy page has no epic-5 story owner Ã¢â‚¬â€ flagged as a backlog gap to close before launch (recorded, not in 5.5 scope).
  - Sally (UX): CONFIRMED the surfaces and MANDATED the copy contracts Ã¢â‚¬â€ full billing.* message-namespace rewrite (verbatim map in D24; deleted keys: free_plan/starter_plan/starter_description/free_description/upgrade_stub/packs.pack*_*/packs.buy/history.credits/cancel.*/dzone.delete_*; retained: title/current_plan/renewal_date (5.7 chip handoff)/failed_renewal (5.7 banner handoff)/status.* (5.6's Ã¢â‚¬â€ UNTOUCHED); packs.buy Ã¢â€ â€™ existing common.actions.buy reuse). PlanCard 5 states (free/active/failed_renewal/cancelled/expired) driven by subscription status never user.tier; state headline at {typography.title} (the section head owns headline); credits line label small muted + tabular-nums value (subscription pool for starter/cancelled; display balance for free/expired); cancelled-state role="status" wraps the headline text ONLY (credits line + CTA outside the live region). PackCards = D15 anatomy verified against DESIGN.md L334 (the EXPERIENCE.md L130 {colors.success} badge is STALE Ã¢â‚¬â€ AC + DESIGN pin {colors.primary}; flagged for a 5.6 doc fix); breakpoint grid-cols-1 md:grid-cols-2; buy busy state label swap + aria-busy + disabled; checkout failure Ã¢â€ â€™ inline role=alert box, no toast (5.6's slot). PaymentHistoryTable = ledger-anatomy (table-fixed, px-3, scope=col); status dot + label never color-only (succeeded #16a34a 3.3:1, failed #dc2626 4.5:1, pending #be4a36 5.9:1, refunded slate #64748b 4.75:1 Ã¢â‚¬â€ all pass 1.4.11); failed rows render the explanatory sub-row with the mailto support link (rich {support} via SUPPORT_EMAIL constant); empty/loading/error phases per the CreditsPage precedent. DangerZone = settings dialog pattern with THREE deviations: single confirm step (cancel is reversible Ã¢â‚¬â€ two-step is for irreversible flows), initial focus on "Keep my subscription" (EXPERIENCE L187 safe-control-first), description in {colors.danger-on-container} never muted-foreground (the settings component's latent AA failure flagged for a drive-by fix, out of scope). Dates: Intl.DateTimeFormat dateStyle medium + latn (DangerZone.tsx:69 precedent); history dates + timeStyle short + '-u-nu-latn' (CreditsPage formatTimestamp precedent); one shared formatBillingDate helper Ã¢â‚¬â€ no new formatting code (the 5.4 D17(e) mandate). Currency: latn digits all locales, "1,500 DZD" / "1,500 Ã˜Â¯.Ã˜Â¬" canonical shape (deliberate FR deviation from "1 500" Ã¢â‚¬â€ flagged for the pre-launch i18n pass). Unit price: server string verbatim, '.' fixed in all locales (FR never "6,7"). <bdi> required around {date} in AR card lines + dialog body, the credits value, and history date cells (not needed for amount+currency or unit price Ã¢â‚¬â€ logical-initial numerals). NO 5.6 status-card placeholder in 5.5 (no empty div, no reserved testid, no pre-added keys).
  - **D15-D24 rulings summary** (details above): D15 = packs contract shape + best_value rule (Winston Q1); D16 = history contract (Winston Q2/John V4 Ã¢â‚¬â€ cap 50, all statuses, {results} wrapper); D17 = cancel contract (Winston Q3/John V2 Ã¢â‚¬â€ active-only, idempotent 200 on cancelled, 409 'subscription_not_active'/'subscription_not_found', userÃ¢â€ â€™sub locks, no tier, no Chargily); D18 = PlanView additive balances (Winston Q4); D19 = reactivation anchor amendment (Winston Q5/John V3 Ã¢â‚¬â€ cancelled branch max(current_period_end, now)); D20 = orphan-renewal pin (Winston Q6 Ã¢â‚¬â€ no handler change, test pins the paid-event-never-skipped ruling); D21 = zero migrations (Winston Q7); D22 = FE data-layer contracts (Winston Q9 Ã¢â‚¬â€ billingKeys + useBilling + cancel invalidates billingKeys.plan); D23 = Upgrade/Reactivate/Retry/Resubscribe CTAs all route through create-checkout (John V1/V3 Ã¢â‚¬â€ no dead stubs); D24 = Sally's verbatim billing.* message map (the complete EN/FR/AR JSON in the consultation output; every Ã¢â€“Â² string pinned verbatim in tests, Ã¢Ëœâ€¦ computed via helper assertions).
- **AC amendments**: 8 clauses amended verbatim by John (2026-08-10) Ã¢â‚¬â€ all recorded in the AC section above in the 5.4 style.

## Dev Agent Record

### Agent Model Used

opencode-go/deepseek-v4-flash

### Debug Log References

- Task 1 RED: 3 failing (PacksView missing). GREEN: PacksView (module-level table imports; best_value = amount == max(PACK_PRICES)); ruff I001 import-sort fix. Gate: 910 pytest / ruff 0 / mypy 0 / check clean / makemigrations clean.
- Task 2 RED: 7 failing (HistoryView missing; 2 test bugs of mine Ã¢â‚¬â€ the contract exposes `id` not chargily_event_id (assertion fixed) and the User model has no first_name/last_name (fixture fixed); the UUID -id tiebreak is a STABLE order, not a meaningful one Ã¢â‚¬â€ test rewritten to assert determinism). GREEN: HistoryView (cap 50, -created_at,-id, raw codes, all statuses). Gates: 917 pytest / ruff 0 / mypy 0 / clean.
- Task 3 RED: 8 failing (CancelView missing). GREEN: CancelView (userÃ¢â€ â€™sub lock order, status re-check under the sub lock, idempotent 200 on cancelled, 409 subscription_not_active/not_found, no tier/ledger/Chargily). Gate: 927 pytest / ruff 0 / mypy 0 / clean.
- Task 4 RED: 4 failing (plan balances key missing + the anchor amendment; the 5.3 exact-shape pins needed the additive key Ã¢â‚¬â€ the documented contract extension). GREEN: PlanView balances via user_balances(); `_grant_creation` cancelled-branch anchor = max(current_period_end, now) (failed_renewal/expired keep the now-anchor); orphan-renewal pin passed on existing behavior by construction (Winston Q6). Gate: 934 pytest / ruff 0 / mypy 0 / clean.
- Task 5 RED: vitest collection error (billing-service missing). GREEN: billing-service.ts (+SUPPORT_EMAIL, numerals, formatBillingDate, createCheckout, SUBSCRIPTION_PRICE_DZD, 409 narrowers) + billingKeys + useBilling. Gate: 700 vitest / lint 0 / tsc 0 / i18n clean.
- Task 6 RED: 5 failing (t.rich not a function in the mock Ã¢â‚¬â€ the mock needed .rich attached; checkout mock resolved undefined Ã¢â€ â€™ default resolved value). GREEN: PlanCard (5 states, role=status headline-only, bdi dates via 0-arg RichTagsFunction Ã¢â‚¬â€ next-intl v3 types reject ReactNode values; the messages rewrite landed early with Sally's map + billing.upgrade_stub RESTORED (live 4.5 ExportModal consumer) + billing.currency added (D25)); a lint display-name fix on the date renderer. Gate: 712 vitest / lint 0 / tsc 0 / i18n clean.
- Task 7 RED: no real RED Ã¢â‚¬â€ the component + test landed together; GREEN on first run (9/9). Gate: 721 vitest / lint 0 / tsc 0 / i18n clean.
- Task 8 RED: 1 failing (duplicate 'Credit pack purchase' text Ã¢â‚¬â€ getAllByText fix). GREEN: PaymentHistoryTable (Fragment-keyed rows, dot+label statuses, failed sub-row with the mailto support link). Gate: 742 vitest / lint 0 / tsc 0 / i18n clean.
- Task 9 RED: 3 failing Ã¢â‚¬â€ Base UI schedules dialog focus via requestAnimationFrame (never runs synchronously in jsdom): tests flushed with an rAF round-trip; initialFocus via the data-billing-keep selector; the trigger hides while open (the confirm is the only matching button); the description color is danger-on-container (Sally Ã¢â‚¬â€ the settings component's muted-on-danger latent AA failure flagged for a drive-by fix). Gate: 742 vitest / lint 0 / tsc 0 / i18n clean.
- Task 10 RED: mostly green first pass; the trilingual pin test initially used Unicode curly apostrophes where the messages use ASCII Ã¢â‚¬â€ aligned to the message ground truth. GREEN: BillingPage + route + messages (63 verbatim pins Ãƒâ€” 3). Gate: 808 vitest / lint 0 / tsc 0 / i18n clean.
- Task 11 (Phase 5): docker daemon DOWN at start Ã¢â€ â€™ Docker Desktop started (the 5.3 lesson); container dzleads-pg16-e2e (host 5433 Ã¢â‚¬â€ the 4.6 lesson); clean migrate; E2E iterations: (a) the JWT cookie path requires email_verified_at (the logged_in_client fixture lesson Ã¢â‚¬â€ Unauthorized until set); (b) duplicate users from a crashed run Ã¢â€ â€™ drop-schema + clean migrate (the 5.4 precedent); (c) UnicodeEncodeError on 'Ã¢â€ â€™' in check names Ã¢â€ â€™ PYTHONIOENCODING=utf-8; (d) crashed runs leave subscription rows (FK SET_NULL) Ã¢â‚¬â€ the container removal is the definitive cleanup. FINAL: 27/27 (packs contract, history contract, cancel full matrix, plan balances, reactivation anchor on real PG). Final regression: 934 backend pytest / ruff 0 / mypy 0 / check clean / makemigrations clean; FE 808 vitest / lint 0 / tsc 0 / i18n clean.

### Completion Notes List

- Story 5.5 implemented end-to-end (TDD redÃ¢â€ â€™green per task): backend Ã¢â‚¬â€ `GET /api/billing/packs/` (the 5.4 RP7 HTTP-surface handoff; best_value server-computed), `GET /api/billing/history/` (cap 50, all statuses incl. the failed-row failure surface per John V4, raw codes), `POST /api/billing/cancel/` (active-only, idempotent on cancelled, 409 not_found/not_active, userÃ¢â€ â€™sub lock order, no tier/ledger/Chargily), PlanView additive `balances` (Winston Q4), the cancelled-row reactivation anchor amendment (max(current_period_end, now) Ã¢â‚¬â€ the AC's literal "resumes from next billing date", John V3/Winston Q5), the orphan-renewal pin (Winston Q6 Ã¢â‚¬â€ paid-event-never-skipped); zero migrations (Winston Q7). FE Ã¢â‚¬â€ the billing data layer (service + keys + 4 hooks), PlanCard (5 states, role=status cancelled announcement, 4 create-checkout CTAs), PackCards (D15 anatomy verbatim), PaymentHistoryTable (dot+label statuses, failed sub-row with mailto support), DangerZone (single-step dialog, safe-control initial focus, danger-on-container), BillingPage + /billing route + the billing.* message-namespace rewrite (Sally's verbatim map; upgrade_stub restored for the 4.5 consumer; currency key added) + 63 trilingual verbatim pins.
- Deferred-work items: "Failed-charge user notification" (5.4 review defer) Ã¢â‚¬â€ RESOLVED for the visible surface by 5.5 (failed rows render in Payment History with the explanatory copy + support contact; failure EMAIL deferred V1.5, recorded). New record: the settings DangerZone muted-on-danger contrast pair (flagged for a drive-by fix, out of scope); the EXPERIENCE.md L130 {colors.success} badge staleness (AC + DESIGN pin primary Ã¢â‚¬â€ flagged for a 5.6 doc fix).
- Persona decisions all implemented: Winston Q1-Q10 (incl. the zero-migration ruling and the plan-view additive contract), John's 8 AC amendments verbatim + V2/V3/V4/V6 rulings, Sally's full copy contracts + a11y/RTL mandates (bdi, latn, dots, focus, danger-on-container) + the delete/keep key rulings (with the upgrade_stub restoration deviation recorded).
- Gates: backend 934 pytest / ruff strict 0 / mypy strict 0 / check clean / makemigrations clean; FE 808 vitest / lint 0 / tsc 0 / check:i18n clean; real-PG16 E2E 27/27.

### File List

- backend/apps/billing/views.py (PacksView + HistoryView + CancelView + PlanView balances)
- backend/apps/billing/urls/billing.py (packs/history/cancel routes)
- backend/apps/billing/tasks.py (`_grant_creation` cancelled-branch anchor amendment)
- backend/apps/billing/tests/test_packs_view.py, test_history_view.py, test_cancel_view.py (new)
- backend/apps/billing/tests/test_plan_view.py (balances suite + additive pins), test_tasks.py (anchor tests + orphan pin)
- frontend/src/lib/api/billing-service.ts, frontend/src/lib/queryKeys/billing.ts, frontend/src/hooks/useBilling.ts, frontend/src/hooks/useCheckoutRedirect.ts (new)
- frontend/src/components/billing/{BillingPage,PlanCard,PackCards,PaymentHistoryTable,DangerZone}.tsx (new)
- frontend/src/app/[locale]/billing/page.tsx (new)
- frontend/messages/{en,fr,ar}.json (billing.* rewrite)
- frontend/src/__tests__/{billing-service.test.ts,plan-card.test.tsx,pack-cards.test.tsx,payment-history-table.test.tsx,billing-danger-zone.test.tsx,billing-page.test.tsx,billing-messages.test.ts} (new)

### Change Log

- 2026-08-10: Story created (in-progress) from the epic 5.5 planning spec; sprint-status 5-5 Ã¢â€ â€™ in-progress (UTF-16 LE preserved); epic-5 already in-progress. Baseline commit c9815a6e.
- 2026-08-10: Persona consultations (parallel subagents Ã¢â‚¬â€ Winston architect + John PM + Sally UX; Sally INCLUDED Ã¢â‚¬â€ the whole story is user-facing surfaces): Winston Q1-Q10 (packs HTTP surface + server best_value; history {results} cap-50 all-statuses; cancel active-only idempotent-200 userÃ¢â€ â€™sub locks no-tier no-Chargily; plan-view additive balances; cancelled-branch reactivation anchor max(current_period_end, now); orphan-renewal ruling pinned Ã¢â‚¬â€ no handler change; zero migrations; no new deps; FE data-layer conventions; Upgrade CTA = direct create-checkout). John's 8 AC amendments recorded verbatim in the AC section (four-section conditional, Upgrade CTA, Starter credits + failed_renewal/expired states, Payment History all-rows-all-statuses + failure surface, Danger Zone active-only + 409/200 semantics, no-auto-renewal interpretation, Reactivate paid path + anchor semantics). Sally's copy contracts verbatim (the billing.* namespace map, D15 cards, dzone dialog, history labels + failed-row note, status.* untouched for 5.6) + a11y mandates (bdi, latn numerals, dot+label, safe-control focus, danger-on-container) + the key delete/keep rulings. Dev Notes D15-D25 + consultation record added.
- 2026-08-10: Implemented (TDD) Ã¢â‚¬â€ Task 1 packs endpoint (3 tests), Task 2 history endpoint (7 tests), Task 3 cancel endpoint (10 tests), Task 4 plan balances + reactivation anchor + orphan pin (7 new tests; 2 additive-key pin updates), Task 5 FE data layer (14 tests), Task 6 PlanCard + i18n rewrite (10 tests + 63 trilingual pins; upgrade_stub restored Ã¢â‚¬â€ 4.5 consumer; currency key added), Task 7 PackCards (9 tests), Task 8 PaymentHistoryTable (10 tests), Task 9 DangerZone (10 tests; rAF-flush lesson recorded), Task 10 BillingPage + route (3 tests). Gates green after every task (backend 910 Ã¢â€ â€™ 934; FE 700 Ã¢â€ â€™ 808; lint/tsc/i18n clean throughout). Phase 5 real-stack: PG16 (host 5433) clean migrate, E2E 27/27 (packs Ã¢â€ â€™ history Ã¢â€ â€™ cancel matrix Ã¢â€ â€™ plan balances Ã¢â€ â€™ reactivation anchor), users cleaned, container removed. Status Ã¢â€ â€™ review (sprint-status 5-5 Ã¢â€ â€™ review; epic-5 stays in-progress).
