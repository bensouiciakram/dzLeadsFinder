---
story_id: 4.4
epic: 4
title: Story 4.4 — Export API Backend
Status: done
frs: [FR-17, FR-18, FR-20]
ads: [AD-3, AD-4, AD-7, AD-11]
ux_drs: [UX-DR22, UX-DR24]
baseline_commit: 4d12b09
---

# Story 4.4: Export API Backend

Status: done

## Story

As a **developer**,
I want **Django REST Framework endpoints for CSV and Excel export generation with rate limiting and credit cost calculation**,
So that **the frontend can trigger exports with the correct gating logic**.

## Acceptance Criteria

**Given** the export API
**When** an authenticated user creates an export via `POST /api/export/`
**Then** the request includes: record IDs or current query, format ('csv'/'xlsx'), `include_unrevealed` (boolean)
**And** the server calculates the credit cost: 1 credit per row, regardless of revealed status
**And** checks the user has sufficient balance
**And** checks the daily export limit (5,000 rows/24h)
**And** if checks pass, deducts credits atomically and creates an export job

**Given** the export download
**When** the export is ready, the user downloads via `GET /api/export/{id}/download/`
**Then** the response is a file download with the correct MIME type:
- CSV: `text/csv`
- XLSX: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`

**Given** CSV export for paid users
**When** a Starter user exports CSV
**Then** the CSV includes header rows localized to the user's active locale
**And** the export includes all rows (up to the 5,000 cap)
**And** no watermark is included

**Given** Excel export for paid users
**When** a Starter user exports .xlsx
**Then** the .xlsx opens cleanly in Microsoft Excel and LibreOffice Calc
**And** phone numbers are preserved as text (not auto-converted to numbers)
**And** column types are explicit (text, number, date) to avoid auto-coercion bugs

**Given** export rate limiting
**When** a user has exported 5,000 rows in the past 24h
**Then** further export requests return HTTP 429
**And** the reset is at 00:00 Africa/Algiers

**Given** credit cost for export
**When** the export is confirmed
**Then** the user is warned of the credit cost before finalising
**And** the cost is: N rows × 1 credit each (revealed and unrevealed alike)
**And** the cost is displayed as: "n revealed + m unrevealed = total credits"
**And** the `exports` table records: id, user_id, format, row_count, credits_cost, included_unrevealed, watermark, created_at

## Tasks / Subtasks

- [x] **Task 1: Models — `Export` in `apps/exports` + 0001 migration** (AC: exports table records; spine DDL `docs/ARCHITECTURE-SPINE.md#L225-235`; the 4.1 test_models.py schema-test precedent — Dev Notes D1)
  - [x] 1.1 RED: `backend/apps/exports/tests/test_models.py` — NEW — module-level `pytestmark = pytest.mark.django_db`; schema tests via `_meta.get_field` + `connection.introspection` (the 4.1 `test_models.py` TestSchema pattern):
    - `exports` table columns: id, user_id, format, row_count, credits_cost, included_unrevealed, watermark, created_at (the AC literal + spine) **AND** the 4.4 extensions `rows_json` (JSONField — the frozen row snapshot, Dev Notes D3) + `locale` (CharField — the header-language frozen at POST, FR-3).
    - `format` DB CHECK allows exactly 'csv'/'xlsx' (TextChoices `ExportFormat` + CheckConstraint — the 4.1 D3 pattern; SQLite-compatible, NO PG-only CREATE TYPE).
    - `row_count >= 0` + `credits_cost >= 0` CheckConstraints.
    - **`Export.user` FK `on_delete=CASCADE`** (Dev Notes D2 — mirrors `Reveal`; `maintenance_tasks.py:14` already guards `('exports', 'Export')` in DEPENDENT_MODELS).
    - `watermark` BooleanField default False (the spine column; 4.6 flips it).
    - composite index `(user_id, created_at)` named `exports_user_created_idx` (the 4.1 review-patch index precedent).
    - ordering `['-created_at']` (the ledger precedent).
  - [x] 1.2 GREEN: `backend/apps/exports/models.py` — NEW — `ExportFormat(models.TextChoices)` ('csv'/'xlsx'); `Export(models.Model)` (`db_table='exports'`): `user = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.CASCADE, related_name='exports')`, `format` (TextChoices), `row_count = models.IntegerField()`, `credits_cost = models.IntegerField()`, `included_unrevealed = models.BooleanField(default=True)`, `watermark = models.BooleanField(default=False)`, `rows_json = models.JSONField(default=dict)`, `locale = models.CharField(max_length=2)`, `created_at = models.DateTimeField(default=timezone.now)`; Meta: ordering `['-created_at']`, CheckConstraints (format, row_count ≥ 0, credits_cost ≥ 0), index `exports_user_created_idx`. Run `makemigrations exports` → `0001_initial.py` (verify no model drift; SQLite-runnable).
  - [x] 1.3 Run backend gates (pytest/ruff/mypy strict) — green (548 total).

- [x] **Task 2: Backend — `debit_export_rows` in `credits/services.py`** (AC: deducts credits atomically — N rows; AD-3/AD-4; the 4.1 SERIALIZABLE + user-row lock precedent — Dev Notes D4)
  - [x] 2.1 RED: `backend/apps/credits/tests/test_export_debit.py` — NEW — unit suite (the 4.1 `test_reveal.py` pattern; fixtures: `create_user` + local `grant(user, amount, pool)` ledger helper):
    - **N-row debit**: user with ledger total 10 → `debit_export_rows(user, 4, 'ref-1')` → exactly one new ledger row `event_type='export_row_debit'`, `amount=-4`, `pool='subscription'`, `balance_after=6`, `reference_id='ref-1'`; `user.credits_balance == 6` (cache decremented in the same transaction — AD-4).
    - **drawdown subscription-first for the WHOLE N (AD-7)**: subscription=3 + pack=5, N=2 → pool='subscription'; subscription=0 + pack=8, N=6 → pool='pack'; subscription=2 + pack=8, N=6 → pool='pack' (the whole N draws from ONE pool — no mixed-drawdown split, Dev Notes D4).
    - **insufficient balance**: ledger total 5, N=6 → raises `InsufficientCreditsError`; NO ledger row; cache unchanged.
    - **balance-from-ledger, never the cache (AD-4)**: `credits_balance=5` cache but EMPTY ledger, N=1 → FAILS (`InsufficientCreditsError`).
    - **composed-transaction contract**: `with transaction.atomic(): balance_after = debit_export_rows(...)` succeeds and the writes COMMIT on exit; an exception raised AFTER the debit inside the same atomic block rolls back EVERYTHING (no ledger row, no cache change — the atomicity AC).
    - **SERIALIZABLE guard + user lock**: the function runs `_serializable_guard()` as its FIRST statement then `select_for_update().get(pk=user.id)` (the 4.1 pattern — assert the call sequence via the SQLite no-op behavior + the lock semantics verified on PG16 in Task 6; the guard must never execute after a query — Dev Notes D4).
    - **balance_after returned**: the function returns the post-debit total (`6` in the first case) — the caller records it in the export response.
  - [x] 2.2 GREEN: `backend/apps/credits/services.py` — UPDATE — `def debit_export_rows(user, row_count: int, reference_id: str) -> int`:
    - **Opens NO transaction of its own** (Dev Notes D4 — documented contract: the caller owns the atomic block; this function MUST be its first statement so the SERIALIZABLE guard pins). Inside the caller's transaction: `_serializable_guard()` (first), `get_user_model().objects.select_for_update().get(pk=user.id)`, `_pool_balances(user)`, `total = sub + pack`; `total < row_count` → raise `InsufficientCreditsError`; `pool = 'subscription' if subscription_balance >= row_count else 'pack'`; `CreditLedger.objects.create(user=user, event_type=CreditEventType.EXPORT_ROW_DEBIT, amount=-row_count, balance_after=total - row_count, pool=pool, reference_id=reference_id)`; `get_user_model().objects.filter(pk=user.id).update(credits_balance=F('credits_balance') - row_count)`; `user.credits_balance -= row_count`; return `total - row_count`.
  - [x] 2.3 Run backend gates — green (557 total).

- [x] **Task 3: Backend — file generation service (CSV + xlsx)** (ACs: localized headers, wilaya format, phones-as-text, explicit column types, MIME; FR-2/FR-3/FR-17; stdlib only — Dev Notes D5/D6)
  - [x] 3.1 RED: `backend/apps/exports/tests/test_generation.py` — NEW — pure-function tests (no DB, no client):
    - **CSV BOM + CRLF**: output starts with `\uFEFF`; line endings CRLF (`\r\n`) — the 4.3 client-CSV rules applied server-side.
    - **RFC-4180 escaping**: fields containing `,`, `"`, or newline are quoted + doubled quotes.
    - **localized headers ×3**: `build_export_csv` receives the locale label set (headers injected — the labels-injected builder precedent, 4.3 D5) — EN/FR/AR header sets pinned (people + company).
    - **people columns stable**: name, role, company, wilaya, email, phone, address — exact order (FR-2 underlying-column-order rule).
    - **company columns stable**: name, industry, wilaya, size_band, website, people_count.
    - **wilaya cell**: `"{localized name} ({code})"` — "Oran (31)" (EXPERIENCE.md:281); empty string when wilaya null.
    - **unrevealed rows carry FULL contact data** (Dev Notes D6 — the export IS the bulk reveal; UJ-1 flow 281): a people row with no ≤30d reveal serializes email/phone/address values when included.
    - **contact data never translated**: data values pass through verbatim (FR-3 — only headers localize).
    - **xlsx package structure**: `build_export_xlsx(...)` output unzips (zipfile) to the standard minimal package: `[Content_Types].xml`, `_rels/.rels`, `xl/workbook.xml`, `xl/_rels/workbook.xml.rels`, `xl/worksheets/sheet1.xml`, `xl/styles.xml`; every XML part parses via `xml.etree.ElementTree.fromstring` (well-formedness).
    - **xlsx cell types**: phone cells are inline strings (`t="s"` or `<is><t>`) — NEVER numeric — the phones-as-text AC (FR-18); `people_count` cells are numeric (`t="n"`); header row = the localized labels; a phone like "0550 12 34 56" round-trips unchanged through the parsed sheet XML.
  - [x] 3.2 GREEN:
    - `backend/apps/exports/export_service.py` — NEW — `build_export_csv(rows: list[dict], headers: dict[str, str]) -> bytes` (stdlib `csv` + `io.StringIO`, BOM + CRLF + RFC-4180; the row dicts carry the file-column values pre-localized — the service is locale-agnostic, Dev Notes D5) and `build_export_xlsx(rows, headers) -> bytes` (stdlib `zipfile` + hand-rolled XML: `[Content_Types].xml` (inline strings default + png ignore? — minimal: worksheet + styles + core parts), `xl/workbook.xml` (one sheet "export"), `xl/_rels/workbook.xml.rels`, `xl/worksheets/sheet1.xml` with `<row>`/`<c r="A1" t="inlineStr"><is><t>…</t></is></c>` for text cells and `<c t="n"><v>…</v></c>` for numeric cells — XML-escaping via `xml.sax.saxutils.escape`; Dev Notes D6 — the minimal spec-compliant writer, zero dependencies).
    - `backend/apps/exports/messages.py` — NEW — trilingual dicts (the quota.py/credits messages pattern): `EXPORT_CSV_HEADERS` / `EXPORT_XLSX_HEADERS`? — NO: header LABELS are `{'people': [...], 'company': [...]}` per-locale dicts `{ar, fr, en}` (the 4.3 CSV labels pattern); `EXPORT_LIMIT_MESSAGES` (EXPERIENCE.md:98 copy verbatim: ar "بلغت حد التصدير (5,000 صف خلال 24 ساعة) — عُد غدًا" / fr "Limite d'export atteinte (5 000 lignes par 24 h) — revenez demain" / en "Export limit reached (5,000 rows per 24 h) — come back tomorrow" — `[PENDING REVIEW]` per the translation-review assumption); `STARTER_ONLY_MESSAGES` (the upgrade copy — `[PENDING REVIEW]`); `EXPORT_RECORD_NOT_FOUND_MESSAGES` (download 404 copy — the credits RECORD_NOT_FOUND pattern); `WATERMARK_MESSAGES` (the FR-19 copy "DZLeads Free — upgrade to remove" + fr/ar — strings land NOW per John Q4, ROW GENERATION lands in 4.6; `[PENDING REVIEW]`).
  - [x] 3.3 Run backend gates — green (579 total).

- [x] **Task 4: Backend — `create_export` orchestration + `POST /api/export/` + `GET /api/export/{id}/download/`** (ACs: cost calc, balance check, rate limit, atomic deduct + export job, download MIME; FR-17/18/20; the 4.2 RevealView adapter precedent — Dev Notes D4/D7/D8)
  - [x] 4.1 RED: `backend/apps/exports/tests/test_export_api.py` — NEW — module-level `pytestmark = pytest.mark.django_db`; fixtures: `create_user`/`api_client` conftest pair + local `export_session` factory (verified Starter user + login — the `test_reveal_api.py` `reveal_session` pattern), seeded Person + Company rows, a local `grant` ledger helper, a local `reveal(user, person)` helper writing a Reveal row, and a local `seed_usage(user, n)` helper writing `daily_usage.export_rows`:
    - **anonymous** → 401 (default IsAuthenticated + CookieJWTAuthentication).
    - **frozen user** → 401 `code == 'account_deleted'` (the auth-layer policy — 4.2 D6; NO view-level checks anywhere).
    - **free tier** → 403 `{'detail': <localized>, 'code': 'starter_only'}` for BOTH formats (Dev Notes D8 — 4.4 is paid-only; the 403-with-code precedent settings.py:110).
    - **strict payload**: missing `record_ids` / missing `format` / missing `include_unrevealed` / extra key → 400 `{'code': 'invalid_payload'}` (the ChecklistView.put strict-keys precedent — `data.keys() != {...}`); `record_ids` not a list / empty list / a list containing a non-string / a string longer than 200 chars / a list longer than 5,000 entries → 400 `invalid_payload`; `format` not in ('csv','xlsx') → 400; `include_unrevealed` not a boolean (`1`, `"true"`) → 400 (the strict-identity precedent — `data['include_unrevealed'] is not True/False`).
    - **mixed record types** (a people id + a company id) → 400 `invalid_payload` (one record type per export).
    - **unresolved id** → 404 `{'code': 'record_not_found'}`; NOTHING written (no ledger row, no export row, `daily_usage.export_rows` unchanged — the atomic-rollback AC).
    - **happy path CSV**: Starter user, 3 seeded people ids (2 with ≤30d Reveal rows, 1 without), `{'record_ids': [...], 'format': 'csv', 'include_unrevealed': True}` → 200; body has EXACTLY `{'id', 'format', 'row_count', 'revealed_count', 'unrevealed_count', 'credits_cost', 'included_unrevealed', 'watermark', 'created_at', 'balances'}` (exact-keys — the 4.3 ledger precedent); `row_count=3`, `revealed_count=2`, `unrevealed_count=1`, `credits_cost=3` (the "n revealed + m unrevealed = total credits" contract — `revealed + unrevealed == row_count == credits_cost`); `watermark=False`; `balances == {'subscription_balance': 12, 'pack_balance': 0, 'display_balance': 12}` for a 15-credit user (confirmed post-commit ledger read — the 4.2 confirmed-balance contract); exactly one new ledger row `export_row_debit amount=-3 pool='subscription' balance_after=12` with `reference_id == str(export.id)`; one `Export` row with `rows_json` containing 3 entries incl. contact data for the unrevealed row; `daily_usage.export_rows == 3`; `credits_balance == 12`.
    - **include_unrevealed=false**: 3 ids (2 revealed) → 200 `row_count=2`, `unrevealed_count=0`, `credits_cost=2`, the export row's `rows_json` has exactly 2 entries, `daily_usage.export_rows == 2` (the AC "each row included in the export costs 1 credit" — cost = the INCLUDED set).
    - **insufficient credits** → 402 `{'code': 'insufficient_credits'}` + localized detail per user.locale (ar/fr/en fixtures); NO ledger row, NO export row, NO usage increment (the atomic-rollback AC).
    - **rate limit at cap**: `seed_usage(user, 4998)` + export of 3 rows → 429 `{'detail': <localized come-back-tomorrow>, 'code': 'export_limit_exceeded', 'limit': 5000}`; NO debit, NO export row, usage STAYS 4998 (the rollback AC — a 429 never burns credits); export of 2 rows → 200, usage == 5000 (headroom semantics — Dev Notes D7); `seed_usage(user, 5000)` + export of 1 → 429.
    - **company export happy path**: 2 company ids → 200; download body has the company column set + the industry cell carrying the CANONICAL `name_en` (data values never translated — D5/FR-3; only headers + wilaya localize; the Auditor-flagged "localized industry name" contradiction resolved toward D5) + `people_count` numeric cell in xlsx.
    - **download CSV**: POST csv → `GET /api/export/{id}/download/` → 200; `Content-Type` starts `text/csv`; `Content-Disposition` contains `attachment` and `filename="dzleads-export-{id}.csv"`; body starts with `\uFEFF`; header row matches the user's locale (fr user → French labels); the unrevealed row's contact data present (Dev Notes D6).
    - **download xlsx**: POST xlsx → 200; `Content-Type == 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'`; `Content-Disposition` filename ends `.xlsx`.
    - **download foreign export** (another user's export id) → 404 `{'code': 'record_not_found'}` (ownership filter — no 403 existence leak, Dev Notes D9).
    - **download unknown id** → 404 `record_not_found`; **download garbage id** (unparseable UUID string) → 404 `record_not_found` (the 4.1 canonicalization guard — no 500).
    - **429 localized detail**: an ar-locale user at the cap gets the AR come-back-tomorrow string (the quota.py locale pattern).
  - [x] 4.2 GREEN:
    - `backend/apps/exports/quota.py` — NEW — `EXPORT_DAILY_ROW_LIMIT: int = 5000` (module constant — the `SEARCH_DAILY_LIMITS` precedent; app_config ops-knob deferred — Dev Notes D7).
    - `backend/apps/exports/services.py` — NEW — `def create_export(user, payload: dict) -> Export` (or in export_service.py — ONE orchestration module, Dev Notes D4):
      1. Validate payload strictly (keys/format/boolean/list rules → raise `InvalidExportPayloadError` — or return a typed error; the strict-parse precedent).
      2. Resolve the record set BEFORE any transaction: probe `Person.objects.filter(pk__in=ids)` (guarded ValueError/TypeError/ValidationError → 404) — if all ids resolve → record_type 'people'; else probe Company — if all resolve → 'company'; else → `ExportRecordNotFoundError` (no partial debit — Dev Notes D8).
      3. Compute the revealed set: `Reveal.objects.filter(user_id, record_type, record_id__in=ids, created_at__gte=now - 30d).values_list('record_id', flat=True)` (the `_revealed_ids` pattern).
      4. Included set = revealed ids when `include_unrevealed=False`, else ALL ids; `row_count = len(included)`; `row_count == 0` → `InvalidExportPayloadError` ("no rows to export" — nothing written, Dev Notes D8).
      5. Assemble the snapshot rows (full file-column data incl. contact fields for every included row — the bulk-reveal contract D6) + freeze `locale = user.locale` (FR-3) + build the file BYTES with `build_export_csv/xlsx` (PURE READ — any failure here returns with ZERO debit).
      6. ONE atomic block (`transaction.atomic()`): `debit_export_rows(user, row_count, reference_id=...)` as the FIRST statement (the SERIALIZABLE guard + user lock live there — Dev Notes D4; pre-generate the export id `uuid.uuid4()` so the reference_id exists first, the reveal precedent) → quota: `usage, _ = DailyUsage.objects.get_or_create(user=user, date=timezone.localdate())`; `usage.export_rows + row_count > EXPORT_DAILY_ROW_LIMIT` → raise `ExportLimitExceededError` (the block rolls back the debit — D7) → `DailyUsage.objects.filter(pk=usage.pk).update(export_rows=F('export_rows') + row_count)` → `Export.objects.create(id=export_id, user=user, format=..., row_count=..., credits_cost=row_count, included_unrevealed=..., watermark=False, rows_json={'record_type': ..., 'rows': [...]}, locale=...)`.
      7. Return the Export row.
    - `backend/apps/exports/views.py` — NEW — `class ExportView(APIView)` with `post`: the thin adapter (RevealView precedent): tier gate `request.user.tier != 'starter'` → 403 `starter_only` (Dev Notes D8); call `create_export(request.user, request.data)` DIRECTLY — never inside an outer atomic (D4); typed-exception mapping: `InvalidExportPayloadError` → 400 `invalid_payload`, `ExportRecordNotFoundError` → 404 `record_not_found`, `InsufficientCreditsError` → 402 `insufficient_credits` (reuse `credits.messages.INSUFFICIENT_CREDITS_MESSAGES`), `ExportLimitExceededError` → 429 `{'detail': EXPORT_LIMIT_MESSAGES[_locale(user)], 'code': 'export_limit_exceeded', 'limit': EXPORT_DAILY_ROW_LIMIT}`; success: `balances = user_balances(request.user)` (post-commit read) → `Response({'id': str(e.id), 'format': e.format, 'row_count': e.row_count, 'revealed_count': n_revealed, 'unrevealed_count': e.row_count - n_revealed, 'credits_cost': e.credits_cost, 'included_unrevealed': e.included_unrevealed, 'watermark': e.watermark, 'created_at': e.created_at.isoformat(), 'balances': balances})` (the exact-keys contract the 4.5 modal consumes — Dev Notes D10).
    - `backend/apps/exports/views.py` — `class ExportDownloadView(APIView)` with `get(self, request, export_id)`: ownership-filtered lookup `Export.objects.filter(user_id=request.user.id, pk=export_id).first()` with the unparseable-UUID guard (ValueError/TypeError/ValidationError → None); `None` → 404 `{'detail': <localized>, 'code': 'record_not_found'}`; else `bytes = build_export_csv/xlsx(e.rows_json['rows'], header_labels[e.locale])` (deterministic regeneration from the frozen snapshot — D3) → `FileResponse(BytesIO(bytes), content_type=..., as_attachment=True, filename=f'dzleads-export-{e.id}.{e.format}')` (CSV: `text/csv; charset=utf-8`; xlsx: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`).
    - `backend/apps/exports/urls.py` — NEW — `app_name = 'exports'`; `path('', ExportView.as_view(), name='export-create')` + `path('<str:export_id>/download/', ExportDownloadView.as_view(), name='export-download')` (the two-path explicit pattern — no free-form slugs).
    - `backend/config/urls.py` — UPDATE — `path('api/export/', include(('apps.exports.urls', 'exports'), namespace='exports'))` (the explicit-namespace form — the 4.3 urls.W005 lesson).
    - `backend/apps/exports/admin.py` — NEW — read-only admin (the 4.1 credits admin precedent: `list_display` = id/format/row_count/credits_cost/user/watermark/created_at, `readonly_fields` = all fields, `has_add/change/delete_permission` → False, list_filter format/watermark, search user__email).
  - [x] 4.3 Run backend gates — green (611 total).

- [x] **Task 5: Checklist `step_export` EXISTS extension** (the 3.7 deferred-work event contract (b) — views.py:315-318; client contract unchanged — Dev Notes D11)
  - [x] 5.1 RED: `backend/apps/search/tests/test_checklist.py` — UPDATE — `TestGet`: after an `Export` row exists for the user, `GET /api/search/checklist/` → `step_export: true`; fresh user → `false`; cumulative first-EVER semantics (an old export still → true — the John PM2 step_reveal pattern); the other steps + dismissal unchanged.
  - [x] 5.2 GREEN: `backend/apps/search/views.py` — UPDATE — `ChecklistView._state`: replace the literal `'step_export': False` + comment with `Export.objects.filter(user_id=request.user.id).exists()` — in-method import (the DailyUsage precedent, load-order agnostic; NO circular import — exports.models imports only django + AUTH_USER_MODEL; search already imports credits.models.Reveal at module level).
  - [x] 5.3 Run backend gates — green (618 total).

- [x] **Task 6: Full regression + real-stack verification** (the 4.2 Task 7 precedent; NFR-1)
  - [x] 6.1 Full backend gates: `pytest` (all apps) + `ruff` + `mypy` strict — ALL green (618 tests / ruff 0 / mypy strict 0; `manage.py check` clean). (Frontend gates: NOT run — 4.4 is backend-only; no FE files touched.)
  - [x] 6.2 Real-stack E2E (docker PG16 — the 4.2 stack): `makemigrations --check` clean; `migrate` applies `exports` 0001 on the live DB; psql assertions: `exports` table + CHECKs + `exports_user_created_idx` + the 0001-in-django_migrations row; curl/django-test flows: login → free user POST (403 starter_only) → starter user POST csv 3 rows (200, exact body, ledger `export_row_debit` −3 with `reference_id`, `daily_usage.export_rows=3`, export row + rows_json) → download (200, MIME, BOM, localized headers, contact data) → xlsx POST + download (200, xlsx MIME) → seed usage 4998 → POST 3 (429, rollback verified: ledger unchanged, usage 4998) → seed 5000 → POST 1 (429) → 0-credit user POST (402, nothing written) → foreign download (404) → garbage-id download (404) → anonymous (401) → frozen (401 account_deleted) → checklist GET shows `step_export: true` after the export; E2E rows cleaned.
  - [x] 6.3 NFR-1 headroom note: the POST is ONE round trip — ≤3 pre-transaction reads (people-probe + company-probe `select_related` joins, worst 2 row-SELECTs + 1 revealed-flag EXISTS) + ONE transaction (guard, user FOR UPDATE, `daily_usage` PK read, 1 ledger aggregate, 4 writes incl. the ~1-2MB TOAST JSONB snapshot); the download is ONE query + deterministic in-memory regeneration; no N+1, no pagination enumeration — recorded in Dev Notes.

## Dev Notes

- **Source of truth — the planning spec**: `_bmad-output/planning-artifacts/epics/epic-04-reveal-credit-export/story-04-export-api-backend.md` (all ACs verbatim). FR-17/18/19/20 in `_bmad-output/planning-artifacts/prds/prd-algerian-b2b-lead-platform-2026-07-18/4-features.md#L161-190`. UX: `DESIGN.md#L333` (export-modal spec — 4.5 anatomy, the POST response contract feeds it), `EXPERIENCE.md#L126` (export modal row: full current result set, cost breakdown, balance-after), `EXPERIENCE.md#L237-248` (Export Gating table), `EXPERIENCE.md#L98` (come-back-tomorrow trilingual copy), `EXPERIENCE.md#L281` (UJ-1 climax: 0 revealed + 5 unrevealed = 5 credits; "the data is real"; "Oran (31)"), mockup `ux-designs/.../mockups/export-modal.html`. Spine: exports DDL `docs/ARCHITECTURE-SPINE.md#L225-235`, daily_usage DDL `#L237-244` (export_rows column exists), export rate limit `#L723`, AD-3/AD-4/AD-7/AD-11 `#L863-882`.
- **CRITICAL CONTRACTS (recorded for the review — MUST be honored verbatim):**
  - **D1 — the exports table schema**: spine columns (id, user_id, format, row_count, credits_cost, included_unrevealed, watermark, created_at) + the 4.4 extensions `rows_json` (JSONField — the frozen row snapshot) + `locale` (header language frozen at POST, FR-3). DB CHECKs: format IN ('csv','xlsx'), row_count ≥ 0, credits_cost ≥ 0 (SQLite-compatible CheckConstraint — the 4.1 D3 pattern; NO PG-only CREATE TYPE). `watermark` Boolean default False — the column exists NOW, 4.6 flips it.
  - **D2 — `Export.user` FK `on_delete=CASCADE`** (mirrors `Reveal`; `backend/tasks/maintenance_tasks.py:12-16` already guards `('exports', 'Export')` in DEPENDENT_MODELS — the model label `apps.exports.Export` is a pre-existing contract, never rename, the 4.1 D13 lesson). SET_NULL would orphan PII snapshots — rejected.
  - **D3 — file storage: JSONB snapshot + deterministic regeneration-on-download** (Winston Q5): the exports table stores `rows_json` (per-row file-column values incl. contact data + the `revealed` flag), NEVER file bytes (no blob lifecycle, no disk state on the ephemeral VPS container, no live-DB drift — the user paid for a specific file; snapshot replays it byte-for-byte). The download regenerates bytes from the snapshot with the frozen `locale` headers. ~1-2MB TOAST rows — fine on PG16.
  - **D4 — the atomic debit composition contract (THE 4.1 deferred-work contract applied to exports)**: `debit_export_rows(user, row_count, reference_id)` in `credits/services.py` (credits app owns ALL ledger writes — AD-4) opens NO transaction of its own; `create_export` owns ONE atomic block and calls it as its FIRST statement — the SERIALIZABLE guard + user-row `select_for_update` live inside the debit function exactly as in `reveal_contact` (the whole-N drawdown from ONE pool — 'subscription' if sub ≥ N else 'pack', AD-7). NO query may precede the guard inside the transaction (PG ProgrammingError otherwise). File generation + snapshot assembly run BEFORE the transaction — any failure returns with ZERO debit. The user-row lock serializes the quota check + debit + increment (the 3.2 TOCTOU deferred-work precedent is closed by the lock; SERIALIZABLE is defense-in-depth). A 429/402 rolls back the debit — a rejected export NEVER burns credits.
  - **D5 — CSV generation**: stdlib `csv` only (NO new dependencies — the skill halts on unapproved deps; openpyxl NOT approved); UTF-8 BOM + CRLF (Excel + Arabic — the 4.3 client-CSV rules applied server-side); RFC-4180 escaping; header row localized to the SNAPSHOT-FROZEN locale (FR-3 — headers localize at export time, data values never translated); stable column order per FR-2: people `name, role, company, wilaya, email, phone, address`, company `name, industry, wilaya, size_band, website, people_count`; wilaya cell `"{localized name} ({code})"` — "Oran (31)" (EXPERIENCE.md:281; empty when null); headers injected via per-locale label dicts (the 4.3 labels-injected builder precedent — locale-agnostic, testable).
  - **D6 — unrevealed rows carry FULL contact data**: an included unrevealed row serializes its email/phone/address (the export IS the bulk reveal — UJ-1's "0 revealed + 5 unrevealed = 5 credits" file "the data is real"; charging 1 credit/row for data already visible in the results would be indefensible). `include_unrevealed=false` → unrevealed rows are EXCLUDED entirely (row_count/cost = the included set only; `unrevealed_count` = 0). Resolved persona divergence (Winston/John full-data vs Sally's empty-cells reading) — recorded in the Change Log; Sally's "never surprises" concern is covered by the 4.5 modal's literal mini-preview showing real rows.
  - **D7 — the 5,000 rows/24h cap (FR-20, Q4 assumption)**: module constant `EXPORT_DAILY_ROW_LIMIT = 5000` in `apps/exports/quota.py` (the `SEARCH_DAILY_LIMITS` precedent); cumulative per account per Algeria calendar day — `daily_usage.export_rows` (the column EXISTS in the model since 3.1) with `date = timezone.localdate()` → reset at 00:00 Africa/Algiers is natural (`TIME_ZONE='Africa/Algiers'` base.py:75, AD-11) — no reset job. A request that would EXCEED the remaining headroom → 429 whole (NEVER truncated silently — the 4.5 modal previews the full set). Headroom check + increment INSIDE the same transaction as the debit (locked — no TOCTOU). 429 shape `{'detail': <EXPERIENCE.md:98 trilingual copy>, 'code': 'export_limit_exceeded', 'limit': 5000}`. Ops-configurability via `app_config` is DEFERRED (the model does not exist yet) — recorded handoff.
  - **D8 — tier gating split (4.4 vs 4.6)**: 4.4 ships the endpoint PAID-ONLY for BOTH formats — `tier != 'starter'` → 403 `{'detail': <localized upgrade copy>, 'code': 'starter_only'}` (403-with-code precedent settings.py:110; 'starter_only' chosen over Winston's 'starter_required' — John's entitlement semantics: 402 = payment state, 429 = limit, 403 = tier). Free users must NEVER receive an unwatermarked export (FR-19 contract + revenue). 4.6 relaxes CSV for free (5-row cap + watermark) and keeps xlsx starter-only forever (FR-18). The 4.5 modal's xlsx disabled-button + upgrade tooltip is 4.5's UI job.
  - **D9 — download ownership**: `GET /api/export/{id}/download/` is ownership-filtered (`user_id=request.user.id`) → 404 `{'code': 'record_not_found'}` for unknown AND foreign ids (no 403 existence leak); unparseable-UUID guard → 404 (the 4.1 canonicalization precedent — no 500). 401 anonymous/frozen stay ENTIRELY at the auth layer (4.2 D6 — zero view-level auth checks). Download contract for 4.5: `Content-Type` per format (CSV `text/csv; charset=utf-8` with BOM, xlsx `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`), `Content-Disposition: attachment; filename="dzleads-export-{id}.{ext}"` (ASCII — no RFC 5987 needed; deterministic for tests; ties to the ledger `reference_id`). 4.5 downloads via DIRECT navigation (anchor/window.location — cookie auth flows same-origin; NOT fetch+blob: double memory + loses the server's Content-Disposition). Synchronous generation — NO job/polling model (≤5,000 rows well within NFR-1; the Chargily polling precedent exists only for the external payment rail).
  - **D10 — the POST response contract (4.5 consumes verbatim)**: EXACT keys `{'id', 'format', 'row_count', 'revealed_count', 'unrevealed_count', 'credits_cost', 'included_unrevealed', 'watermark', 'created_at', 'balances'}` (pinned by exact-keys tests) with `revealed_count + unrevealed_count == row_count == credits_cost` over the EXPORTED set and `balances = user_balances()` (the 4.2 confirmed-balance contract — the pill reconciles from server truth). `revealed` = the ≤30d reveals EXISTS (the search `_revealed_ids` predicate — the modal and API can never disagree; rows revealed >30d ago count as unrevealed but still cost 1 credit). Error codes the 4.5 modal maps to ITS OWN localized copy (never the server detail verbatim — the messages.py contract): `429 export_limit_exceeded` → come-back-tomorrow, `402 insufficient_credits` → top-up, `403 starter_only` → upgrade, `404 record_not_found`, `400 invalid_payload`.
  - **D11 — the 3.7 checklist event contract (deferred-work.md entry (b))**: 4.4 MUST extend `ChecklistView._state` with `'step_export': Export.objects.filter(user_id=request.user.id).exists()` (cumulative first-ever, the step_reveal pattern) replacing the literal False at `backend/apps/search/views.py:315-318`; the CLIENT contract does NOT change (same 4-key shape). The 4.5 export mutation MUST `invalidateQueries({queryKey: checklistKeys.all})` on success — recorded handoff (the 3.7 review flagged a missing deferred-work record once; the Change Log records it for 4.5).
- **Prior-story contracts this story closes/extends**: 4.1 (the SERIALIZABLE + user-row lock precedent — `reveal_contact` services.py:110-120 — D4 reuses the exact pattern; the `_contact_data` shapes define the snapshot contact fields; the index/CheckConstraint/admin precedents); 4.2 (the thin-adapter view + typed-exception mapping + trilingual messages + the 4.2 D6 auth-layer policy); 4.3 (the client-side ledger CSV stays AS-IS — handoff: CSV-infra consolidation = 4.6; the labels-injected builder + BOM/RFC-4180/Western rules applied server-side); 3.7 (the step-export event contract — D11); 3.2 (the TOCTOU deferred-work — closed by the lock — D4).
- **Out of scope (handoff)**: the 4.5 modal (cost preview, mini-preview, confirm — the FE consumes D10 + D9); free-tier watermarked CSV + 5-row cap + watermark ROW GENERATION = 4.6 (the trilingual watermark STRINGS ship in 4.4 messages.py per John Q4; `watermark` boolean lives in the schema now); app_config ops-knob for the 5,000 cap = deferred (D7); CSV-infra consolidation = 4.6 (the 4.3 ledger CSV untouched); the 0-credit /search danger banner = 5.x (4.3 handoff).
- **NFR-1 (≤1.5s p95 on 4G)**: the POST is ONE round trip with ≤3 pre-transaction reads + ONE transaction (all PK/index seeks, no N+1, no pagination enumeration); the JSONB snapshot write (~1-2MB for 5,000 rows) is within PG16's TOAST budget; the download is ONE indexed query + in-memory regeneration (tens of ms CPU for 5,000 rows). Not CI-testable — recorded as design headroom.
- **Testing standards**: pytest + `django_db`; the conftest `create_user`/`api_client` pair + the `test_reveal_api.py` session-fixture pattern; strict-key/strict-parse + exact-keys precedents; the 4.1 vendor-guard + CheckConstraint patterns; xlsx verification PARSES the generated file (unzip + xml.etree well-formedness + cell-type assertions) — never just the MIME type (John Q5); gates = `pytest` + `ruff` + `mypy` (strict). Run ALL backend gates after EVERY task. Frontend gates NOT run (backend-only story — no FE files touched).

## Dev Agent Record

### Agent Model Used

opencode-go/deepseek-v4-flash

### Debug Log References

- RED runs: test_models.py collection error (no models module — collection fails until 1.2); test_export_debit.py ImportError (debit_export_rows missing); test_generation.py collection error (no export_service module); test_export_api.py 32 failed (no views/urls); test_checklist.py 2 new step_export tests failed (literal False); test_admin.py passed only after ChecklistView GREEN ran (admin was written in 4.2's GREEN).
- GREEN fixes during implementation: `create_user` fixture returns a User INSTANCE (not a factory — the 4.1 test pattern uses local `make_user` helpers) → test_models/test_export_debit fixtures rewritten; the CSV builder emits a standard trailing CRLF → `_csv_lines` strips before split; the RFC-4180-quoted address field broke naive `split(',')` → test parses with `csv.reader`; ruff: E501 on the xlsx XML string fragments (split at 100 cols), N817/N813 on `ElementTree as ET/etree` (import as module), I001 import sort (auto-fixed); mypy strict: `object`-typed user/records vars → `Any`/`cast` (the 4.1 pattern), `.first()` returning Any → `assert isinstance` narrowing, test `_create` returning Any → `assert isinstance(export_id, str)`; mixed people+company id set resolved 404 via "not found" — the resolver now counts people+company matches: unresolvable ids → 404, mixed types → 400 invalid_payload (Winston Q2 semantics); headroom test math fixed (4998 + 2 = 5000, the story's literal); checklist user-isolation test: the shared `api_client` cookie jar is overwritten by the second login — the test logs the second user in on a fresh `Client()` (the 4.1 cross-user precedent).
- E2E (docker PG16, live stack): `makemigrations --check` clean; migrate applied exports.0001; psql verified table + 3 CHECK constraints + `exports_user_created_idx` + FK CASCADE; seeded 6 users (starter A with 15 credits + 2 people + 1 reveal + usage 4999, free B, frozen C, 0-credit D, fresh E, fr-locale F with 15 credits); curl flows verified: POST 2 rows at usage 4999 → 429 export_limit_exceeded (atomic rollback: no exports row, no ledger debit, usage unchanged); POST 1 row → 200 exact body (id/format/row_count/revealed_count/unrevealed_count/credits_cost/included_unrevealed/watermark/created_at/balances; cost 1, revealed 1, balances 14); CSV download → `text/csv; charset=utf-8` + `attachment; filename="dzleads-export-{id}.csv"` + UTF-8 BOM + EN headers + contact data; xlsx POST (user F) → 200 (fr locale, cost 1) + download → xlsx MIME + disposition + valid zip (all 6 parts); post-cap POST → 429; free → 403 starter_only; 0-credit → 402; foreign download → 404 record_not_found; garbage id → 404; anonymous → 401; frozen → 401 account_deleted; checklist A → step_export true, E → false; psql: exports row (csv/1/1/t/f/en), ledger export_row_debit −1 balance_after 14 pool subscription reference_id == export id, daily_usage 0/5000, cache 14. E2E rows cleaned (users cascaded exports/reveals/usage; ledger rows SET_NULL-anonymised per the 2.6 contract then removed as test artifacts).

### Completion Notes List

- Story 4.4 implemented end-to-end (TDD red→green for every task): `Export` model + 0001 migration (spine DDL + rows_json/locale extensions, 3 CHECK constraints, `exports_user_created_idx`, CASCADE FK — `maintenance_tasks.py:14` label contract), `debit_export_rows` in credits/services.py (no transaction of its own — first-statement SERIALIZABLE guard + user-row lock + ledger-only balance + whole-N single-pool drawdown, AD-3/4/7), stdlib CSV + hand-rolled xlsx generators (BOM/CRLF/RFC-4180; inline-string phones + explicit numeric cells — FR-18), trilingual messages + header labels (FR-3), `create_export` orchestration (resolve → snapshot → generate BEFORE the transaction; ONE atomic block: debit → quota check+increment → exports row; 429/402 roll back everything), POST /api/export/ (strict payload, 403 starter_only paid-only gate, exact 10-key response + confirmed balances), GET /api/export/{id}/download/ (ownership-filtered 404, deterministic snapshot regeneration, exact MIME/disposition), exports quota module (5,000 constant), read-only admin, `ChecklistView._state` step_export EXISTS (the 3.7 deferred-work contract (b) — client contract unchanged).
- Gates: backend 618 pytest / ruff 0 / mypy strict 0; `manage.py check` clean. Frontend gates NOT run (backend-only story — no FE files touched). Real-PG16 E2E verified (429 atomic rollback, 200 exact body, CSV + xlsx download contracts, 403/402/404/401 mapping, checklist step_export flip, psql row/ledger/usage/cache assertions).

### File List

- `backend/apps/exports/models.py` (NEW)
- `backend/apps/exports/migrations/0001_initial.py` (NEW)
- `backend/apps/exports/services.py` (NEW — create_export orchestration + typed errors)
- `backend/apps/exports/export_service.py` (NEW — build_export_csv / build_export_xlsx)
- `backend/apps/exports/quota.py` (NEW — EXPORT_DAILY_ROW_LIMIT)
- `backend/apps/exports/messages.py` (NEW — trilingual headers/limit/starter-only/404/watermark)
- `backend/apps/exports/views.py` (NEW — ExportView + ExportDownloadView)
- `backend/apps/exports/urls.py` (NEW)
- `backend/apps/exports/admin.py` (NEW — read-only)
- `backend/apps/exports/tests/__init__.py` (NEW)
- `backend/apps/exports/tests/test_models.py` (NEW — 12 tests)
- `backend/apps/exports/tests/test_generation.py` (NEW — 22 tests)
- `backend/apps/exports/tests/test_export_api.py` (NEW — 32 tests)
- `backend/apps/exports/tests/test_admin.py` (NEW — 4 tests)
- `backend/apps/credits/services.py` (MODIFIED — debit_export_rows)
- `backend/apps/credits/tests/test_export_debit.py` (NEW — 9 tests)
- `backend/apps/search/views.py` (MODIFIED — ChecklistView step_export EXISTS)
- `backend/apps/search/tests/test_checklist.py` (MODIFIED — 3 step_export tests)
- `backend/config/urls.py` (MODIFIED — api/export include, explicit namespace)

### Change Log

- 2026-08-07: Story created (ready-for-dev) from the epic 4.4 planning spec. Customization resolution: `resolve_customization.py` FAILED (Python 3.10 — no stdlib `tomllib`; the 4.3 precedent) → manual base→team→user fallback: base `.agents/skills/bmad-create-story/customize.toml` read; no team override (`_bmad/custom/bmad-create-story.toml` absent) and no user override (absent) → effective workflow = base only (activation steps empty; persistent_facts `file:{project-root}/**/project-context.md` matched NOTHING — no project-context.md exists in the repo; recorded as unavailable). Config loaded from `_bmad/bmm/config.yaml` (user_name Bensouici Akram; communication/document language English; planning_artifacts + implementation_artifacts resolved). Inputs loaded: epic 4.4 planning spec (SELECTIVE), epic 4 index, epics DR register (UX-DR22 responsive / UX-DR24 interaction primitives), PRD FR-17/18/19/20 (`4-features.md#L161-190`), architecture (`docs/ARCHITECTURE-SPINE.md` — the spine DDL + AD index), UX (`DESIGN.md` export-modal + `EXPERIENCE.md` Export Gating + mockup `export-modal.html`), `deferred-work.md` (3.7 entry (b) + 3.2 TOCTOU), stories 4.1/4.2/4.3 (precedents), git log (HEAD 4d12b09 = Story 4.3). Web research: SKIPPED (no version-sensitive tech — Django 5/DRF stable APIs; stdlib csv/zipfile; the only candidate lib, openpyxl, is NOT approved).
- 2026-08-07: Persona consultations (parallel subagents — the project-recorded practice, 3.5/3.7/4.1/4.3 Change Log precedent). Resolved decisions recorded:
  - **Winston (architect)** — Q1 endpoint shape: plain DRF APIViews POST /api/export/ + GET /api/export/{id}/download/, FileResponse over BytesIO as_attachment, CSV `text/csv; charset=utf-8` + BOM + CRLF, xlsx MIME per AC; Q2 payload: `record_ids` ONLY (strict keys {record_ids, format, include_unrevealed}, non-empty list ≤5000, one record type, unresolved id → 404 no partial debit; the query-object is 4.5-deferred); Q3 atomic debit: `debit_export_rows` in credits/services.py opening NO transaction — the caller's ONE atomic block, debit first statement (guard + lock), whole-N single-pool drawdown, snapshot/file-gen BEFORE the transaction, TOCTOU closed by the user-row lock; Q4 rate limit: check + increment inside the same atomic block (get_or_create + F()+= n), typed ExportLimitExceededError → 429 {detail: EXPERIENCE.md:98 copy, code export_limit_exceeded, limit 5000}, quota + messages in apps/exports/quota.py, app_config deferred; Q5 storage: (c) JSONB `rows_json` + `locale` snapshot columns + deterministic regeneration — NOT blob/disk/live-DB; Q6 CSV: stdlib, stable columns (people name/role/company/wilaya/email/phone/address; company name/industry/wilaya/size_band/website/people_count), "Oran (31)" wilaya, unrevealed rows EXCLUDED when include_unrevealed=false and FULL contact data when included; Q7 xlsx: hand-rolled stdlib zipfile+XML (inline strings, explicit t="s"/"n"), package-structure test (unzip + well-formedness), 4.4 paid-only for ALL formats (403), openpyxl stays out pending approval; Q8 model: CASCADE FK (maintenance_tasks.py:14 already guards ('exports','Export')), read-only admin, CHECKs + exports_user_created_idx; Q9 step_export EXISTS in ChecklistView._state with in-method import (no circular import); Q10 response: flat export keys + nested balances; download ownership-filtered → 404 (no existence leak), invalid UUID → 404; Q11 NFR-1: ≤3 pre-tx reads + 1 transaction, all index seeks, safe. Flag recorded: debit_export_rows must be the FIRST statement of the create_export atomic block.
  - **Sally (UX)** — Q1 4.4 has ZERO UI surface; the contracts 4.5 consumes are pinned in 4.4: POST response exact keys (server truth), download contract (filename `dzleads-export-{id}.{ext}` ASCII, Content-Type, Content-Disposition), error-code mapping (429 come-back-tomorrow / 402 top-up / 404 generic, never leak existence / 400 bug); Q2 checklist EXISTS: client contract unchanged (4-key shape), 4.5 MUST invalidate checklistKeys.all on success — the 4.4 Change Log records the handoff (the 3.7 missing-record lesson); Q3 download via DIRECT navigation (not fetch+blob — loses Content-Disposition), synchronous generation, NO job/polling model; Q4 cost split: client preview (page-scoped estimate) → server truth (POST response); recorded for 4.5: page-scoped drift risk + ≤30d-revealed counting; Q5 CSV shape matches the mini-preview contract (localized headers at export time, "Oran (31)", stable columns, BOM/RFC-4180/Western numerals); Q6 the file response carries no a11y obligations (correct MIME + attachment is the whole story; step-3 announcement is the checklist card's job).
  - **John (PM)** — Q1 cost semantics: EXACTLY 1 credit per included row, revealed and unrevealed alike — never a discount; breakdown "already revealed" = the ≤30d window (the search revealed flag predicate — modal and API can never disagree); Q2 cap: hard module constant 5000, calendar-day Africa/Algiers (TIME_ZONE base.py:75), 429 whole on overshoot (never truncate), same-transaction check+increment; app_config ops-knob deferred + recorded handoff; Q3 gate split: 403 `starter_only` (402 = payment state, 429 = limit, 403 = entitlement — three distinct classes, three codes; 403 precedent settings.py:110), free users have NO export until 4.6 (feature is dark), xlsx starter-only forever; Q4 watermark: trilingual STRINGS ship in 4.4 messages.py (`[PENDING REVIEW]`), row generation + `watermark` flip = 4.6; Q5 xlsx: same 403 gate, 4.5 disabled button + tooltip, hand-rolled stdlib writer, acceptance tests must PARSE the generated file; Q6 all-or-nothing POST (402 → nothing written), response carries server-confirmed balances, pill drops from server truth only.
  - **Resolved divergences** (recorded per the practice): (a) unrevealed-row content — Sally's empty-contact-cells reading vs Winston/John's full-contact-data: RESOLVED full contact data (D6 — the pricing law: 1 credit/row for data already visible in results is indefensible; UJ-1 climax EXPERIENCE.md:281 "the data is real" with 0 revealed + 5 unrevealed; the modal's literal preview covers the surprise concern); (b) gate error code — Winston 'starter_required' vs John 'starter_only': RESOLVED `starter_only` (John's three-class code semantics + the 403 precedent); (c) POST response shape — John's nested `{'export': ..., 'balances': ...}` vs Winston/Sally flat keys: RESOLVED flat export keys + nested `balances` (majority + simpler 4.5 consumption; balances mirrors the reveal response contract).
- 2026-08-07: Scope decisions recorded (D1-D11): exports table schema lands in 4.4 (spine + rows_json + locale extensions); the full export transaction = ONE atomic block owned by create_export with debit_export_rows as its first statement; record_ids-only payload (query-object 4.5-deferred); snapshot regeneration-on-download; paid-only 403 gate (4.6 relaxes CSV); watermark strings in 4.4, generation in 4.6; CSV + xlsx both generated in 4.4 (FR-18 ACs are in the 4.4 spec; hand-rolled stdlib xlsx — no openpyxl); step_export EXISTS replaces the literal False (3.7 contract (b)); 0-row export → 400 invalid_payload (nothing written). Sprint-status 4-4 → ready-for-dev (epic-4 stays in-progress).
- 2026-08-07: Implemented (TDD) — Task 1 Export model + 0001 (spine + extensions, CHECKs, index, CASCADE), Task 2 debit_export_rows (first-statement guard contract + whole-N pool drawdown + composed-transaction rollback tests), Task 3 stdlib CSV + hand-rolled xlsx generators + trilingual messages (BOM/CRLF/RFC-4180, inline-string phones, explicit numeric cells, package-structure + parsed-XML assertions), Task 4 create_export orchestration + POST/GET views + quota + urls + read-only admin (strict payload, 403 starter_only, exact 10-key response + balances, ownership-filtered 404 downloads, snapshot regeneration), Task 5 ChecklistView step_export EXISTS (3.7 contract (b)) + exports admin tests, Task 6 full regression + real-PG16 E2E (429 atomic rollback, 200 exact body, CSV/xlsx download contracts, 403/402/404/401 mapping, step_export flip, psql ledger/usage/cache assertions; E2E rows cleaned). Gates: backend 618 / ruff 0 / mypy strict 0; `manage.py check` clean. FE gates not run (backend-only). Status → review (sprint-status 4-4 → review).
- 2026-08-07: Code review (full mode — Blind Hunter + Edge Case Hunter + Acceptance Auditor, parallel subagents). Blind 10 (2 High) + Edge 8 (1 High) + Auditor 4 (all ACs SATISFIED, 1 Medium + 2 Low + 1 info) → triaged to 15 patch + 0 defer + 1 dismiss; every patch applied (canonical UUID handling, CSV formula-injection + control-char hardening, 409 concurrent_export mapping, duplicate-id dedupe, industry-localization adjudication toward D5/FR-3, snapshot header freeze, debit guard, sprint-status.yaml UTF-16 restore, fixture/test coverage fixes, story doc fixes); gates re-run fully green (backend 630 / ruff 0 / mypy strict 0).
- 2026-08-08: Review resolved — all patch findings applied, gates fully green; Status → done (sprint-status 4-4 → done, epic-4 stays in-progress).

### Review Findings

Code review (full mode — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-08-07). Blind: 2 High + 3 Medium + 5 Low. Edge: 1 High + 3 Medium + 4 Low. Auditor: 1 Medium + 2 Low + 1 informational — all 6 AC blocks SATISFIED, contracts D1–D11 honored. Triage: 15 patch, 0 defer, 1 dismiss (Auditor's 402-trilingual coverage info — the view reuses the already-trilinguinal-tested `INSUFFICIENT_CREDITS_MESSAGES`). All patches applied; gates re-run green (backend 630 pytest / ruff 0 / mypy strict 0).

- [x] [Patch][High] Non-canonical UUID forms (uppercase/braced/compact) resolved in the DB but `records_by_id[record_id]` indexed by the raw string → unhandled `KeyError` → HTTP 500 (D9's no-500 contract; the 4.1 canonicalization guard the docstring claims) [apps/exports/services.py] — fixed: `_validate_payload` canonicalizes every id via `uuid.UUID()` (order-preserving dedupe; unparseable → 400 invalid_payload, never 404); the revealed-set filter now matches canonical forms (double-charge + wrong exclude paths closed); 3 variant tests + a 400-not-404 test added.
- [x] [Patch][High] CSV formula injection — scraped cells beginning `=`/`+`/`-`/`@` execute in Excel on open (OWASP) [apps/exports/export_service.py `_cell`] — fixed: leading formula triggers neutralized with a `'` prefix (CSV path; the xlsx inline-string path is inherently safe); tests added.
- [x] [Patch][Medium] PG serialization aborts under concurrency (same-user parallel exports, export racing a reveal) → unhandled IntegrityError/OperationalError → 500 [apps/exports/views.py] — fixed: `(IntegrityError, OperationalError)` → 409 `concurrent_export` with trilingual `CONCURRENT_EXPORT_MESSAGES` (the 4.2 precedent); monkeypatch test added (the user-row lock + rollback mean nothing was written).
- [x] [Patch][Medium] Company `industry` cell hardcoded `name_en` vs the story task's "localized industry name" — adjudicated toward D5/FR-3 (data values never translated; only headers + wilaya localize — EXPERIENCE.md:215): `name_en` kept, the story task contract amended to record the resolution, and the download-body contract test strengthened (fr headers, `Advertising E2E44` industry cell, "Oran (31)", size band).
- [x] [Patch][Medium] C0 control characters (0x00-0x08/0x0B/0x0C/0x0E-0x1F) in scraped data → malformed sheet1.xml Excel/LibreOffice refuse to open + broken CSV [apps/exports/export_service.py] — fixed: stripped in `_cell` + `_xml_escape`; CSV + xlsx tests added (incl. XML well-formedness re-parse).
- [x] [Patch][Medium] Duplicate `record_ids` (e.g. `[p1, p1]`) → resolver mis-branches → 404 for an existing id [apps/exports/services.py `_resolve_record_type`] — fixed by the order-preserving dedupe in `_validate_payload` (cost = distinct rows); test added.
- [x] [Patch][Medium] Raw client strings filtered against canonical `Reveal.record_id` → reveals silently missed (double-charge; wrong exclude when include_unrevealed=false) [apps/exports/services.py `_revealed_ids`] — closed by canonicalization (see the High patch); covered by the uppercase-variant revealed-count assertions.
- [x] [Patch][Low] `debit_export_rows` unguarded against row_count < 1 (negative N would CREATE credits) [apps/credits/services.py] — fixed: `ValueError` guard before the guard; 0/-3 unit tests added.
- [x] [Patch][Low] D3 "byte-for-byte" regeneration broken: download re-derived headers from the live label dict, so `[PENDING REVIEW]` label edits would silently change previously-paid files [apps/exports/views.py + services.py] — fixed: header labels frozen into `rows_json['headers']` at POST; download reads the snapshot with a live-dict fallback; snapshot-freeze test added.
- [x] [Patch][Low] Tier gate ran before payload validation — free users with malformed bodies got 403 instead of 400 (entitlement-state leak; 4.5 code mapping ambiguity) [apps/exports/views.py] — fixed: `_validate_payload` runs first (400), then the 403 gate; test added.
- [x] [Patch][Low] `sprint-status.yaml` UTF-16 file corrupted (BOM + mojibake arrows) by the PowerShell write path — restored from git (UTF-16 LE, arrows intact) and re-applied the 4-4 → review change with the matching encoding.
- [x] [Patch][Low] Shared `api_client` cookie jar in `test_download_foreign_export_404` silently re-logged-in as the second user [apps/exports/tests/test_export_api.py] — fixed: `export_session` creates a fresh `Client()` per login (the 4.1 cross-user pattern).
- [x] [Patch][Low] Missing coverage: 30-day reveal-window boundary (31-day-old reveal flips revealed_count) + the vacuous `test_headers_never_repeat_data_in_workbook` (replaced with the workbook sheet-name assertion) — boundary test added; vacuous test replaced.
- [x] [Patch][Low] `EXPORT_LIMIT_MESSAGES` carried trailing periods not in the EXPERIENCE.md:98 copy (D7 verbatim contract) — removed; the 429 ar-detail test remains self-consistent.
- [x] [Patch][Low] Task-level checkboxes Tasks 2-6 left `[ ]` in the story file (Auditor doc finding) — marked `[x]` per the 4-1 convention.
- [x] [Dismissed] 402 insufficient-credits trilingual detail coverage gap (Auditor informational) — the view reuses `INSUFFICIENT_CREDITS_MESSAGES`, already trilingual-tested in the credits app; the export API tests pin the code + code-path, not each localized string.
