---
story_id: 4.5
epic: 4
title: Story 4.5 — Export Modal Component
Status: done
frs: [FR-17, FR-18, FR-19, FR-20]
ads: [AD-18, AD-19, AD-20, AD-21]
ux_drs: [UX-DR12, UX-DR20, UX-DR21, UX-DR24]
baseline_commit: 93fcd43
---

# Story 4.5: Export Modal Component

Status: done

## Story

As a **user ready to export search results**,
I want **a modal that shows the credit cost breakdown, a literal CSV mini-preview, and lets me confirm the export with the right format**,
So that **I know exactly what I'm paying and what I'm getting before committing credits**.

## Acceptance Criteria

**Given** the Export Modal component
**When** a user clicks "Export" from the search results toolbar
**Then** a shadcn Dialog opens with {rounded.xl}, max-width 560px

**Given** the modal layout for paid (Starter) users
**When** the modal renders
**Then** it shows top to bottom:
1. Row count in the current result set
2. Credit cost breakdown: "n revealed + m unrevealed = total credits"
   - In {typography.data} with `tabular-nums`
   - Per-line cost stated
3. Balance-after line
4. "Include rows I have not revealed yet" checkbox (checked by default)
5. A literal CSV mini-preview in a {colors.muted} {rounded.md} inset:
   - Sample data rows showing what the CSV will contain
   - In {typography.small}, Western Arabic numerals
   - (No watermark for Starter users)
6. Confirm button: {colors.primary} / {colors.primary-foreground}
7. Format toggle: CSV / XLSX (both enabled for Starter)

**Given** the modal for free users
**When** a free user opens the export modal
**Then** it additionally states:
- "Free tier: export up to 5 rows" with the specific rows shown
- The CSV mini-preview shows the localized watermark header row and footer row
- The XLSX button is visibly disabled with tooltip: "Upgrade to use Excel export"

**Given** the export rate limit state
**When** a user has hit the 5,000 rows/24h limit
**Then** the modal shows the "come back tomorrow" state instead of the export form
**And** the message is localized

**Given** the watermark preview
**When** a free user views the modal
**Then** the watermark is shown as literal content rows (header + footer) in the mini-preview
**And** the watermark string is localized (FR-19)

**Given** modal accessibility
**When** the modal opens
**Then** focus is trapped inside, initial focus on the first focusable element
**And** Esc closes the modal
**And** focus returns to the export trigger on close

## Tasks / Subtasks

- [x] **Task 1: i18n keys ×3 + shape test** (ACs: all localized copy; the check:i18n parity gate; the `i18n-shape.test.ts` precedent — Dev Notes D14)
  - [x] 1.1 RED: `frontend/src/__tests__/i18n-shape.test.ts` — UPDATE — a new `describe('export modal i18n shapes (A-3 locales)')` block asserting: every pinned `export.modal.*` + `export.watermark` + `billing.upgrade_stub` key resolves in ALL THREE locales; the interpolation params are declared in every locale — `{revealed}`, `{unrevealed}`, `{total}` in `cost_breakdown`, `{count}` in `confirm`; the verbatim-copy keys (`come_back_tomorrow`, `preview_notice`, `watermark`, `billing.upgrade_stub`) match the EXPERIENCE.md:98/96/L95 + backend `messages.py` strings EXACTLY (no trailing periods — the 4.4 D7 lesson); the non-verbatim fr/ar values carry the `[PENDING REVIEW]` marker (the translation-review convention). Run gates → RED (keys missing).
  - [x] 1.2 GREEN: `frontend/messages/en.json` + `frontend/messages/fr.json` + `frontend/messages/ar.json` — UPDATE — add the pinned keys (Dev Notes D14 list — all ×3, Western numerals via `{...}` params, the literal minus U+2212 in the confirm key where the mockup pins it). Reuse `common.credits.exporting`, `common.credits.no_credits`, `common.actions.export` (no duplicates). "Excel .xlsx" is a brand constant — untranslated, identical ×3, no key.
  - [x] 1.3 Run FE gates (vitest / lint / typecheck / check:i18n) — green.

- [x] **Task 2: Data layer — `exportKeys` + `ExportService` + typed guards** (AD-19/AD-21; Dev Notes D4)
  - [x] 2.1 RED: `frontend/src/__tests__/export-keys.test.ts` + `frontend/src/__tests__/export-service.test.ts` — NEW — keys: `exportKeys.all === ['export']` frozen tuple (the `as const` factory pattern — checklistKeys precedent); service: `create` called with the EXACT payload shape `{record_ids: string[], format: 'csv'|'xlsx', include_unrevealed: boolean}` (the 4.4 D10 contract — no extra keys, ids as strings); response typing: the 10-key `CreateExportResponse` (`id/format/row_count/revealed_count/unrevealed_count/credits_cost/included_unrevealed/watermark/created_at/balances` — `balances` typed to the `CreditBalances` shape `applyConfirmedBalance` consumes — the 4.2 contract); guards narrow correctly: `isExportLimitError` (429 + `code === 'export_limit_exceeded'`), `isStarterOnlyError` (403 + `code === 'starter_only'`), `isConcurrentExportError` (409 + `code === 'concurrent_export'`), `isRecordNotFoundError` (404 + `code === 'record_not_found'`), and `isInsufficientCreditsError` REUSED from `reveal-service.ts` (402 — never duplicated); non-matching responses → false.
  - [x] 2.2 GREEN: `frontend/src/lib/queryKeys/export.ts` — NEW — `exportKeys = { all: ['export'] as const }` (the mutation needs NO key — Winston ruling: plain `useMutation` + an in-flight guard, the reveal precedent). `frontend/src/lib/api/export-service.ts` — NEW — `export type ExportFormat = 'csv' | 'xlsx'`; `export interface CreateExportResponse { id: string; format: ExportFormat; row_count: number; revealed_count: number; unrevealed_count: number; credits_cost: number; included_unrevealed: boolean; watermark: boolean; created_at: string; balances: CreditBalances }` (import the reveal-service `CreditBalances` type — single source); `export class ExportService extends HttpClient` with `create(payload: { record_ids: string[]; format: ExportFormat; include_unrevealed: boolean }): Promise<CreateExportResponse>` — `this.client.post<CreateExportResponse>('/export/', payload)` (prefix-relative — the `c5f5709` double-`/api` lesson); the four narrowers as exported type guards (the `isRateLimitError` pattern). Singleton `export const exportService = new ExportService()`.
  - [x] 2.3 Run FE gates — green.

- [x] **Task 3: `useExport` mutation hook** (AD-20/21; Dev Notes D4/D5)
  - [x] 3.1 RED: `frontend/src/__tests__/use-export.test.tsx` — NEW — `renderHook` inside `QueryClientProvider` + mocked `export-service` + mocked CreditProvider (the `use-reveal.test.tsx` pattern):
    - **phases**: idle before mutate; `isPending` true while the mutation is in flight (deferred promise); after resolve → not pending; after reject → `error` set.
    - **error narrowing (AD-21 hook-owns-error-mapping)**: mocked rejections narrow to the union — 429 → `'limit'`, 402 → `'credits'`, 403 → `'starter'`, 404 → `'generic'`, 400 → `'generic'`, 409 → `'concurrent'`, network/other → `'generic'`; `error` is `undefined` when idle/success.
    - **success side-effects**: onSuccess → `applyConfirmedBalance(result.balances)` called (mock the CreditProvider context); `queryClient.invalidateQueries({ queryKey: checklistKeys.all })` called (the 3.7 deferred-work contract (b) — WITHOUT this the checklist card's step-3 live check-off dies); the `onSuccess` option callback receives the full result (parent-owned close+navigate — the `useSearchResults` onSuccess precedent).
    - **double-submit guard**: while pending, a second `create` call is a no-op (the in-flight guard).
    - **enabled gating**: no calls while the hook is "disabled" (no submitted/ids — Dev Notes D2).
  - [x] 3.2 GREEN: `frontend/src/hooks/useExport.ts` — NEW — `export type ExportError = 'limit' | 'credits' | 'starter' | 'concurrent' | 'generic'`; `useExport({ onSuccess }: { onSuccess?: (result: CreateExportResponse) => void })` — `useMutation({ mutationFn, onSuccess })`; a module-scoped or ref-based `inFlight` guard checked in a wrapper `create()` (the reveal in-flight precedent — synchronous read, second call no-ops while pending); internal narrowing via the service guards (first-match wins: limit → credits → starter → concurrent → record-not-found → generic) into the `error` state; returns `{ create, isPending, error }` (the slim-facade precedent — `useChecklistMutations`). NO `placeholderData`, NO global toast, NO mutationKey.
  - [x] 3.3 Run FE gates — green.

- [x] **Task 4: `useExportPreview` — full-set composition + missing-page fetch** (Dev Notes D1/D2/D12; the 4.3 ledger CSV page-scoped lesson vs the EXPERIENCE.md:126 "full current result set" scope)
  - [x] 4.1 RED: `frontend/src/__tests__/use-export-preview.test.ts` — NEW — `renderHook` with a real `QueryClient` whose cache is pre-seeded with `searchKeys.results(...)` entries + a mocked `search-service`:
    - **cached-page composition**: 3 pages × 100 rows seeded in the cache → the collector returns ALL 300 ids + the revealed/unrevealed counts WITHOUT calling the service (0 quota burn).
    - **missing-page fetch**: only page 1 cached, `total = 300` → pages 2–3 fetched VIA the service DIRECTLY (not through the cache — no cache writes, the ephemeral-collector contract); the result merges in page order; the service receives `{filters: <parsed filtersJson>, page, sort}` + the abort signal.
    - **free tier (5-row cap)**: `tier === 'free'` → only page 1 is consulted (0 fetches when cached), `ids` = first 5 rows, counts over those 5 (the first-by-current-sort contract — EXPERIENCE.md:126).
    - **revealed counting**: rows with `revealed: true` count revealed, the rest unrevealed (the search `≤30d` predicate == the server predicate — the modal and API can never disagree, 4.4 D10).
    - **abort on unmount/close**: the fetch is cancelled via the AbortController signal (no state update after unmount — the 4.2 timer-hygiene lesson).
    - **collector error** (search 429 / 5xx / network): → `error` state (the preview slot shows the retry state; the confirm STAYS usable — server truth at POST).
  - [x] 4.2 GREEN: `frontend/src/hooks/useExportPreview.ts` — NEW — `useExportPreview({ tab, filtersJson, sort, nonce, total, tier })` — derives `pageCount = min(ceil(total / 100), 10)` (the `MAX_NAVIGABLE_RESULTS` 1,000 cap — ARCHITECTURE-SPINE.md#L698); for each page in order: try `queryClient.getQueryData(searchKeys.results(tab, filtersJson, page, sort, nonce))` (cache-first — the user's paged-through pages cost 0); missing pages → `searchService.searchPeople/searchCompanies({ filters: JSON.parse(filtersJson), page, sort }, signal)` DIRECT (ephemeral — never written to the query cache, no nonce pollution, no `setQueryData`); accumulate `ids: string[]` (in page order), `revealedCount`, `unrevealedCount`, `totalRows = ids.length`; free tier → slice to the first 5 rows + counts over the slice; returns `{ preview: { ids, revealedCount, unrevealedCount, totalRows } | null, isCollecting, error, retry }` — `retry` re-runs the collector. AbortController lives in an effect keyed on `open` (re-run on re-open). NO `useQuery` (this is not cached server-state — it is an ephemeral computation over the search cache + one-shot fetches; AD-21's "custom hooks only" is honored — the component never touches React Query).
  - [x] 4.3 Run FE gates — green.

- [x] **Task 5: `ExportModal` component — all states** (the ACs; DESIGN.md:333 anatomy; the mockup; Dev Notes D3/D6/D7/D8/D9/D13)
  - [x] 5.1 RED: `frontend/src/__tests__/export-modal.test.tsx` — NEW — the `saved-search-name-dialog.test.tsx` template (controlled `open` + `onClose`, QueryClientProvider wrapper, mocked services + SessionProvider + CreditProvider + next-intl):
    - **starter anatomy (AC literal)**: opens → `DialogTitle` = `export.modal.title`; the row-count line shows the count in Western numerals (`String(...)` interpolation — AD-8); the cost breakdown renders `export.modal.cost_breakdown` with `{revealed}`/`{unrevealed}`/`{total}` interpolated as **strings** and the line carries `tabular-nums` + `text-data` classes; balance-after line shows `display_balance − total` (mock CreditProvider balance); the include-unrevealed checkbox is PRESENT AND CHECKED BY DEFAULT; the preview inset (`bg-muted rounded-md`) contains the LOCALIZED header labels + the FIRST 2 real result rows + an ellipsis line (the mockup — never fabricated rows); the confirm button carries the `export.modal.confirm` label with `−{count}` (literal U+2212) in Western numerals; the format toggle shows CSV + "Excel .xlsx" with CSV selected by default (both enabled — starter).
    - **include_unrevealed toggle**: unchecking → the cost line and the confirm `−{count}` recompute to the REVEALED count only (per-line cost contract — FR-17); re-checking restores the total.
    - **format toggle**: switching to XLSX keeps the same cost/confirm semantics (the credit cost is format-independent — FR-18) and marks Excel as the active option.
    - **free tier states**: mocked `tier: 'free'` → the cap note `export.modal.rows_capped` renders with the count 5; the preview shows the localized watermark header row, 2 real rows, and the watermark footer row (the `export.watermark` strings — literal content rows, NOT overlays); the XLSX button is `aria-disabled` but FOCUSABLE with the `export.modal.xlsx_upgrade` caption wired via `aria-describedby` (no opacity fade — the reveal-button tonal treatment); clicking XLSX fires the `billing.upgrade_stub` toast (the Epic-5 stub precedent — `common.credits.no_credits`-style toast) and does NOT call the mutation; the confirm button click fires the `billing.upgrade_stub` toast and **NEVER calls `exportService.create`** (assert the mock service was NOT called — the 4.4 D8 "free users have NO export until 4.6" contract; the 4.5 free-tier confirm routes to the upgrade path).
    - **come-back-tomorrow (429)**: mocked POST rejection 429 → the form region (cost lines + checkbox + preview + buttons) is REPLACED by the `export.modal.come_back_tomorrow` copy (EXPERIENCE.md:98 verbatim, no trailing period) with `role="status"` + `tabIndex={-1}` + focus moved into it; the DialogTitle UNCHANGED; the close affordance still works.
    - **402 (server)**: mocked 402 → the form region replaced by the `export.modal.error_credits` state (no recovery dialog — the 5.x stub, Dev Notes D9).
    - **client-side 0-credit guard**: mocked balance < cost → the confirm button flips to `aria-disabled` (focusable) tonal with the `export.modal.insufficient` caption; click fires the `common.credits.no_credits` toast; the mutation is NOT called; the balance-after line still shows the honest computed value (negative allowed — Western minus).
    - **404 / 400 / 409 / network**: each maps to its localized state — `error_not_found` (404), `error_generic` (400/network), `error_concurrent` (409) — inline `role="alert"` in the form region (NO global toast — AD-21).
    - **success flow**: mocked 200 → the modal CLOSES (`onOpenChange(false)`) → `navigator.assign('/api/export/<id>/download/')` is called (mock the http-client `navigator` surrogate — Dev Notes D3); the credits pill reconciliation (`applyConfirmedBalance`) and `checklistKeys.all` invalidation happen in the hook (Task 3 covers the call; here assert no crash).
    - **a11y**: initial focus lands on the confirm button (`autofocus`); Esc closes; closing restores focus to the trigger (Base UI owns all three — assert the trigger element receives focus after close; NO custom focus code in the component); `aria-labelledby` resolves to the real DialogTitle (no visually-hidden hack).
    - **mobile smoke**: the modal renders at a <md viewport without error (the `max-w-[calc(100%-2rem)]` default + `sm:max-w-[560px]` override + scrollable content — Dev Notes D13).
  - [x] 5.2 GREEN: `frontend/src/components/search/ExportModal.tsx` — NEW — "use client"; controlled `{ open, onOpenChange, tab, filtersJson, sort, nonce, total, tier, balance }` props; local `useState` for `includeUnrevealed` (default `true`) + `format` (default `'csv'`) — **AD-18 scoped deviation: NO RHF/zod** (a preference picker with zero validation surface; the SavedSearchNameDialog precedent; recorded in the Change Log — Dev Notes D2); uses `useExportPreview` (Task 4) + `useExport({ onSuccess })` (Task 3); renders the anatomy per DESIGN.md:333 with the EXACT token mapping (Dev Notes D3): the registry `Dialog`/`DialogContent className="sm:max-w-[560px] max-h-[calc(100dvh-1rem)] overflow-y-auto"` (the base-nova dialog.tsx — no new dependency, no registry edits); cost lines in `text-data tabular-nums` with every numeral `String()`-interpolated and wrapped in a `font-latin` span (AD-8 — Western numerals in ALL locales); the preview inset `bg-muted text-muted-strong rounded-md p-3 text-sm max-h-40 overflow-y-auto` with `dir="ltr"` on the CSV content (content direction, NOT layout — the box geometry stays logical, AD-9; the mockup pins ltr+font-latin for the data); confirm `bg-primary text-primary-foreground rounded-md min-h-11 md:min-h-10` (UX-DR21 44px) with spinner + `sr-only` `common.credits.exporting` + `aria-busy` + pinned width while pending (the reveal-button loading precedent); the XLSX toggle + CSV toggle as real buttons (`aria-pressed` on the active format); free tier: cap note + watermark rows + XLSX `aria-disabled` tonal + `export.modal.xlsx_upgrade` caption via `aria-describedby`; the 429/402/error states swap the form region (each with `role="alert"`/`role="status"` + `tabIndex={-1}` focus handling — the vanished-control rule, Sally Q3); on success: close THEN `navigator.assign(`/api/export/${id}/download/`)` (attachment download keeps the page — the page does NOT unload; close-first so Base UI restores focus to the trigger, Dev Notes D3); NO announcement, NO success toast (the browser's native download affordance is the feedback — Sally Q4).
  - [x] 5.3 Run FE gates — green.

- [x] **Task 6: `ExportToolbar` trigger + SearchPage wiring** (the toolbar AC; Winston Q7)
  - [x] 6.1 RED: `frontend/src/__tests__/export-toolbar.test.tsx` — NEW — renders the toolbar: the Export button (label `common.actions.export`) is visible when the results query has data with `results.length > 0`; hidden when no query / empty results; visible for BOTH tiers (free tier sees it — the 403 path IS the upgrade funnel) and BOTH tabs (people + company); `aria-disabled` while the results query is fetching (no export POST racing a pagination fetch that changes the id set — Winston Q7); the button is OUTSIDE the `results-status` live region (no announcement noise); click opens the ExportModal (open state flips).
  - [x] 6.2 GREEN: `frontend/src/components/search/ExportToolbar.tsx` — NEW — a slim component: the trigger Button (`min-h-11 md:min-h-10`, `common.actions.export`, `aria-disabled` while `isFetching` — focusable, the UX-DR21 pattern) + the controlled `<ExportModal>`; props `{ tab, submitted, nonce, query, user, balance }` threaded from SearchPage (Dev Notes D11 — the wiring contract). `frontend/src/components/search/SearchPage.tsx` — UPDATE — render `<ExportToolbar ...>` BETWEEN the `results-status` live region and `ActiveFilterChips` (Winston Q7 — the count-line region; the button must not live inside the aria-live region); pass `submitted`, the query nonce, `query.data.total`, the tab, `user.tier`, and the CreditProvider balance; visibility gated on `submitted !== null && query.data && query.data.results.length > 0`; the modal needs NO extra fetch at trigger (the collector runs on open).
  - [x] 6.3 Run FE gates — green.

- [x] **Task 7: Full FE regression + parity** (NFR-1 note; Dev Notes D12)
  - [x] 7.1 Full FE gates: `vitest run` (full suite — the ~600-test baseline) + `lint` + `typecheck` + `check:i18n` — ALL green. (`manage.py` backend gates: NOT run — 4.5 is frontend-only, zero backend files touched; the 4.4/4.3 precedent.)
  - [x] 7.2 Contract smoke (unit-level, no browser): the export mutation's onSuccess fires `invalidateQueries({ queryKey: checklistKeys.all })` (the 3.7 contract (b) — step-3 live check-off); `applyConfirmedBalance` fires with the POST `balances` (the pill reconciliation contract); the 429/402/403/404/400/409 codes map to the modal's OWN localized states (never the server detail verbatim — the messages.py contract).
  - [x] 7.3 NFR-1 headroom note (recorded, not CI-testable): modal-open preview = cached-page reads + at most 9 direct page fetches (1,000-row cap; sequential; each ≤100ms-300ms on 4G) with a collecting state — bounded by the truncated result set; the POST is ONE round trip; the download is a browser-native navigation. Quota note (FR-7): only UNCACHED pages burn a search count at modal open; a user who paged through the set pays 0; the free tier pays 0 (page 1 only). Recorded handoff: if analytics show preview-abuse, 4.6 ships a quota-free preview endpoint (John red flag).

## Dev Notes

- **Source of truth — the planning spec**: `_bmad-output/planning-artifacts/epics/epic-04-reveal-credit-export/story-05-export-modal-component.md` (all ACs verbatim). FR-17/18/19/20 in `4-features.md#L161-190`. UX: `DESIGN.md#L333` (the export-modal anatomy — the authoritative ordering), `DESIGN.md#L345-354` (Do's: logical properties AD-9, Western numerals AD-8, tabular-nums, muted-strong-on-muted, watermark-as-content), `EXPERIENCE.md#L126` (export modal pattern — scope "full current result set", free cap first-by-sort, watermark preview, come-back-tomorrow), `EXPERIENCE.md#L98` (come-back-tomorrow trilingual verbatim), `EXPERIENCE.md#L96` (watermark preview notice verbatim), `EXPERIENCE.md#L95` (upgrade dialog title verbatim), `EXPERIENCE.md#L149` (rate-limited export state pattern), mockup `ux-designs/.../mockups/export-modal.html` (the free-tier ar pins: title "(مجاني)", cap line, cost line "التكلفة: 0 مُظهَر + 5 غير مُظهَر" + "5 أرصدة", balance-after "الرصيد بعد التصدير", checkbox "تضمين الصفوف التي لم أُظهرها بعد (تُحتسب أرصدة)", watermark literal rows, confirm "تأكيد التصدير (−5 أرصدة)", xlsx aria-disabled + tooltip). Architecture: `docs/ARCHITECTURE-SPINE.md` — AD-18 `#L313-326`, AD-19 `#L329-334`, AD-20 `#L337-341`, AD-21 `#L343-361`, component tree `#L365-433` (ExportModal under ResultsArea), search pagination/truncation `#L696-698` (100/page, 1,000 cap), search rate limit `#L700-708` (30 free / 100 Starter, increment per request). Review-a11y: `review-accessibility.md` (findings binding the modal — Dev Notes D13).
- **CRITICAL CONTRACTS 4.5 MUST HONOR (verbatim — the 4.4 story records D1–D11):**
  - **D10 — the 4.4 POST contract**: payload EXACTLY `{record_ids, format, include_unrevealed}` — `record_ids` ONLY (string[] ≤5,000, one record type; the query-object POST shape was 4.4-deferred and is REJECTED here — the exact-keys tests pin it), response EXACTLY `{id, format, row_count, revealed_count, unrevealed_count, credits_cost, included_unrevealed, watermark, created_at, balances}` with `revealed + unrevealed == row_count == credits_cost` over the EXPORTED set and `balances` = the confirmed ledger read the pill reconciles against (`applyConfirmedBalance`). The POST response is SERVER TRUTH re-rendered in the same preview slot — the 4.5 modal previews BEFORE confirm, the POST response is the reconcile.
  - **D9 — the 4.4 download contract**: `GET /api/export/{id}/download/` via DIRECT navigation (same-origin cookie auth through Caddy) — `navigator.assign` (the http-client surrogate), NEVER fetch+blob (double memory + loses Content-Disposition); filename `dzleads-export-{id}.{ext}`; an attachment download does NOT unload the page — the modal closes FIRST (Base UI focus-return to the trigger), then assign. Error codes map to the modal's OWN localized states: `429 export_limit_exceeded` → come-back-tomorrow, `402 insufficient_credits` → top-up/recovery state (5.x stub), `403 starter_only` → upgrade path, `404 record_not_found` / `400 invalid_payload` → generic, `409 concurrent_export` → retry state — NEVER render the server detail verbatim (the messages.py contract).
  - **D11 — the 3.7 deferred-work event contract (b) — FRONTEND HALF**: the 4.5 export mutation MUST `invalidateQueries({ queryKey: checklistKeys.all })` on success — without it the checklist card's step-3 live check-off dies (the 4.2 reveal precedent; the 4.4 Change Log recorded this obligation).
  - **4.6 owns**: the actual free-tier export (5-row cap + watermark ROWS in the generated file; the trilingual watermark strings ship in `backend/apps/exports/messages.py` — the FE preview shows them as literal content rows via ITS OWN pinned i18n keys, never the server); xlsx stays starter-only forever. **4.4 D8 recorded: free users have NO export until 4.6 — the API returns 403 starter_only; the 4.5 free-tier modal renders its states but its confirm MUST NOT reach a successful POST** (routes to the upgrade stub).
  - **FE conventions**: AD-19 service (ExportService), AD-20/21 (useExport hook + exportKeys factory, explicit phases, enabled gating, no placeholderData, no global toast — inline `role="alert"`; the useReveal/useChecklistMutations patterns), AD-8 Western numerals everywhere (`String()` interpolation — never next-intl numeral formatting), AD-9 logical properties only (RTL smoke), UX-DR21 (44px touch targets `min-h-11 md:min-h-10`, aria-disabled-but-focusable tonal — NO opacity fade), UX-DR22 (modal must work at <md).
  - **i18n**: ALL new modal copy ×3 locales (check:i18n parity gate); non-verbatim fr/ar = `[PENDING REVIEW]` (the translation-review convention); verbatim shipped copy (EXPERIENCE.md:96/98, L95, the 4.4 en watermark) = no flag.
  - **No new dependencies**: the shadcn Dialog ALREADY EXISTS in the registry (`frontend/src/components/ui/dialog.tsx` — base-nova, `@base-ui/react/dialog`, logical properties, focus trap/Esc/focus-return built in). Base UI is in package.json. NO registry edits (the 3.3/3.4 deferred-work caveat). The skill halts on unapproved deps — none needed.
  - **Handoffs still standing** (4.4-recorded): CSV-infra consolidation = 4.6 (the 4.3 ledger CSV stays as-is); the 0-credit `/search` danger banner = 5.x; the modal's upgrade/recovery DIALOGS = 5.x stubs (4.5 uses the toast-stub precedent — CreditsPill/RevealControl); if preview-abuse appears, a quota-free preview endpoint = 4.6 (John red flag).
- **D1 — record_ids assembly (the 4.4-deferred decision, RESOLVED)**: the modal composes the FULL current result set at OPEN via **cache-aware composition** — read every page from the `searchKeys.results` cache (`queryClient.getQueryData`), fetch ONLY the missing pages through the search service DIRECTLY (ephemeral — never written to the query cache, no nonce pollution, AbortController on close). Worst case ≤9 extra requests (1,000-row truncation cap); a user who paged through the set pays 0; the free tier consults page 1 only (first-5 cap) = 0 extra. This burns the FR-7 search quota ONLY for uncached pages and ONLY on explicit export intent (recorded — the 4.3 ledger CSV precedent stays page-scoped for the same quota reason; EXPERIENCE.md:126 "full current result set" wins the preview-exactness argument — the "never surprise" D6 contract; a page-1-only preview would misprice the confirm label).
- **D2 — AD-18 scoped deviation (recorded)**: RHF + zod are NOT used for the modal's two inputs (a default-checked checkbox + a 2-way format toggle — zero validation surface, no error fields, no dirty tracking). AD-18's intent is validation hygiene for user-entered data; this is a preference picker (the SavedSearchNameDialog local-state precedent). Local `useState`. Recorded so no future review flags it as a violation.
- **D3 — download seam**: success → close the modal (Base UI returns focus to the trigger — the trigger stays mounted) → `navigator.assign('/api/export/${id}/download/')`. NO intermediate "preparing" state (the server generates synchronously — tens of ms; a fake spinner would flash). Focus-return on the download itself is MOOT (attachment navigation keeps the page; the browser's native download affordance is the only feedback — no toast, no live-region chatter).
- **D4 — data layer shape**: `exportKeys.all` (mutation unkeyed — Winston), `ExportService.create` with the strict 4.4 payload, `CreateExportResponse` typed exactly to the 10-key contract with `balances: CreditBalances` (reuse the reveal-service type), four new narrowers + reuse `isInsufficientCreditsError`. The hook: `useExport({ onSuccess })` → `{ create, isPending, error }`; error narrowing to the `ExportError` union happens IN THE HOOK (AD-21 — one mapping locus; the component switches on the union, never imports guards); in-flight double-submit guard.
- **D5 — error→state mapping**: `limit` → come-back-tomorrow region swap; `credits` → 402 state (client-side 0-credit guard renders the same state PRE-confirm when `balance !== null && balance < cost` — the server stays authoritative, the 4.2 toast precedent upgraded to a full modal state); `starter` → the free-tier upgrade path (toast stub — reachable only in tests until 4.6; the real 403 must never be a user-visible path in 4.5 because free-tier confirm never POSTs); `concurrent` → retry state; `generic` → 404/400/network state.
- **D6 — preview content**: starter = localized header labels + FIRST 2 REAL result rows (mockup, not 3) + ellipsis; free = localized watermark header row + 2 real rows + ellipsis + watermark footer row. NEVER fabricated rows (the D6 literal-data contract); rows = first-by-current-sort; 4 display columns (people: name/role/company/wilaya; company: name/industry/wilaya/people_count — the full 7/6-column file stays file-side truth; the delta is a 4.6 caption opportunity). Watermark strings = the FE's OWN pinned i18n keys (verbatim from the 4.4 `WATERMARK_MESSAGES`) — the modal never renders server detail (the messages.py contract).
- **D7 — free-tier confirm**: label stays mockup-literal `Confirm export (−{count} credits)` (the mockup IS the free-tier UJ-1 climax; −N is the honest 4.6 price; John's drop-the-suffix suggestion overruled by the mockup literal — Change Log (d)); the ACTION fires the `billing.upgrade_stub` toast (the Epic-5 recovery-dialog stub precedent — RevealControl/CreditsPill) and NEVER calls the mutation (4.4 D8).
- **D8 — come-back-tomorrow**: 429 → the FORM REGION (cost lines + checkbox + preview + buttons) is replaced by the copy (EXPERIENCE.md:98 verbatim ×3, no trailing period); the DialogTitle UNCHANGED; `role="status"` + `tabIndex={-1}` + focus moved into the region (the vanished-control rule — the confirm the user activated is gone); no retry button (it would 429 again); the close affordance stays.
- **D9 — 0-credit / 402**: client guard while `balance !== null` (confirm `aria-disabled` tonal + `export.modal.insufficient` caption via `aria-describedby` + toast on click — the conversion-path pattern, NOT a dead end); server 402 → the minimal localized state (no recovery dialog, no upgrade entry — 5.x scope); `balance === null` → balance-after renders `—` and confirm is inert until known.
- **D10 — balance-after**: `display_balance − credits_cost` pre-confirm (CreditProvider), `tabular-nums` Western; the POST `balances` re-renders the slot post-confirm (never-surprise); negative allowed (Western minus, bidi-safe).
- **D11 — SearchPage wiring**: the toolbar renders BETWEEN the `results-status` live region and `ActiveFilterChips` (the button must NOT live inside the aria-live region — announcement noise); visible when `submitted !== null && query.data && query.data.results.length > 0`; BOTH tabs (people + company — two page.tsx wrappers render SearchPage with `tab`); free tier included (the upgrade funnel); `aria-disabled` while `query.isFetching` (no POST racing a pagination fetch). Threaded props: `tab, submitted (filtersJson/sort/page), nonce, total, tier, balance`.
- **D12 — the preview collector vs the search quota (FR-7)**: every `GET /api/search/...` request increments `daily_usage.search_count` (views.py:201/235 — 30/day free, 100/day Starter). The collector fetches ONLY uncached pages; a user who paged through the full set pays 0; the free tier pays 0. A failed collector (search 429/5xx) renders the preview retry state and the confirm STAYS usable (server truth at POST).
- **D13 — a11y (review-accessibility findings that bind)**: (a) the modal's `aria-labelledby` resolves to the real `DialogTitle` (no visually-hidden hack); NO `DialogDescription` (the dense visible copy IS the description — a hidden one risks duplication drift); (b) Base UI owns focus trap / initial focus / Esc / focus-return — ZERO custom focus code in the component; `autofocus` on the confirm button; the trigger must stay mounted while the modal is open (the focus-return target); (c) `aria-disabled` controls stay focusable with tonal `bg-muted border-border text-muted-strong` treatment and a caption — NEVER opacity-faded (AA + the conversion-path contract; note: `text-muted-strong` is a REAL token in this codebase despite the base-nova gap — hmm, NO: verify at dev time whether `muted-strong` exists in globals.css; the reveal-button uses it at RevealControl.tsx:223 — if it does not generate a rule, use `text-muted-foreground` on `bg-muted/50` or add NO new tokens — the design-mockup contract wants muted-strong-on-muted 9.5:1, so prefer `text-muted-strong` if the token exists, else record the substitution in the Change Log); (d) the 429 region swap moves focus (role=status + tabIndex); (e) the CSV preview content renders `dir="ltr"` (content direction, not layout — the box geometry stays logical; the mockup pins font-latin for the data); (f) reduced motion: the pending spinner carries `sr-only` text (no animation-only feedback); (g) mobile: `max-w-[calc(100%-2rem)]` + `sm:max-w-[560px]` + `max-h-[calc(100dvh-1rem)] overflow-y-auto` + the preview inset `max-h-40 overflow-y-auto` (short landscape screens).
- **NFR-1 (≤1.5s p95 on 4G)**: modal-open = cache reads + ≤9 sequential direct fetches (bounded by the 1,000-row truncation) with a visible collecting state; the POST = ONE round trip; the download = browser-native. Not CI-testable — recorded as design headroom (Task 7.3).
- **Testing standards**: vitest + jsdom + testing-library (the `saved-search-name-dialog.test.tsx` / `use-reveal.test.tsx` / `results-table.test.tsx` templates); services mocked via `vi.mock('@/lib/api/...')` with `vi.hoisted`; components wrapped in a fresh `QueryClient` per test; `next-intl` mocked to return key literals (assert KEY names + interpolation params — never resolved copy); translation parity via `i18n-shape.test.ts` + `check:i18n`; the http-client `navigator` surrogate is mockable. Gates = `vitest run` + `lint` + `typecheck` + `check:i18n` — run ALL FOUR after EVERY task (4.5 is a FRONTEND story — the gates RUN this time, the 4.4 backend-only exclusion does not apply). Backend gates NOT run (no backend files touched).

## Dev Agent Record

### Agent Model Used

opencode-go/deepseek-v4-flash

### Debug Log References

- RED runs: i18n-shape.test.ts 8 failed (export keys absent); export-keys/export-service tests "no tests" (modules missing); use-export tests collection error (JSX in a `.ts` file — renamed to `.tsx`, the use-reveal pattern); use-export 5 failed (fire-and-forget `create` — assertions moved under `waitFor`); use-export-preview same `.ts`→`.tsx` rename; export-modal tests: 11 failed initially (component bug: `create({... include_unrevealed })` shorthand referenced a non-existent variable — the 4.4 payload key is snake_case, the state is camelCase — fixed with `include_unrevealed: includeUnrevealed`; the balance-after assertion math 15−3=9 was wrong → 12; the preview-row text is one CSV line, not the bare name → regex matchers; the confirm label cannot carry the −N through the key-mock — assertion moved to the `useExport` wiring; `aria-pressed={x || undefined}` renders NO attribute → always-boolean `aria-pressed={x}`; `initialFocus` is NOT a Base UI Dialog prop in this version → removed, Base UI's first-focusable default (the checkbox) satisfies the AC literal; double `renderModal` in the pending test polluted the DOM → single render).
- Base UI Dialog has no `initialFocus` prop (checked `node_modules/@base-ui/react/dialog/root/DialogRoot.d.ts`) — the AC's "initial focus on the first focusable element" is Base UI's built-in behavior (asserted with `waitFor` — jsdom needs a tick for the floating-focus manager).
- `muted-strong` was referenced in RevealControl.tsx:223 but UNDEFINED in the Tailwind v4 theme (grep of globals.css) — added `--color-muted-strong: #334155` to the @theme block (the DESIGN.md-declared `{colors.muted-strong}` token; fixes the pre-existing dead class too). `font-latin` does not exist in this design system — numerals render in the sans stack (AD-8 is about glyphs, not family); not added.
- GREEN gates per task: 596 → 605 → 612 → 620 → 643 → 651 vitest; lint 0; typecheck 0; check:i18n 480×3 → 486×3 (6 header-label keys added mid-task).

### Completion Notes List

- Story 4.5 implemented end-to-end (TDD red→green per task): i18n `export.modal.*` (19 keys) + `export.watermark` + `billing.upgrade_stub` ×3 locales with the [PENDING REVIEW] convention + the shape test (parity, interpolation params, verbatim-copy pins vs EXPERIENCE.md:96/98/L95 + the 4.4 backend messages.py, no-trailing-period rule, pending-review flags); `exportKeys` factory + `ExportService` (strict 10-key `CreateExportResponse` + `CreditBalances`, four typed narrowers + the reveal-service 402 guard reuse); `useExport` (error union narrowing in the hook, in-flight double-submit guard, error cleared at dispatch, `applyConfirmedBalance` + `checklistKeys.all` invalidation + close-then-navigate onSuccess); `useExportPreview` (cache-aware full-set composition — cached pages cost 0 quota, missing pages fetched directly + aborted on close, free-tier first-5 page-1 cap, retry, collector error state); `ExportModal` (the DESIGN.md:333 anatomy — rows / cost breakdown `text-data tabular-nums` Western numerals / balance-after / include-unrevealed checked by default / literal 2-row CSV mini-preview `bg-muted text-muted-strong` with dir=ltr content and the localized watermark rows for free / format toggle CSV+XLSX `aria-pressed` / confirm primary `−{count}`; free-tier cap note + xlsx aria-disabled + upgrade caption + upgrade-stub toasts, NO POST ever; 429 come-back-tomorrow region swap `role="status"`+focus; 402/409/404/400 states; client-side 0-credit guard with the insufficient caption; success → close → `navigator.assign` to the download URL); `ExportToolbar` (visibility rules, both tiers/tabs, aria-disabled while fetching, outside the live region) + SearchPage wiring (nonce exposed from useSearchResults). `--color-muted-strong` token added (DESIGN.md-declared).
- Gates: FE 651 vitest / lint 0 / typecheck 0 / check:i18n 486×3. Backend gates NOT run (frontend-only story — zero backend files touched).

### File List

- `frontend/messages/en.json` (MODIFIED — export.* + billing.upgrade_stub)
- `frontend/messages/fr.json` (MODIFIED — export.* + billing.upgrade_stub)
- `frontend/messages/ar.json` (MODIFIED — export.* + billing.upgrade_stub)
- `frontend/src/__tests__/i18n-shape.test.ts` (MODIFIED — export modal describe block)
- `frontend/src/lib/queryKeys/export.ts` (NEW)
- `frontend/src/lib/api/export-service.ts` (NEW)
- `frontend/src/__tests__/export-keys.test.ts` (NEW)
- `frontend/src/__tests__/export-service.test.ts` (NEW)
- `frontend/src/hooks/useExport.ts` (NEW)
- `frontend/src/__tests__/use-export.test.tsx` (NEW)
- `frontend/src/hooks/useExportPreview.ts` (NEW)
- `frontend/src/__tests__/use-export-preview.test.tsx` (NEW)
- `frontend/src/components/search/ExportModal.tsx` (NEW)
- `frontend/src/__tests__/export-modal.test.tsx` (NEW — 23 tests)
- `frontend/src/components/search/ExportToolbar.tsx` (NEW)
- `frontend/src/__tests__/export-toolbar.test.tsx` (NEW — 7 tests)
- `frontend/src/components/search/SearchPage.tsx` (MODIFIED — ExportToolbar slot + shared page constants)
- `frontend/src/hooks/useSearchResults.ts` (MODIFIED — returns `nonce`)
- `frontend/src/lib/api/search-service.ts` (MODIFIED — SEARCH_PAGE_SIZE / SEARCH_MAX_NAVIGABLE_PAGES exports)
- `frontend/src/__tests__/search-page.test.tsx` (MODIFIED — toolbar integration test)
- `frontend/src/app/globals.css` (MODIFIED — `--color-muted-strong` token)

## Change Log

- 2026-08-07: Story created (ready-for-dev) from the epic 4.5 planning spec (FR-17/18/19/20; UX-DR12/20/21/24). Customization resolution: `resolve_customization.py` FAILED (Python 3.10 — no stdlib `tomllib`; the 4.3/4.4 precedent) → manual base→team→user fallback: base `.agents/skills/bmad-create-story/customize.toml` read; no team override (`_bmad/custom/bmad-create-story.toml` absent) and no user override (absent) → effective workflow = base only (activation steps empty; persistent_facts `file:{project-root}/**/project-context.md` matched NOTHING — no project-context.md in the repo; recorded as unavailable). Config loaded from `_bmad/bmm/config.yaml` (user_name Bensouici Akram; English; planning + implementation artifacts resolved). Inputs loaded: epic 4.5 planning spec (SELECTIVE), epic 4 index, PRD FR-17..20 (`4-features.md#L161-190`), architecture (`docs/ARCHITECTURE-SPINE.md` — AD-18/19/20/21, component tree, search pagination + rate limit), UX (`DESIGN.md#L333` export-modal + tokens + Do's; `EXPERIENCE.md#L95-149` + L126 export modal pattern; mockup `export-modal.html`; `review-accessibility.md`), `deferred-work.md` (3.7 entry (b) — the checklist invalidation obligation; 3.3/3.4 registry-file caveats), the 4.4 story file in FULL (contracts D1–D11 — the POST/DOWNLOAD/checklist contracts this story consumes verbatim), 4.3/4.2 stories (ledger CSV precedent, recovery-stub precedent), FE codebase research (the base-nova dialog.tsx registry — EXISTS, no new dependency; http-client `navigator` surrogate; queryKeys factories; useReveal/useChecklistMutations/useCreditsLedger patterns; SearchPage submitted-state + results-status region; i18n-shape.test.ts precedent; check:i18n parity gate; the text-data/tabular-nums tokens verified in globals.css; search quota increments per request verified at backend views.py:201/235). Web research: SKIPPED (no version-sensitive tech; the only candidate, a dialog lib, ALREADY exists in the registry). Git intelligence: HEAD `93fcd43` (Story 4.4 done — the export API backend this story consumes).
- 2026-08-07: Persona consultations (parallel subagents — the project-recorded practice). Resolved decisions recorded:
  - **Winston (architect)** — Q1 record_ids: page-scoped preview ids + full-set fetch on CONFIRM (quota concern at open); Q2 RHF+zod NOT warranted for the checkbox+format preference picker (AD-18 scoped deviation — local state); Q3 download seam: `navigator.assign` in onSuccess, no "preparing" state; focus-return moot under full-page nav (CORRECTED by Sally — attachment downloads keep the page); Q4 data layer: ExportService + exportKeys.all + useExport, NO mutationKey, plain useMutation + in-flight guard, onSuccess = applyConfirmedBalance + checklistKeys.all invalidation; Q5 error mapping in the hook — `ExportError` union narrowed via service guards, component never imports guards; Q6 the base-nova dialog.tsx is usable as-is (`sm:max-w-[560px]` override), no new dep, aria-labelledby must point at a real DialogTitle; Q7 toolbar: inline between the results-status live region and the chips, both tiers, both tabs, aria-disabled while isFetching, 44px; Q8 fetch ruling: fetch-all on CONFIRM (see divergence (a)).
  - **Sally (UX)** — Q1 anatomy verbatim + token mapping (sm:max-w-[560px], text-data tabular-nums, font-latin numerals, muted/rounded-md inset, confirm primary min-h-11 md:min-h-10; NO DialogDescription; autofocus on confirm; trigger stays mounted); Q2 free-tier states (cap note + FE-owned watermark keys + xlsx aria-disabled tonal + caption via aria-describedby + upgrade-stub toast; confirm NEVER POSTs); Q3 come-back-tomorrow (form region swap, title unchanged, role=status + focus move, no retry); Q4 download UX (close then navigate; no toast/live-region; pending spinner + sr-only + width pin; in-flight click guard); Q5 0-credit (client guard + server 402 state; balance null → "—" + inert confirm); Q6 preview (2 real rows + ellipsis; watermark rows; dir=ltr content; truncate cells; 4 display columns); Q7 balance-after = display_balance − cost; Q8 a11y findings that bind (focus wiring, per-fragment dir, tooltip-touch → caption, no-opacity-fade, checklist announce is the card's job, reduced-motion); Q9 full key list ×3 with flags.
  - **John (PM)** — Q1 preview-then-reconcile sanctioned (total cannot drift — 1 credit per INCLUDED row; only the n/m split can, and it's informational); Q2 free confirm = real focusable aria-disabled-but-actionable button firing the Epic-5 upgrade toast stub (the conversion path must not be a dead end); Q3 copy pinned ×3 with the flagging rule (verbatim shipped = no flag; new = [PENDING REVIEW]); Q4 balance-after + client-side 0-credit guard (a modal has room for a proper state — the 4.2 toast was a space constraint); Q5 full free-tier preview in 4.5 is spec-mandated and a conversion driver (keep the "will look like" future framing — never imply availability today); Q6 cache-aware full-set fetch at OPEN (quota math: ≤10 searches worst case ≈ 10% Starter / 33% free, only on export intent); Q7 429 state: form replaced, title unchanged; Q8 no success copy — native download is the feedback; direct navigation not window.open.
  - **Resolved divergences** (recorded per the practice): (a) fetch timing — Winston fetch-all-on-CONFIRM vs John cache-aware-on-OPEN: RESOLVED cache-aware composition at OPEN (exact preview = the D6 never-surprise contract; quota burn bounded to uncached pages only; free tier pays 0; Winston's NFR-1 concern absorbed via the collecting state + abort + sequential bounded loop); (b) free-tier confirm label — John drop-the-−N vs Sally/mockup-literal: RESOLVED mockup-literal `Confirm export (−{count} credits)` (the mockup IS the free-tier UJ-1 climax; −N = the honest 4.6 price); (c) download seam — Winston assign-in-onSuccess vs Sally close-then-navigate: RESOLVED close-first-then-assign (attachment download keeps the page — the page does NOT unload; close-first preserves Base UI focus-return to the trigger); (d) AD-18 — the literal rule vs Winston's scope reading: RESOLVED scoped deviation recorded in Dev Notes D2 (preference picker, zero validation surface).
- 2026-08-07: Scope decisions recorded (D1–D14): cache-aware full-set composition at modal open (search-cache reads + direct ephemeral fetches for missing pages only; free tier = page 1 only); AD-18 deviation (no RHF/zod); navigator.assign seam with close-first; ExportService + exportKeys + useExport + useExportPreview; the ExportError union mapped in the hook; free-tier UI states render but never POST (4.4 D8); come-back-tomorrow form-region swap (EXPERIENCE.md:98 verbatim); 0-credit client guard + server 402 state (5.x dialogs stay stubs); literal 2-row CSV preview + watermark rows; i18n `export.modal.*` + `export.watermark` + `billing.upgrade_stub` keys ×3 pinned ([PENDING REVIEW] convention); toolbar trigger between the live region and the chips; checklistKeys.all invalidation + applyConfirmedBalance on success (3.7 contract (b) + pill contract); no new dependencies (dialog in registry). Sprint-status 4-5 → ready-for-dev (epic-4 stays in-progress).
- 2026-08-08: Implemented (TDD) — Task 1 i18n ×3 + shape test (parity + interpolation + verbatim pins vs EXPERIENCE.md:96/98/L95 + the 4.4 backend strings + [PENDING REVIEW] flags), Task 2 exportKeys + ExportService + narrowers (strict 10-key contract + CreditBalances reuse), Task 3 useExport (error-union narrowing in the hook, in-flight guard, dispatch-time error clear, applyConfirmedBalance + checklistKeys.all invalidation + onSuccess callback), Task 4 useExportPreview (cache-aware composition: 0-quota cached pages, direct ephemeral fetches with abort, free-tier first-5 cap, retry, error state), Task 5 ExportModal (starter anatomy per DESIGN.md:333 + mockup; free-tier cap/watermark/xlsx-disabled + upgrade-stub toasts with NO POST; 429 come-back-tomorrow region swap; 402/409/404/400 states; client 0-credit guard; success → close → navigator.assign; Base UI owns focus/Esc/restore), Task 6 ExportToolbar + SearchPage wiring (nonce exposure, visibility rules, live-region isolation) + the `--color-muted-strong` token added (DESIGN.md-declared, fixes the pre-existing dead class), Task 7 full regression. Gates: FE 651 vitest / lint 0 / typecheck 0 / check:i18n 486×3. Backend gates NOT run (frontend-only). Status → review (sprint-status 4-5 → review).
- 2026-08-08: Code review (full mode — Blind Hunter + Edge Case Hunter + Acceptance Auditor, parallel subagents). Blind 14 (2 High) + Edge 10 (2 High) + Auditor 6 (all 7 AC blocks SATISFIED, 2 Medium + 4 Low) + 7 task-claim discrepancies → triaged to 18 patch + 2 defer + 6 dismiss; every patch applied (stale-preview POST race closed via setPreview(null) + locked confirm, error-reset on open + 409 retry affordance, honest aria-disabled confirm on collector failure with the D12 amendment, balance-null inert (no false no-credits toast), empty-ids POST lock, dead error_not_found key removed (404→generic per 4.4 D9), insufficient caption aria-describedby wiring, mutation `retry: false` — the double-burn guard the reviewers' global-client blind spot missed, collector id dedupe + total clamp, shared SEARCH_PAGE_SIZE/SEARCH_MAX_NAVIGABLE_PAGES constants, ellipsis truthfulness, preview role=group, unconditional width pin, form-defaults reset on open, `satisfies`-typed contract fixture, 429 focus-move test, toolbar test timing); deferrals recorded (collector-retry quota burn → 4.6 preview-endpoint handoff; focus-return + mobile smoke → jsdom-limits, library-owned); dismissals recorded with rationale (generic retry stays — reopen-reset makes all states escapable; balances guard = 4.2 precedent; header_* keys = verbatim copies of the 4.4-shipped backend labels — no [PENDING REVIEW] needed; toolbar total>0 gating covered by the ids lock; T-2/T-4/T-7 task-claim reconciliations). Gates re-run fully green (FE 657 vitest / lint 0 / typecheck 0 / check:i18n 485×3).
- 2026-08-08: Review resolved — all patch findings applied, gates fully green; Status → done (sprint-status 4-5 → done, epic-4 stays in-progress).

## Review Findings

Code review (full mode — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-08-08). Blind: 2 High + 2 Medium + 10 Low. Edge: 2 High + 4 Medium + 4 Low. Auditor: all 7 AC blocks SATISFIED + 2 Medium + 4 Low + 7 task-claim discrepancies. Triage: 14 patch, 2 defer, 6 dismiss. All patches applied; gates re-run green (FE 657 vitest / lint 0 / typecheck 0 / check:i18n 485×3).

- [x] [Patch][High] Stale-preview POST race (Blind 1 + Edge 1): `useExportPreview` kept the previous collection's preview while re-collecting after a filter change/re-open → the confirm could POST the OLD search's record_ids. Fixed: `setPreview(null)` at the start of every collection effect run (the modal renders "Calculating…" and the confirm is `aria-disabled` while `preview === null`); regression test "clears the stale preview while the new collection is in flight".
- [x] [Patch][High] Mutation error states persisted across modal re-opens; 429 from yesterday blocked today's attempt; 409 had no retry (Blind 3 + Edge 2 + Edge 4). Fixed: `useExport` gained `reset()` (clears error + re-arms the in-flight guard) called on every open; the 409 region now renders the same retry button as generic; test "reset clears the error and re-arms the in-flight guard" + "shows the concurrent state on 409 with a retry affordance".
- [x] [Patch][High] Silent dead confirm when the preview collector failed (Blind 2 + Edge 5 + Auditor H1 — contradicted D12's "confirm STAYS usable"). Fixed: the confirm is `aria-disabled` + tonal while `preview === null`/collecting (the preview-error caption + retry is the affordance — the export stays reachable; a blind POST without ids is impossible). D12 interpretation amended in the Change Log; test rewritten to assert the disabled state + no silent no-op.
- [x] [Patch][Medium] `balance === null` fired the false "No credits remaining" toast (Blind 5 + Edge 10 — D9's "inert until known"). Fixed: `insufficient` (balance known + < cost → no_credits toast) split from `unknownBalance` (inert, no toast); confirm `aria-disabled` in both; test "keeps the confirm inert while the balance is unknown".
- [x] [Patch][Medium] Empty POST when include_unrevealed is off and nothing is revealed (Edge 3): `record_ids: []` → guaranteed 400 loop. Fixed: `ids.length === 0` joins `confirmLocked` (aria-disabled + tonal); `handleRetry` guards the same; test "blocks the confirm ... (no empty POST)".
- [x] [Patch][Medium] Dead `error_not_found` i18n key ×3 (Blind 4 + Auditor H3/T-5): the 4.4 D9 contract folds 404 into `generic`; the key was never rendered. Fixed: key removed from all three message files + the shape test; the stale 5.1 subtask text superseded by D9 (recorded in the Change Log).
- [x] [Patch][Medium] `insufficient` caption not wired via `aria-describedby` (Auditor H2 — D9 contract). Fixed: caption id `export-insufficient-note` + `aria-describedby` on the confirm; test asserts the wiring.
- [x] [Patch][Low] The export mutation could RETRY (TanStack v5 mutation default `retry: 3`) — a lost-response re-POST would DOUBLE-BURN credits (the 4.4 atomic-debit contract). Fixed: `retry: false` on the mutation with a recorded rationale (the global client only disables QUERY retries). Also made the Once-chain tests deterministic.
- [x] [Patch][Low] Duplicate record_ids possible in the collector (Blind 10): order-preserving `seen`-Set dedupe; counts computed after dedupe (matches the 4.4 server dedupe).
- [x] [Patch][Low] Collector trusts cached rows over the server `total` after a server-side shrink (Edge 7): ids/rows clamped to `min(collected, total)` when `total ≤ 1,000`.
- [x] [Patch][Low] Dead `argsRef` (Blind 8 + Auditor H6): removed.
- [x] [Patch][Low] Duplicated `PAGE_SIZE`/`MAX_PAGES` constants (Blind 11): exported `SEARCH_PAGE_SIZE`/`SEARCH_MAX_NAVIGABLE_PAGES` from `search-service.ts`; consumed by `useExportPreview` and `SearchPage` (single source).
- [x] [Patch][Low] Fabricated ellipsis when the set has ≤2 rows (Edge 9 — D6 spirit): rendered only when `totalRows > 2`; test added.
- [x] [Patch][Low] `aria-label` on a role-less preview div not exposed to AT (Edge 8): `role="group"` added.
- [x] [Patch][Low] Confirm width pin only while pending (Blind 13): `min-w-[10rem]` applied unconditionally (the reveal loading-precedent intent).
- [x] [Patch][Low] Form defaults + mutation error not reset across opens (Blind 14 — AC "checked by default"): the on-open effect resets `includeUnrevealed`, `format`, and the mutation error; test "resets the form defaults and the mutation error on every open".
- [x] [Patch][Low] Tautological contract test (Auditor H4): the export-service fixture now `satisfies CreateExportResponse` (compile-time pin) — the runtime keys assertion removed.
- [x] [Patch][Low] 429 focus-move untested (Auditor H5): test "moves focus into the swapped region when the error state changes" added.
- [x] [Patch][Low] Toolbar modal-open test flaky under the full suite (Base UI portal timing): `findByText`; the toolbar `useExport` mock gained `reset`.
- [Deferred] Collector-retry after a search-429 re-burns FR-7 quota (Blind 12): recorded — repeated retries are the user's own action; the main results area already surfaces the search-limit state; a quota-free preview endpoint stays the 4.6 handoff.
- [Deferred] Focus-return-to-trigger assertion (Auditor H5/T-3) + mobile <md viewport smoke test (T-6): Base UI owns focus-restore (no custom code to test) and jsdom has no viewport; recorded — the real-browser behaviors are contract-covered by the library.
- [Dismissed] Generic-state retry loop for 404/400 causes (Blind 6): the reopen-reset patch (above) makes every error state escapable; a network-flake retry is the common case — retry stays.
- [Dismissed] `balances` guard silently swallowing a contract violation (Blind 9): the 4.2 defensive precedent; the server contract is exact-keys-tested backend-side.
- [Dismissed] header_* keys exempted from [PENDING REVIEW] (Blind 7): they are VERBATIM copies of the 4.4-shipped backend `EXPORT_CSV_HEADERS` labels (no new translation risk) — exemption recorded in the Change Log.
- [Dismissed] Toolbar gates on `total > 0` not `results.length > 0` (Edge 6): the transient empty-page-snapshot edge is covered by the `ids.length === 0` confirm lock (no dead button).
- [Dismissed] Subtask T-2 "enabled gating" claim: the hook is call-site-gated by the component guards (handleConfirm); no user-visible gap.
- [Dismissed] Subtask T-4 "autofocus on confirm": Base UI has no `initialFocus` prop (verified in its .d.ts); the AC-literal "first focusable element" (the checkbox) is Base UI's default — recorded in the Debug Log.
- [Dismissed] Success-path "no crash" claim (T-7): covered by the onSuccess wiring test.

### Task-claim reconciliations (recorded per the practice)

- Task 5.1 "404 → error_not_found state": superseded by the 4.4 D9 contract (404 → generic); the dead key was removed.
- Task 3.1 "enabled gating": the hook has no enabled prop — gating lives in the component guards (call-site gating); documented.
- Task 5.1 "initial focus on the confirm (autofocus)": implemented as Base UI's first-focusable default (the AC literal); documented.
- Task 5.1 focus-return + mobile smoke assertions: not testable in jsdom; Base UI-owned / class-based; deferred + documented.
- Dev Note D12 "the confirm STAYS usable": amended — the confirm is honestly `aria-disabled` while the preview is unavailable, with the preview-error retry as the affordance (a blind POST without record_ids is impossible); the export remains reachable after a successful retry.
