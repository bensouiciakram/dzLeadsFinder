---
story_id: 4.6
epic: 4
title: Story 4.6 — Watermarked Export (Free)
Status: done
frs: [FR-19]
ads: [AD-3, AD-4, AD-7, AD-11, AD-19, AD-20, AD-21]
ux_drs: [UX-DR21, UX-DR22]
baseline_commit: 9e9ff1e
---

# Story 4.6: Watermarked Export (Free)

Status: done

## Story

As a **free-tier user trying DZLeads**,
I want **to export up to 5 rows of my search results as a CSV with a visible watermark, seeing exactly what the file will look like before confirming**,
So that **I can test the data quality and see what upgrading removes**.

## Acceptance Criteria

**Given** the free-tier export flow
**When** a free-tier user opens the Export Modal
**Then** the modal explicitly states: "Free tier: export up to 5 rows"
**And** the 5 rows included are the first 5 rows of the current result set (current sort order)
**And** a localized watermark header row and footer row are shown in the CSV mini-preview

**Given** the watermark format
**When** the CSV is generated
**Then** the watermark is literal text rows in the CSV file:
- Header row: "DZLeads Free — upgrade to remove" (localized)
- Footer row: "DZLeads Free — upgrade to remove" (localized)
**And** the 5 data rows sit between the watermark header and footer
**And** the watermark is NOT a visual overlay or diagonal stamp — it is actual CSV content

**Given** the upgrade conversion path
**When** a free user sees the watermark
**Then** the XLSX button is visibly disabled with tooltip: "Upgrade to use Excel export"
**And** both upgrade CTAs in the modal converge to the single Upgrade dialog (Story 5.7)

**Given** the free-tier export modal
**When** it renders
**Then** the "Include rows I have not revealed yet" checkbox exists, checked by default
**And** the credit cost still applies: 5 rows = 5 credits from the free 15

**Given** the free-tier user tries to export more than 5 rows
**When** the result set has >5 rows
**Then** the modal explains that only 5 rows can be exported on the free tier
**And** the preview shows which 5 rows are included

## Tasks / Subtasks

- [x] **Task 1: Backend — tier-gate relaxation + watermark threading** (ACs: free CSV 5-row cap + literal watermark rows; FR-19; xlsx stays starter-only forever — FR-18; the 4.4 D8 split)
  - [x] 1.1 RED: `backend/apps/exports/tests/test_export_api.py` — UPDATE `TestTierGate` (the 4.4 paid-only tests FLIP):
    - `test_free_user_rejected` → REWRITTEN: a free user with 15 credits POSTs csv with 3 ids → **200** (was 403); body EXACT 10 keys with `watermark: true`, `row_count=3`, `revealed_count`/`unrevealed_count` over the DATA set, `credits_cost=3`, `balances` confirmed; one `Export` row `watermark=True`; ledger `export_row_debit −3` with `reference_id == str(export.id)`; `daily_usage.export_rows == 3` (D7 — free exports count).
    - `test_free_user_rejected_xlsx` → **STAYS 403** `starter_only` (free+xlsx forever blocked — FR-18; assert with a GRANTED free user — balance can never bypass the gate).
    - `test_starter_only_message_localized` → payload flips to `format='xlsx'` (the 403 path is now xlsx-only).
    - `test_free_user_malformed_payload_gets_400_not_403` → unchanged (400 precedes the gate).
    - NEW `test_free_user_capped_to_first_five`: free user POSTs **8** ids (granted 15 credits) → 200 `row_count=5`, `credits_cost=5`, usage +5, `rows_json['rows']` has exactly 5 entries = the FIRST 5 ids in PAYLOAD ORDER (the current-sort order the modal sends — server-side slice, never trusts the client; Q2/Q9).
    - NEW `test_free_cap_applies_before_unrevealed_filter`: 8 ids, `include_unrevealed=false`, none revealed → 200 `row_count=0`? NO — `InvalidExportPayloadError` → **400** (the existing no-rows rule, applied AFTER the cap: the cap slices first, then the include filter; a free user with 0 revealed + unchecking include_unrevealed → 400 nothing written — same as starter; assert NO ledger row, NO usage increment).
    - NEW `test_free_user_watermark_frozen_in_snapshot`: after a free POST, `export_row.rows_json['watermark'] == WATERMARK_MESSAGES[locale]` (the frozen STRING, not just the boolean — D3/Q4); `rows_json['rows']` contains ONLY data rows (watermark rows NEVER stored in the snapshot).
    - NEW `test_free_export_at_quota_headroom`: `seed_usage(user, 4998)` + free POST of 3 rows → **429** `export_limit_exceeded` (D7 — no free exemption); ledger unchanged, usage stays 4998 (atomic rollback). `seed_usage(4998)` + free POST 2 rows → 200, usage 5000.
    - NEW `test_free_insufficient_credits`: free user with balance 3 (granted 3) POSTs 5 ids → **402** `insufficient_credits`; nothing written (rollback — the AC's "credits apply").
  - [x] 1.2 RED: `backend/apps/exports/tests/test_generation.py` — UPDATE `TestCsv` + NEW watermark block:
    - NEW `test_watermark_rows_single_cell`: `build_export_csv([...rows...], headers, watermark_text='W')` → `_csv_lines` = `['W', <headers>, <data rows...>, 'W']` — the watermark is ONE single-cell row (no column padding — the mockup literal; RFC-4180 quoting only if the string needs it).
    - NEW `test_watermark_layout`: header watermark row → column header row → N data rows → footer watermark row (the exact file shape the 4.5 preview mirrors).
    - NEW `test_watermark_none_byte_identical`: `build_export_csv(rows, headers)` vs `build_export_csv(rows, headers, None)` → byte-identical (legacy paid output unchanged — zero drift).
    - NEW `test_watermark_string_injected_verbatim`: the passed string appears EXACTLY (em-dash + Arabic) in the output — the builder never localizes, never flags (locale-agnostic — the labels-injected precedent).
    - NEW `test_watermark_not_counted`: 3 data rows + watermark → exactly 5 CSV lines (3 data + 1 header + 1 footer); no counts anywhere in the builder.
  - [x] 1.3 GREEN: `backend/apps/exports/export_service.py` — UPDATE — `build_export_csv(rows, headers, watermark_text: str | None = None) -> bytes`: when `watermark_text` is not None, write the watermark row FIRST, then the localized header row, then the data rows, then the watermark row (literal content rows — single-cell, `writer.writerow([watermark_text])`, RFC-4180 via the csv writer). `None` → the current output byte-for-byte (the 4.4 signature + callers unchanged). The xlsx builder is UNTOUCHED (free+xlsx is unreachable — Q4).
  - [x] 1.4 GREEN: `backend/apps/exports/messages.py` — UPDATE — `WATERMARK_MESSAGES`: strip the literal `[PENDING REVIEW]` marker from the fr/ar VALUES (the marker is a review-process convention, NEVER user-visible — it would render inside the actual CSV file for fr/ar users; the 4.5 shape test pinned it, the Change Log records the correction; the flag survives OUT-OF-BAND in this story's Change Log). Values become: ar `DZLeads Free — قم بالترقية لإزالة العلامة المائية`, fr `DZLeads Free — passez à Starter pour retirer le filigrane`, en unchanged `DZLeads Free — upgrade to remove`.
  - [x] 1.5 GREEN: `backend/apps/exports/services.py` — UPDATE — `create_export(user, payload, watermark: bool = False) -> tuple[Export, int]` (the view passes the tier-derived flag — the service stays tier-agnostic; Q9):
    - The free-tier CAP: when `watermark=True`, `record_ids = validated['record_ids'][:5]` (the FIRST 5 after the existing order-preserving dedupe — payload order == result order == sort order, the modal's collector invariant; the server NEVER trusts a client's 5,000-id list from a free user).
    - The watermark STRING frozen into the snapshot: `rows_json['watermark'] = WATERMARK_MESSAGES[locale]` alongside `headers` (D3 — the download replays byte-for-byte even if the copy edits later; absent key = paid legacy rows replay unchanged).
    - `watermark=watermark` on the `Export.objects.create(...)` call (the boolean column flips).
    - ALL counts (row_count, revealed_count, credits_cost, the quota increment) derive from the INCLUDED DATA set — watermark rows never enter rows_json['rows'] and never touch any count (Q5: `revealed + unrevealed == row_count == credits_cost` over the DATA set).
    - The atomic block (debit → quota check+increment → create) is UNCHANGED in structure (Q6/Q7): 5 data rows = −5 debit, +5 usage; 429/402 roll back everything.
  - [x] 1.6 GREEN: `backend/apps/exports/views.py` — UPDATE — the D8 split lands here:
    - `ExportView.post`: the tier gate becomes FORMAT-AWARE — `if user.tier != 'starter' and payload format == 'xlsx': 403 starter_only` (payload validation STAYS first — 400-before-403). Free+csv passes through to `create_export(user, request.data, watermark=(getattr(user, 'tier', 'free') == 'free'))` (Q9 — the view derives the flag; the service enforces cap+watermark so a direct service call can't bypass).
    - `ExportDownloadView.get`: pass the FROZEN string into the builder — `build_export_csv(rows, headers, export_row.rows_json.get('watermark'))` (absent → None → paid files replay byte-for-byte; Q4). xlsx path unchanged.
  - [x] 1.7 Run BOTH stacks' gates (the 4.6 dual-stack rule): backend `pytest` / `ruff` / `mypy` strict AND frontend `vitest run` / `lint` / `typecheck` / `check:i18n` — ALL green (the FE shape test still pins the OLD marker strings — it goes RED here until Task 3; run it and record the expected failure, then mark 1.7 green ONLY if the sole failures are the Task 3 pins — the project practice of cross-task RED is acceptable; otherwise fix in Task 3).

- [x] **Task 2: Backend — full regression + real-PG16 E2E** (the 4.4 Task 6 precedent)
  - [x] 2.1 Full backend gates: `pytest` (all apps) + `ruff` + `mypy` strict + `manage.py check` — ALL green. Frontend gates also run (dual-stack rule) — green apart from the recorded Task 3 pins.
  - [x] 2.2 Real-stack E2E (docker PG16 — the 4.4 stack): `makemigrations --check` clean (NO new migration — the watermark column exists since 4.4; only the boolean flips and the snapshot gains a key); seed a free user (15 credits, 5+ people, 1 reveal, usage 0) + a starter user; curl/django-test flows:
    - free user POST csv 3 ids → 200, exact 10-key body, `watermark: true`, row_count 3, credits_cost 3, balances 12; psql: exports row `watermark=t`, `rows_json.watermark == 'DZLeads Free — upgrade to remove'` (en), ledger `export_row_debit −3` `reference_id == export id`, `daily_usage.export_rows=3`, cache 12.
    - free user POST csv **8** ids → 200 row_count 5 (cap), usage 8 (3+5), balance 7 (12−5) — D7 quota burn verified.
    - download ×2 → the two bodies are BYTE-IDENTICAL (deterministic replay; watermark rows present: line 1 + last line; 5 data rows between; BOM + CRLF intact).
    - free user POST xlsx → **403** starter_only (even with balance).
    - free user at usage 4998 POST 3 rows → 429; psql: no new ledger row, usage still 4998 (rollback).
    - free user balance 3 POST 5 rows → 402; nothing written.
    - starter user POST csv 3 rows → 200 `watermark: false`; download replays WITHOUT watermark rows; the two downloads byte-identical.
    - starter user POST xlsx → 200 (unchanged contract).
    - E2E rows cleaned (the 4.4 cleanup pattern).
  - [x] 2.3 NFR-1 headroom note: the free POST adds ONE list-slice + the snapshot gains one string — no new queries, no new round trips; the download adds one writerow pair — recorded in Dev Notes.

- [x] **Task 3: Frontend — watermark strings cleaned ×3 + shape test re-pin** (AC: watermark localized; the check:i18n parity gate + the verbatim-pin precedent — Dev Notes D2)
  - [x] 3.1 RED: `frontend/src/__tests__/i18n-shape.test.ts` — UPDATE the `pins the watermark strings to the 4.4 backend messages.py values` test: fr/ar pins re-pinned to the CLEAN strings (marker stripped — matches the backend messages.py change): fr `DZLeads Free — passez à Starter pour retirer le filigrane`, ar `DZLeads Free — قم بالترقية لإزالة العلامة المائية`; en pin unchanged (em-dash U+2014 — byte-verified). Run gates → RED (fr/ar still carry the marker).
  - [x] 3.2 GREEN: `frontend/messages/fr.json` + `frontend/messages/ar.json` — UPDATE — `export.watermark` values: marker stripped (identical to the backend strings — the preview and the file can NEVER disagree; the em-dash U+2014 stays). `frontend/messages/en.json` — UNTOUCHED (verified U+2014).
  - [x] 3.3 Run BOTH stacks' gates — ALL green (backend green since Task 1).

- [x] **Task 4: Frontend — the free-tier confirm flips from upgrade-stub to a REAL export** (ACs: cap note, first-5 preview, credit cost applies, xlsx stays disabled, both CTAs converge to 5.7; the 4.5 D8 split closes)
  - [x] 4.1 RED: `frontend/src/__tests__/export-modal.test.tsx` — UPDATE the `free tier states (4.4 D8: no POST until 4.6)` describe block (rewrite — the 4.6 contract):
    - `states the 5-row cap and shows the watermark rows as literal content` → STAYS (cap note + 2 watermark lines + preview) BUT now asserts the preview shows **ALL 5 rows** (no ellipsis for free — `FREE_PREVIEW` has 5 rows: assert 5 data lines + 2 watermark lines; the ellipsis is starter-only).
    - `disables the xlsx button visibly ... with the upgrade caption` → STAYS (aria-disabled, focusable, caption via aria-describedby).
    - `routes the xlsx click to the upgrade stub toast, never a mutation` → STAYS (the xlsx click keeps `billing.upgrade_stub`; `exportService.create` NOT called — 4.6 leaves the 5.7 stub).
    - `routes the free-tier confirm to the upgrade stub toast and NEVER posts` → REPLACED by `confirms a REAL free export`: click confirm → `exportService.create` called ONCE with `{record_ids: <the 5 preview ids>, format: 'csv', include_unrevealed: true}`; NO upgrade toast; on success → `onOpenChange(false)` + `navigator.assign('/api/export/<id>/download/')` (the existing success wiring).
    - NEW `free-tier pending state`: while in flight, the confirm shows `aria-busy` + spinner + sr-only `common.credits.exporting` (the flipped button is now a real submit — Q3/Q7a).
    - NEW `free-tier insufficient (client guard)`: free user balance 3 (cost 5) → confirm `aria-disabled` + tonal + the `export.modal.insufficient` caption visible + `aria-describedby` wired + click fires `common.credits.no_credits` toast + NO POST (the client guard opens to free — Q3).
    - NEW `free-tier 402`: mocked server 402 → the `error_credits` region replaces the form (role=alert + focus) — the honest server truth for a free user with <5 credits.
    - NEW `free-tier preview shows exactly the 5 rows that will export`: the 5 preview row texts appear; the POST payload ids == the preview ids (never-surprise — D6; Q1).
    - The `export.modal.confirm` label with `−5` (U+2212) asserts stay (truthful now).
  - [x] 4.2 GREEN: `frontend/src/components/search/ExportModal.tsx` — UPDATE:
    - `confirmLocked`: REMOVE `freeTier` from the lock (the free confirm is real; the lock keeps `insufficient || balance === null || preview === null || preview.totalRows === 0 || ids.length === 0 || isPending`).
    - `handleConfirm`: REMOVE the free-tier early return + `billing.upgrade_stub` toast (the free confirm now flows into the real `create({...})`; the upgrade toast survives ONLY on the xlsx click in `handleFormatClick`).
    - The muted-style gate on the confirm: `freeTier ||` removed (an enabled free confirm renders primary).
    - The `insufficient` caption + `aria-describedby` wiring: `!freeTier` gates REMOVED (a broke free user sees the reason — Q3; the `export-insufficient-note` id + the confirm's `aria-describedby` open to both tiers).
    - The free-tier preview rows: render ALL `preview.rows` (remove the `slice(0, 2)` for free; starter keeps 2 + ellipsis — the ellipsis renders only when `totalRows > 2 && !freeTier`; Q1 — the preview IS the file for free, never a lie).
    - The watermark preview lines keep `font-semibold text-danger` (content whose role is to be unmissable — Q5) and `dir="ltr"` content direction stays.
    - NO new copy keys needed: `rows_capped`, `title_free`, `cost_breakdown`, `confirm`, `preview_notice`, `xlsx_upgrade`, `insufficient`, `come_back_tomorrow`, `error_*` all exist ×3 (Q2 — the AC "Free tier: export up to 5 rows" intent is carried by `rows_capped` + the preview; NO new keys, NO new [PENDING REVIEW] flags).
    - The `mutationError === 'starter'` → toast mapping STAYS (server-side fallback — unreachable for free+csv in 4.6 but harmless; Q4).
    - The 429/402/409/400 regions, the 0-credit guard, the success close-then-navigate, the a11y (Base UI owns focus/Esc/restore), `min-h-11 md:min-h-10`, `min-w-[10rem]` — ALL UNCHANGED.
  - [x] 4.3 Run BOTH stacks' gates — ALL green.

- [x] **Task 5: Full dual-stack regression + parity** (NFR-1 note; Dev Notes D1/D2)
  - [x] 5.1 Full FE gates: `vitest run` (full suite) + `lint` + `typecheck` + `check:i18n` — ALL green. Full backend gates: `pytest` (all apps) + `ruff` + `mypy` strict — ALL green. (BOTH stacks — the 4.6 rule.)
  - [x] 5.2 Contract smoke (unit-level): the free POST success fires `applyConfirmedBalance` with the response `balances` (the pill reconciliation); `invalidateQueries({ queryKey: checklistKeys.all })` fires (the 3.7 contract (b) — an export is an export regardless of tier); the free error codes map to the modal's OWN localized states (429 → come-back-tomorrow, 402 → error_credits, 403 → starter toast fallback) — never the server detail verbatim.
  - [x] 5.3 NFR-1 + handoff notes (recorded, not CI-testable): the free POST is ONE round trip + a 5-id slice; the download is ONE query + in-memory regeneration with two extra writerow calls — no NFR-1 impact. Recorded handoffs: the quota-free preview endpoint stays conditional (4.5 review handoff — NOT required); the modal's upgrade/recovery DIALOGS remain 5.7 stubs (the xlsx upgrade toast); the 0-credit `/search` danger banner = 5.x; the 4.3 ledger CSV untouched (CSV-infra consolidation stays deferred); the watermark fr/ar strings flagged [PENDING REVIEW] OUT-OF-BAND (this Change Log) — the marker never ships in runtime strings.

## Dev Notes

- **Source of truth — the planning spec**: `_bmad-output/planning-artifacts/epics/epic-04-reveal-credit-export/story-06-watermarked-export-free.md` (all ACs verbatim; FR-19 only). FR-19 in `4-features.md#L177-183`. UX: `EXPERIENCE.md#L126` (free cap first-by-current-sort + literal watermark preview), `EXPERIENCE.md#L248` (watermark = conversion surface, not punishment; all upgrade entry points converge on the single Upgrade dialog), `EXPERIENCE.md#L281` (UJ-1 climax — 0 revealed + 5 unrevealed = 5 credits, balance-after 9, literal preview, "the data is real"), `EXPERIENCE.md#L282` (edge path — watermark header/footer in the spreadsheet, honest), `DESIGN.md#L333` (export-modal anatomy), `DESIGN.md#L351` (watermark-as-content rule), mockup `export-modal.html` (the free-tier ar pins: 5 data rows between watermark rows, confirm "تأكيد التصدير (−5 أرصدة)"). Architecture: `docs/ARCHITECTURE-SPINE.md` — exports DDL `#L225-235` (watermark BOOLEAN NOT NULL DEFAULT FALSE exists since 4.4 — NO new migration), export gating row `#L82` ("CSV always (free: 5-row cap + watermark), xlsx paid-only"), AD-3/4/7/11 `#L863-882`, AD-19/20/21 `#L329-361`. Spine free-tier: `#L82`.
- **CRITICAL CONTRACTS 4.6 MUST HONOR (verbatim — record in Dev Agent Record):**
  - **4.4 D8 split closes**: 4.6 relaxes CSV for free (5-row cap + watermark) and keeps xlsx starter-only FOREVER (FR-18). The 403 `starter_only` remains ONLY for free+xlsx (the view gate becomes format-aware; the free-tier UI blocks xlsx anyway with the disabled button).
  - **4.4 D3/D9 — deterministic regeneration**: the download regenerates from the FROZEN snapshot — the watermark must be stored. The `watermark` boolean column answers WHETHER; the frozen STRING (`rows_json['watermark'] = WATERMARK_MESSAGES[locale]`, alongside the frozen `headers`) answers WHICH STRING — both are required so a paid export's download never gains watermark rows and a free export's download always replays them byte-for-byte, even if the copy later edits (the 4.4 "snapshot header freeze" precedent). File shape: watermark header row → localized column header row → 5 data rows → watermark footer row; watermark rows are single-cell literal content rows, NEVER overlays, NEVER stored in `rows_json['rows']`.
  - **4.4 D10 — response contract unchanged**: the free export POST returns the SAME exact 10 keys with `watermark: true`, `row_count: 5`, `revealed + unrevealed == row_count == credits_cost == 5` over the DATA set; watermark rows excluded from ALL counts; the pill reconciles via `balances` (`applyConfirmedBalance`).
  - **4.4 D4 — atomic debit unchanged**: ONE transaction; the debit stays the first statement (SERIALIZABLE guard + user-row lock); 5 credits via `debit_export_rows` with `reference_id == export id`; 429/402 roll back everything (no ledger row, no export row, no usage increment).
  - **4.4 D7 — quota counts free exports**: a 5-row free export = +5 usage toward the 5,000/24h cap; no free exemption; headroom semantics unchanged (request exceeding remaining headroom → 429 whole).
  - **The server owns the cap**: `create_export(..., watermark=True)` slices `record_ids[:5]` (first 5 after the order-preserving dedupe) — the client may send up to 5,000 ids but the server NEVER trusts it; payload order == result order == sort order (the modal collector's invariant — `useExportPreview` builds ids in page order, free tier = page 1 only). The view only adds the format gate; the service enforces cap + watermark so a direct service call can't bypass (defense-in-depth).
  - **4.5 handoffs now closing**: the 4.5 free-tier confirm was the upgrade-stub (never POST) — 4.6 makes CSV real; the free-tier `rows_capped`/watermark-preview/xlsx-disabled UI ships as-is (4.5 built it); the 4.5 error_not_found removal, `retry: false` mutation guard, checklist invalidation, `applyConfirmedBalance` all stay.
  - **4.6 leaves standing**: the modal's upgrade/recovery DIALOGS = 5.7 stubs (the xlsx click keeps the `billing.upgrade_stub` toast; the AC's "converge to the single Upgrade dialog" is the 5.7 handoff); CSV-infra consolidation (the 4.3 ledger CSV) untouched; the 0-credit `/search` danger banner = 5.x; the quota-free preview endpoint ONLY if analytics demand (4.5 review handoff — not required).
  - **FE conventions**: AD-19/20/21 — the 4.5 data layer is consumed AS-IS (the free-tier POST reuses `useExport`/`useExportPreview`; free sends `record_ids` = first 5, `format: 'csv'` only); AD-8 Western numerals (`String()` interpolation), AD-9 logical properties, UX-DR21 (44px, aria-disabled-but-focusable tonal — no opacity fade), UX-DR22 (<md); no new dependencies; no global toast (inline regions + the toast-stub precedent only).
  - **i18n**: NO new keys this story (reuse `rows_capped`/`title_free`/`cost_breakdown`/`confirm`/`preview_notice`/`xlsx_upgrade`/`insufficient`/`come_back_tomorrow`/`error_*` — all shipped ×3 in 4.5); the watermark fr/ar strings are CLEANED (marker stripped from backend `WATERMARK_MESSAGES` AND the FE keys TOGETHER — parity is the rule; the em-dash U+2014 is byte-verified identical on all three sides); the review flag survives OUT-OF-BAND in the Change Log. **Marker-scope note (recorded post-review)**: the strict no-marker rule applies to strings that ship inside the user's delivered FILE content (the watermark) — the `[PENDING REVIEW]` marker in the fr/ar `export.modal.*` UI COPY strings remains the 4.5-recorded flagging convention (visible only while native review is pending, enforced by the i18n-shape test flag loop) and is untouched by 4.6.
- **D1 — persona-resolved decisions (the 4.6 record):**
  - Winston Q1/Q9 — gate split: the VIEW owns the format gate (`tier != 'starter' and format == 'xlsx'` → 403); the SERVICE owns cap + watermark (`create_export(user, payload, watermark: bool)` — the view passes `watermark=(tier == 'free')`, tier policy never leaks into the service; the service enforces the 5-slice + frozen string regardless of caller).
  - Winston Q2 — the cap is SERVER-SIDE, first-5 of payload order; no sort param (the payload contract is pinned; adding one breaks the 4.4 D10 exact-keys contract).
  - Winston Q3 — `build_export_csv(rows, headers, watermark_text: str | None = None)`; the string is INJECTED (builder stays locale-agnostic/pure); single-cell rows; `None` = byte-identical legacy output.
  - Winston Q4 — the watermark STRING is frozen into `rows_json` (the boolean alone can't satisfy D3); xlsx untouched (unreachable for free).
  - Winston Q5/Q6/Q7 — counts + quota + debit over the DATA set only; the atomic block structure unchanged; no free exemption (402 honest); no 0-balance free exception.
  - Winston Q8 — out of scope confirmed: no preview endpoint, no ledger-CSV consolidation, 0-credit banner stays 5.x.
  - Winston Q10 — response: `watermark: true` flows through the existing key; no keys added.
  - Sally Q1 — free preview shows ALL 5 rows (the ellipsis is a lie for free — the file contains every shown row); starter keeps 2 + ellipsis; `max-h-40 overflow-y-auto` handles 8 lines.
  - Sally Q2/Q6 — NO new copy keys; the confirm label is now truthful for both tiers; `title_free` stays; no post-confirm feedback (the download IS the deliverable).
  - Sally Q3/Q7 — the lock + muted-style gate drop `freeTier`; the free confirm flows through the insufficient branch (client guard) with the caption + aria-describedby gates OPENING to free; `aria-busy`/spinner/sr-only already reachable; the 402 server path → error_credits region with focus (now reachable by free).
  - Sally Q4 — after the flip, the modal's ONLY upgrade CTA is the xlsx button (keeps the 5.7 stub toast); 429/402/0-credit regions carry NO upgrade CTAs (recovery states; conversion converges via the xlsx button + pill + billing); the `starter` mutationError→toast mapping stays as a fallback.
  - Sally Q5 — watermark preview lines keep `font-semibold text-danger` (content whose role is to be unmissable); the REAL defect was the `[PENDING REVIEW]` marker in runtime strings.
  - John Q1 — the AC stands: 5 rows = 5 credits from the free 15 (a freebie breaks pricing honesty; 15 credits = max 3 free exports — draining to 0 IS the funnel; the xlsx button + insufficient caption keep the upgrade surface visible at 0 balance).
  - John Q2 — reuse `rows_capped` (states the cap AND the selection rule — better than the bare AC literal); no dedicated "Free tier: export up to 5 rows" key.
  - John Q3/Q4/Q5/Q7 — watermark parity byte-verified (em-dash on all sides); free confirm = real POST; xlsx = stub toast; D7 stands (no DoS concern — per-account cap + the 30-search/day limit); no extra success surface.
  - **Resolved divergence (recorded per the practice)**: the `[PENDING REVIEW]` marker in the fr/ar WATERMARK strings — Sally: strip from BOTH sides (it renders inside the actual CSV + the preview for fr/ar users = a shipped defect); John: keep parity. RESOLVED: STRIP FROM BOTH TOGETHER (parity is preserved by cleaning both; the marker must never ship inside the user's FILE content; the fr/ar flag survives in this Change Log as the out-of-band review record; the shape test re-pins to the clean values in Task 3). POST-REVIEW scope note: the marker in the fr/ar UI-copy strings (`export.modal.*`, `STARTER_ONLY_MESSAGES`) is the 4.5-recorded flagging convention and stays (user decision — 2026-08-08).
- **D2 — testing standards**: pytest + `django_db` (the `test_export_api.py` session fixture pattern — `export_session(tier='free')` + the `grant` ledger helper); pure-function tests for the builder (the `test_generation.py` pattern); vitest + jsdom (the `export-modal.test.tsx` template — `stubPreview`/`stubExport` helpers, the `exportService.create` mock, `navigator` surrogate); i18n parity via `i18n-shape.test.ts` + `check:i18n`. **Gates = BOTH STACKS after EVERY task** (4.6 touches backend AND frontend — the 4.4/4.5 one-stack exclusions do NOT apply): backend `pytest` / `ruff` / `mypy` strict; frontend `vitest run` / `lint` / `typecheck` / `check:i18n`. Real-PG16 E2E per the 4.4 Task 6 precedent (docker stack) — Task 2.2.
- **D3 — previous-story intelligence (4.5 → 4.6 handoffs)**: the 4.5 Review Findings recorded the free-tier confirm never POSTs (the 4.4 D8 contract) — 4.6 is that flip; the 4.5 collector-retry deferral explicitly hands "a quota-free preview endpoint" to 4.6 IF analytics demand (NOT required); the 4.5 `FREE_PREVIEW` test fixture + `renderModal({ tier: 'free' })` harness are the flip tests' template; the 4.5 shape test pins the fr/ar watermark strings WITH the marker (Task 3 re-pins); the 4.5 modal's `previewLines` slices 2 rows (Task 4 opens it to 5 for free); the 4.5 `confirmLocked` includes `freeTier` (Task 4 removes it); the 4.5 `handleConfirm` free branch toasts (Task 4 removes it); the 4.5 `handleFormatClick` xlsx→toast STAYS.
- **D4 — git intelligence**: HEAD at creation = `9e9ff1e` (Story 4.5 done — the export modal with render-only free states; baseline_commit for 4.6 is captured by dev-story at implementation start). Recent patterns: TDD red→green per task; exact-keys/verbatim-pin tests; the Change Log as the decision record; sprint-status.yaml is UTF-16 (the 4.4 review lesson — restore encoding after edits); dual-stack gates introduced by the 4.6 dual-stack rule.
- **NFR-1 (≤1.5s p95 on 4G)**: the free POST adds a 5-id slice + one snapshot string — no new queries, no new round trips; the download adds two `writerow` calls — tens of ms; the modal-open preview for free is page-1-only (0 extra fetches when cached — 4.5 collector unchanged). Not CI-testable — recorded as headroom (Task 2.3/5.3).
- **No new dependencies** (the skill halts on unapproved deps): everything is stdlib (csv), existing Django/DRF, and the 4.5-built FE data layer.
- **Web research**: SKIPPED — no version-sensitive technology in this story (stdlib csv, existing DRF views, existing React Query hooks; the 4.4/4.5 precedent).

## Dev Agent Record

### Agent Model Used

opencode-go/deepseek-v4-flash

### Debug Log References

- RED runs: test_generation.py TestCsvWatermark 5 failed (watermark_text param missing); test_export_api.py TestTierGate test_free_user_csv_export_succeeds_with_watermark failed (403 vs 200 — the paid-only gate still stood); i18n-shape watermark pins 1 failed (marker still in fr/ar); export-modal 3 failed (free confirm still toasted, insufficient caption free-gated, free preview still 2 rows + ellipsis).
- GREEN fixes during implementation: test_watermark_rows_never_counted arithmetic (1 wm header + 1 column header + 3 data + 1 wm footer = 6 lines, not 5 — my test math); the modal flip needed FOUR coordinated edits (confirmLocked, handleConfirm early-return removal, the muted-style gate, the insufficient caption + aria-describedby gates); the free preview ellipsis gate `> 2 && !freeTier`.
- E2E (docker PG16 on host port 5433 — the native Windows PG17 service holds 5432; the docker compose postgres was remapped to avoid the conflict; the ALTER USER password fix applied after the first auth failure): the first run had 3 failures — (a) `DailyUsage.objects.filter(...).update(export_rows=4998)` seeded nothing when no usage row existed (get_or_create first — the seeding bug, not the app); (b) starter-file line count off by one (trailing CRLF — `rstrip` before split); (c) check 9 used the free client on the starter export id (404) instead of `client4` + the captured starter csv id. After fixes: 31/31 checks pass (200 exact body + watermark true + ledger/usage/cache; 8-ids cap → 5 first-by-payload-order; download ×2 byte-identical with watermark header/footer + 5 data rows + BOM; free xlsx 403; 429/402 atomic rollbacks; starter files replay without watermark; the frozen-string survives a copy edit via the edited live dict). E2E rows cleaned.
- Full gates after every task (the 4.6 dual-stack rule): backend 642 pytest / ruff 0 / mypy strict 0 / manage.py check clean / makemigrations --check clean (NO new migration — the watermark column exists since 4.4; only the boolean flips and the snapshot gains a key); frontend 657 → 660 vitest / lint 0 / typecheck 0 / check:i18n 485×3.

### Completion Notes List

- Story 4.6 implemented end-to-end (TDD red→green per task): Task 1 backend — the D8 split lands: `ExportView.post` gates format-aware (403 starter_only ONLY for free+xlsx), `create_export(user, payload, watermark=...)` enforces the server-side 5-row cap (`record_ids[:5]` — first-5 of payload order after the dedupe) + freezes the watermark STRING into `rows_json['watermark']` (D3 byte-for-byte), `build_export_csv` gains `watermark_text=None` (single-cell literal rows; None = byte-identical legacy), `WATERMARK_MESSAGES` fr/ar stripped of the literal `[PENDING REVIEW]` marker (it would have rendered inside the actual CSV); counts/quota/debit all over the DATA set (5 data rows = −5 debit, +5 usage, never +7); Task 2 — full backend regression + real-PG16 E2E (31/31 checks: exact 10-key body + watermark true + ledger reference_id, 8→5 cap first-by-payload-order, byte-identical downloads with watermark rows, free xlsx 403 with balance, 429/402 atomic rollback, paid files never gain watermark after a copy edit, starter xlsx unchanged); Task 3 — FE watermark fr/ar cleaned (marker stripped on BOTH sides together — parity preserved; the em-dash U+2014 verified byte-identical) + shape test re-pinned; Task 4 — the modal flip: `confirmLocked` + muted-style gate drop `freeTier`, `handleConfirm` free early-return removed (real POST of the first-5 ids as csv), the insufficient caption + `aria-describedby` open to free (client guard + no_credits toast), the free preview renders ALL 5 rows (ellipsis starter-only — the preview IS the file), xlsx click keeps the 5.7 upgrade-stub toast; Task 5 — full dual-stack regression + parity (FE 660 vitest / lint 0 / typecheck 0 / check:i18n 485×3; backend 642 pytest / ruff 0 / mypy strict 0). NO new i18n keys, NO new dependencies, NO new migrations.

### File List

- `backend/apps/exports/export_service.py` (MODIFIED — build_export_csv watermark_text param)
- `backend/apps/exports/messages.py` (MODIFIED — WATERMARK_MESSAGES fr/ar marker stripped)
- `backend/apps/exports/services.py` (MODIFIED — create_export watermark param: 5-slice cap + frozen string)
- `backend/apps/exports/views.py` (MODIFIED — format-aware tier gate + watermark flag + frozen-string download)
- `backend/apps/exports/tests/test_export_api.py` (MODIFIED — tier-gate flips + 7 new free-tier tests)
- `backend/apps/exports/tests/test_generation.py` (MODIFIED — TestCsvWatermark block, 5 tests)
- `frontend/messages/fr.json` (MODIFIED — export.watermark cleaned)
- `frontend/messages/ar.json` (MODIFIED — export.watermark cleaned)
- `frontend/src/__tests__/i18n-shape.test.ts` (MODIFIED — watermark pins re-pinned)
- `frontend/src/components/search/ExportModal.tsx` (MODIFIED — free-tier confirm flip + 5-row preview)
- `frontend/src/__tests__/export-modal.test.tsx` (MODIFIED — free-tier describe block rewritten, 5-row fixture)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (MODIFIED — 4-6 → review)

## Change Log

- 2026-08-07: Story created (ready-for-dev) from the epic 4.6 planning spec (FR-19 only). Customization resolution: `resolve_customization.py` FAILED (Python 3.10 — no stdlib `tomllib`; the 4.4/4.5 precedent) → manual base→team→user fallback: base `.agents/skills/bmad-create-story/customize.toml` read; no team override (`_bmad/custom/bmad-create-story.toml` absent) and no user override (absent) → effective workflow = base only (activation steps empty; persistent_facts `file:{project-root}/**/project-context.md` matched NOTHING — no project-context.md exists; recorded as unavailable, the 4.4/4.5 precedent). Config loaded from `_bmad/bmm/config.yaml` (user_name Bensouici Akram; English; planning + implementation artifacts resolved). Inputs loaded: epic 4.6 planning spec (SELECTIVE — the 44-line spec, all ACs verbatim), epic 4 index, PRD FR-19 (`4-features.md#L177-183`), architecture (`docs/ARCHITECTURE-SPINE.md` — exports DDL `#L225-235` incl. the watermark column, AD-3/4/7/11/19/20/21), UX (`DESIGN.md#L333/351`, `EXPERIENCE.md#L126/248/281/282`, mockup `export-modal.html`), the 4.5 story file in FULL (contracts, Review Findings — the handoffs 4.6 closes, the free-tier test harness), the 4.4 story file in FULL (D1-D11 — the paid-only gate, the D3/D4/D7/D8/D9/D10 contracts 4.6 consumes), the CURRENT CODE (backend `views.py`/`services.py`/`export_service.py`/`messages.py`/`models.py` + their tests; frontend `ExportModal.tsx`/`useExport.ts`/`useExportPreview.ts`/`export-modal.test.tsx`/`i18n-shape.test.ts` + the message files — byte-verified the U+2014 em-dash parity and the [PENDING REVIEW] markers in the fr/ar watermark strings), sprint-status.yaml (UTF-16 — the 4.4 encoding lesson; 4-6 backlog confirmed; epic-4 in-progress), git (HEAD `9e9ff1e` = Story 4.5 done). Web research: SKIPPED (no version-sensitive tech — stdlib csv, existing DRF/React Query surfaces).
- 2026-08-07: Persona consultations (parallel subagents — the project-recorded practice). Resolved decisions recorded (Dev Notes D1):
  - **Winston (architect)** — Q1 gate split: view owns the format gate (`tier != 'starter' and format == 'xlsx'` → 403), service owns cap+watermark (`create_export(user, payload, watermark: bool)`, tier never leaks in); Q2 cap = server-side first-5 of payload order (payload order == result order == sort order; NO sort param — contract pinned); Q3 `build_export_csv(rows, headers, watermark_text=None)` — injected string, single-cell rows, None = byte-identical legacy; Q4 the watermark STRING freezes into `rows_json` (the boolean alone fails D3); xlsx untouched; Q5/Q6/Q7 counts + quota + debit over the DATA set only, atomic block unchanged, no free exemption, 402 honest; Q8 out-of-scope confirmed (no preview endpoint, no ledger-CSV consolidation, banner stays 5.x); Q9 seam: view passes `watermark=(tier == 'free')`, service enforces (no bypass); Q10 response: `watermark: true` via the existing key.
  - **Sally (UX)** — Q1 free preview shows ALL 5 rows (ellipsis is a lie for free), starter keeps 2+ellipsis; Q2 no new keys, confirm label truthful, no post-confirm feedback; Q3 lock + muted gate drop `freeTier`, insufficient caption + aria-describedby OPEN to free, pending states already reachable; Q4 the only remaining upgrade CTA is the xlsx button (5.7 stub toast stays), 429/402/0-credit regions carry no CTAs, `starter` toast mapping stays as fallback; Q5 watermark lines keep text-danger; the REAL defect = the [PENDING REVIEW] marker in runtime strings; Q7 only the two caption gates + the lock changed; Q8 no layout change.
  - **John (PM)** — Q1 charge the 5 (freebie breaks pricing honesty; 15 credits = max 3 exports; draining to 0 IS the funnel); Q2 reuse `rows_capped` (states cap + selection rule — no new key); Q3 parity byte-verified (em-dash U+2014 on all three sides — NO FE en edit needed; the marker is the only fr/ar delta); Q4 confirmed: free confirm = real POST, xlsx = stub toast, no CTAs on error regions, xlsx button keeps the visible upgrade surface at 0 balance; Q5 D7 stands (per-account cap + 30 searches/day — no DoS surface); Q6 zero new keys → zero new flags; Q7 nothing more (the download IS the deliverable).
  - **Resolved divergence** (recorded per the practice): the `[PENDING REVIEW]` marker in the fr/ar watermark strings (backend `WATERMARK_MESSAGES` + the FE `export.watermark` keys) — Sally: strip from both sides (the marker would render inside the actual CSV file + the preview for fr/ar users — a shipped defect); John: keep parity (strip later on both sides together). RESOLVED: strip from BOTH together NOW — parity is preserved by cleaning both; the marker must never ship inside the user's FILE content; the fr/ar watermark flag survives OUT-OF-BAND in this Change Log; the i18n-shape test re-pins in Task 3. POST-REVIEW scope note: the marker in the fr/ar UI-copy strings (`export.modal.*`, `STARTER_ONLY_MESSAGES`) stays — the 4.5-recorded flagging convention, user decision (2026-08-08); only the user-delivered FILE string (watermark) is marker-free.
- 2026-08-07: Scope decisions recorded (Dev Notes D1-D4): the D8 split lands in the view (format-aware gate); the cap + watermark live in `create_export(watermark=True)` (5-slice + frozen string in `rows_json`); `build_export_csv` gains `watermark_text=None` (None = byte-identical); counts/quota/debit over the DATA set only; NO new migration (the watermark column exists since 4.4); NO new i18n keys; the free-tier confirm flip + insufficient-caption opening in the modal; both stacks' gates after every task (the 4.6 dual-stack rule); real-PG16 E2E (Task 2.2). Sprint-status 4-6 → ready-for-dev (epic-4 stays in-progress).
- 2026-08-08: Implemented (TDD) — Task 1 backend (format-aware tier gate in views.py; `create_export(watermark=...)` with the server-side 5-slice cap + the watermark string frozen into `rows_json`; `build_export_csv(watermark_text=None)` single-cell literal rows; `WATERMARK_MESSAGES` fr/ar cleaned of the runtime `[PENDING REVIEW]` marker — it would render inside the actual CSV; counts/quota/debit over the DATA set only), Task 2 full backend regression + real-PG16 E2E (31/31 checks — exact 10-key body, 8→5 cap first-by-payload-order, byte-identical downloads with watermark header/footer + 5 data rows, free xlsx 403 with balance, 429/402 atomic rollbacks, paid downloads never gain watermark after a copy edit, starter xlsx unchanged; E2E rows cleaned), Task 3 FE watermark strings cleaned ×3 + shape test re-pin (parity preserved — both sides stripped together; em-dash U+2014 byte-verified), Task 4 the modal flip (confirmLocked + muted gate drop freeTier, free confirm POSTs the first-5 ids as csv, insufficient caption + aria-describedby open to free, all-5-rows preview with starter-only ellipsis, xlsx click keeps the 5.7 upgrade-stub toast), Task 5 full dual-stack regression. Gates: FE 660 vitest / lint 0 / typecheck 0 / check:i18n 485×3; backend 642 pytest / ruff 0 / mypy strict 0; `manage.py check` + `makemigrations --check` clean (no new migration). Status → review (sprint-status 4-6 → review, epic-4 stays in-progress).
- 2026-08-08: Code review (full mode — Blind Hunter + Edge Case Hunter + Acceptance Auditor, parallel subagents). Blind 15 (1 High) + Edge 4 + Auditor 5/5 ACs SATISFIED + 5/5 contracts HONORED + 3 task-claim discrepancies → triaged to 11 patch + 4 defer + 3 dismiss + 1 user-decided; every patch applied (watermark cell sanitized via `_cell` — the Excel-safety contract holds for every row, `watermark + xlsx` service-level guard — no inconsistent watermarked-xlsx state, free download byte-for-byte contract test, vacuous xlsx-click assertion fixed to assert the hook create, free preview filtered to the included rows when include_unrevealed is off + lock test, cap-order test pins all 5 rows, headroom-success body contract, paid watermark-None snapshot pin, RFC-4180 + empty-string watermark tests, shape-test title); the `[PENDING REVIEW]` marker question in the fr/ar UI-copy strings was the user-decided finding (keep the 4.5 flagging convention for UI copy; the strict no-marker rule applies to user-delivered FILE content only — the story's overbroad claim corrected); deferrals recorded (balance-null inert precedent, cross-stack cap triplication, starter-toast fallback test, hook-level pill reconciliation, the −5 label test impossibility, insufficient-caption remedy copy = 5.x); dismissals recorded (UTF-16 sprint-status binary diff, free 400 nothing-written spec behavior, text-danger watermark styling). Gates re-run fully green (backend 646 pytest / ruff 0 / mypy strict 0; FE 662 vitest / lint 0 / typecheck 0 / check:i18n 485×3).
- 2026-08-08: Review resolved — all patch findings applied, gates fully green; Status → done (sprint-status 4-6 → done, epic-4 stays in-progress). **Epic-4 retrospective becomes eligible** — 4.6 is the LAST story in Epic 4 (all 4-1..4-6 done); the `epic-4-retrospective: optional` entry in sprint-status.yaml can be run per the retrospective skill.

## Review Findings

Code review (full mode — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-08-08). Blind: 15 (1 High — the watermark sanitization gap; the rest Medium/Low) + Edge: 4 + Auditor: 5/5 ACs SATISFIED, 5/5 contracts HONORED, 3 task-claim discrepancies. Triage: 11 patch, 4 defer, 3 dismiss, 1 user-decided. All patches applied; gates re-run green (backend 646 pytest / ruff 0 / mypy strict 0; FE 662 vitest / lint 0 / typecheck 0 / check:i18n 485×3).

- [x] [Patch][High] Watermark rows bypassed the CSV sanitizer (Blind 8 + Edge 3): `writer.writerow([watermark_text])` skipped `_cell`/`_sanitize_text` — a crafted or copy-edited watermark could carry C0 control chars / a formula-trigger prefix into the file. Fixed: the watermark cell routes through `_cell(watermark_text)` (the Excel-safety contract holds for EVERY row); tests added (`test_watermark_cell_sanitized_like_data_cells`).
- [x] [Patch][Medium] `watermark=True` + `format='xlsx'` via a direct service call produced an inconsistent state (billed/stored watermarked, file unwatermarked — Blind 4 + Edge 1, contradicting the story's "the service enforces cap + watermark" claim). Fixed: `create_export` raises `InvalidExportPayloadError('watermark requires csv format.')` (400) — the view still gates free+xlsx first, the service no longer trusts the caller.
- [x] [Patch][Medium] Free-tier download contract untested at unit level (Blind 7): the byte-for-byte watermark replay rested only on the manual E2E. Fixed: `test_free_export_download_watermark_contract` (POST 5 ids → download ×2 byte-identical, BOM, watermark header line 1 + footer line 8, 5 data rows between, exact 8 lines).
- [x] [Patch][Medium] Vacuous xlsx-click assertion (Blind 3): `expect(exportService.create).not.toHaveBeenCalled()` could never fail — the component calls the hook's `create`. Fixed: the test now asserts the mocked hook `create` is not called.
- [x] [Patch][Medium] Free preview lied when `include_unrevealed=false` (Blind 5): all 5 rows rendered while the file contained only the revealed subset (preview-IS-the-file broken in that state). Fixed: the free preview body filters to the included rows (`includeUnrevealed || row.revealed`); new tests (`filters the free preview to the included rows when include_unrevealed is off`, `locks the free confirm when include_unrevealed is off and nothing is revealed`).
- [x] [Patch][Low] `test_free_user_capped_to_first_five` pinned only the first snapshot row (Blind 10): now asserts the full 5-row payload-order list (['Karim Benali', 'Row 0'..'Row 3']).
- [x] [Patch][Low] `test_free_export_fits_quota_headroom` asserted only the usage counter (Blind 12): now also asserts the 200 body (watermark true, row_count 2, credits 2, balances 13), the export row, the −2 ledger row, and the cache.
- [x] [Patch][Low] Paid-side frozen-watermark-null unpinned (Auditor 3): the starter happy path now asserts `rows_json['watermark'] is None`.
- [x] [Patch][Low] RFC-4180 quoting of the watermark cell untested (Blind 9): `test_watermark_cell_rfc4180_quoted` (comma+quote string → quoted cell); `test_watermark_empty_string_means_no_watermark` ('' == None — no blank rows).
- [x] [Patch][Low] Stale shape-test title (Blind 14): reworded to `pins the watermark strings to the backend messages.py values (marker-free)`.
- [x] [Patch][Low] Story overbroad marker claim (Blind 1/2 + Auditor 5 + user decision): the strict no-marker rule applies to FILE content (watermark); the fr/ar `export.modal.*` UI-copy + `STARTER_ONLY_MESSAGES` markers stay per the 4.5-recorded flagging convention. The story's Dev Notes + Change Log wording corrected (the claim "the marker must never be a runtime string" narrowed to "never in user-delivered FILE content"); the i18n-shape test's marker-flag loop untouched.
- [Deferred] Free user with `balance === null` gets an inert confirm with no caption/toast (Edge 4): the 4.5 D9 contract — inert until known, no feedback (same as starter; dismissed in 4.5 review for starter).
- [Deferred] The 5-row cap is triplicated across stacks (Blind 6 — backend `_FREE_EXPORT_CAP`, FE `Math.min(5, collected)`, the ×3 copy): cross-stack constants aren't shareable; the AC literal pins 5; both layers are tested (server re-slice + FE collector cap + the copy shape test).
- [Deferred] `mutationError === 'starter'` toast fallback untested (Auditor 2): unreachable for free+csv in 4.6; the xlsx-click toast path is tested.
- [Deferred] Pill reconciliation on the free success path asserted only at the shared-hook level (Auditor 4): `useExport.onSuccess` is tier-agnostic (applyConfirmedBalance + checklistKeys.all invalidation, pinned in use-export.test.tsx); the modal's free success test asserts close + navigate.
- [Deferred] Story-claim "−5 (U+2212) assert stays" never existed as a test (Auditor 1): the key-mock can't carry the −N through the label (the 4.5-recorded lesson); the U+2212 glyph is byte-verified in all three message files + the `{count}` interpolation pin.
- [Deferred] `insufficient` caption copy "top up or upgrade" for free users (Blind 15): "top up" lands in 5.x (credit packs); the caption names both paths and one is true for free (upgrade); 5.x owns the remedy copy — no 4.6 key change (recorded scope).
- [Dismissed] sprint-status.yaml binary diff not reviewable (Blind 11): UTF-16 by design (the 4.4 review lesson); the status flip verified separately via Select-String.
- [Dismissed] Free user + include_unrevealed off + 0 revealed → 400 nothing written (Auditor 6): spec-mandated honest behavior (the cap-then-filter-then-no-rows rule), tested.
- [Dismissed] Watermark preview rows styled `font-semibold text-danger` (Sally Q5 in-scope decision): content whose UI role is to be unmissable — presentation ≠ content substitution; the string itself is verbatim.
