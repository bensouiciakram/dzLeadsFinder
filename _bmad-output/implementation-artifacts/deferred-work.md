# Deferred Work

## Deferred from: code review of story-5.5 (2026-08-10)

- **Cancelled-row state after period end** — a cancelled subscription is never expired (the expiry task touches only active/failed_renewal rows), so the Plan Card shows "Cancelled — access until {past date}" + a stale "Credits left this cycle" forever, and the leftover paid-for subscription-pool credits remain spendable by credit-gated surfaces. The post-period cancelled-row state machine (expire + zero the pool + card state) is a 5.7 product decision — the banner/chip story owns subscription-state semantics. [backend/apps/billing/tasks.py (expire_failed_renewals), frontend/src/components/billing/PlanCard.tsx]
- **AR credit noun divergence** — billing.packs.unit_price AR uses 'ائتمان' (Sally's verbatim D24 contract) while every other AR credit reference in the app uses 'نقطة'; the 5.5 trilingual pin locks the divergence into permanence. Noun consistency is a pre-launch i18n-pass decision (same pass as the AR receipt-subject numerals). [frontend/messages/ar.json, frontend/src/__tests__/billing-messages.test.ts]

## Deferred from: code review of story-5.4 (2026-08-10)

- **Failed-charge user notification** Ã¢â‚¬â€ a charged-but-ungrantable payment (off-table pack amount, anonymised user, unknown type) settles `failed` with only an ERROR log: the customer paid and gets no credits, no receipt, and no failure email; the only recovery is manual ops (refund). USER DECISION 2026-08-10: defer to 5.5 Ã¢â‚¬â€ the refund/DangerZone story owns the failure surface (consistent with the 5.3 precedent for subscription rows). [backend/apps/billing/tasks.py]
- **AR pack receipt subjects embed bare Latin numerals in RTL prose** Ã¢â‚¬â€ `PACK_RECEIPT_SUBJECTS['ar']` has the numeral naked inside RTL text (subject lines are plain text Ã¢â‚¬â€ `<bdi>` cannot apply; the 5.3-era subscription subjects have the same structural hazard). Client-dependent rendering; low impact. Deferred to the pre-launch i18n pass. [backend/tasks/email_tasks.py]
- **Re-enqueued receipts re-read the row at run time** Ã¢â‚¬â€ a row edited later (5.5 refunds adjusting amount/credits) would re-send a receipt whose copy no longer matches the original purchase; the unknown-amount fallback can pair an EN/500 subject with an Arabic body. Rows are edited only by ops; the marker is the guard. [backend/tasks/email_tasks.py]

## Deferred from: code review of story-5.3 (2026-08-10)

- **FREE_SIGNUP unlocked write races the grant's absolute cache set** Ã¢â‚¬â€ the 2.2 verify-email path (`apps/accounts/views/auth.py`) writes `user.credits_balance += 15` with no row lock and computes its ledger `balance_after` from a separate aggregate read; interleaved with a 5.3 grant (which sets the cache to the in-transaction ledger total under the user-row lock) the cache can end 15 off the ledger (visible via /api/auth/me/). PRE-EXISTING 2.2 code Ã¢â‚¬â€ recommendation: lock the user row in the verify path (or switch the verify grant to the locked pattern). [backend/apps/accounts/views/auth.py]
- **Webhook body cap bypassable via chunked/missing Content-Length** Ã¢â‚¬â€ `MAX_WEBHOOK_BYTES` pre-checks only the DECLARED header; a chunked request reads `request.body` unbounded (Django's DATA_UPLOAD_MAX_MEMORY_SIZE does not bound raw bodies). Pre-existing 5.2 residual; real fix = proxy-level `client_max_body_size` (nginx) before the app. [backend/apps/billing/webhooks.py]
- ~~**Receipt delivery is single-shot** Ã¢â‚¬â€ (a) `send_payment_receipt` is not idempotent: an ambiguous SMTP failure (server accepted the DATA) triggers its 1 autoretry Ã¢â€ â€™ possible duplicate; (b) exhausted retries Ã¢â€ â€™ no re-send path; (c) broker-down at the grant's commit Ã¢â€ â€™ the on_commit enqueue fails (P7 ERROR log) and the retry path early-returns on the status check, so the receipt never fires and nothing rescues a `succeeded` row. A receipt-resend sweep for succeeded-no-receipt rows + a dedupe key is a 5.4+ deliverable. [backend/apps/billing/tasks.py, backend/tasks/email_tasks.py]~~ Ã¢â‚¬â€ **RESOLVED by 5.4**: `receipt_sent_at` marker (before-send, cleared on failure, terminal on unreceiptable rows) + `resend_missing_receipts` sweep (hourly beat, type-dispatched, 3-retry, per-row resilient, backfilled migration 0004).

## Deferred from: code review of story-5.2 (2026-08-09)

- **Chargily webhook envelope conformance unverified** Ã¢â‚¬â€ docs.chargily.com was unreachable from the dev environment at pin time (5.2 D11); the executable pin tests are self-consistent, not docs-conformant. PRE-PROD GATE: verify the X-Signature scheme (header name, digest encoding, raw-body semantics) and the checkout API payload shape against the live Chargily docs before enabling webhooks in production; the adapter is isolated in `backend/apps/billing/chargily.py` for a single-file correction. [backend/apps/billing/chargily.py]
- **Shaped-metadata `checkout_id` semantics** Ã¢â‚¬â€ the webhook's shaped metadata `checkout_id` stores `data.id` (the same value normalized into `chargily_event_id`); the REAL Chargily checkout id (returned by create-checkout) is the 5.6 polling key. The 5.6 status-polling lookup must not rely on the metadata `checkout_id` field until the payload semantics are pinned at the docs gate. [backend/apps/billing/webhooks.py]
- **Replay-after-purge re-grant window** Ã¢â‚¬â€ the 90-day retention purge (5.1 D2) deletes the AD-5 guard row (`payment_transactions`); a replayed webhook event id becomes insertable again Ã¢â€ â€™ double grant. Needs a tombstone/retention design (5.3+/ops). [backend/apps/billing/webhooks.py, backend/tasks/maintenance_tasks.py]

## Deferred from: code review of story 5.1 (2026-08-09)

- **Tier split-brain (`User.tier` vs `Subscription.tier`)**: entitlement reads `User.tier` (`search/quota.py`), the new `subscriptions.tier` is unlinked Ã¢â‚¬â€ an ACTIVE starter subscription can coexist with `user.tier='free'` and vice versa. Deferred to 5.3 (grant flow owns the `user.tier='starter'` write) / 5.7 (cancel sync); cross-table invariants need triggers, not constraints. [backend/apps/billing/models.py]
- **No index on `payment_transactions.status`**: the pending-reconciliation sweep (spine Celery flow) will full-scan an append-only table; add a (partial) index when the 5.3 webhook reconciliation query lands. [backend/apps/billing/models.py]
- **PG CI job excludes the SERIALIZABLE service suites (credits/exports)**: the documented SET-first composition contract (5.1 D13) Ã¢â‚¬â€ pytest-django's per-test atomic wrapper makes `SET TRANSACTION ISOLATION LEVEL` unrunable on PG under pytest; the 4.1 E2E-race is their proof vehicle. 5.3 webhook-concurrency tests should extend the PG job scope. [.github/workflows/ci.yml]
- **No true concurrent-transaction PG test in CI**: ON CONFLICT/unique proofs exist (5.1 E2E 10/10 + billing tests on PG in CI); the concurrent double-webhook race is a 5.3 deliverable (the 4.1 two-process E2E-race pattern). [.github/workflows/ci.yml]
- **`chargily_metadata` persists raw webhook payloads unbounded** (PII scope creep, no size cap / redaction / retention story): payload shaping + redaction + retention is a 5.2 webhook-design decision (epic-4 retro action item #2 spike). [backend/apps/billing/models.py]
- **Event-id case normalization**: a replay differing only in letter case bypasses the UNIQUE guard; the 5.3 webhook handler must normalize (`strip().lower()`) on write. [backend/apps/billing/models.py]
- **`bulk_create(ignore_conflicts=True)` diverges by backend on non-unique violations** (SQLite `INSERT OR IGNORE` silently drops, PG raises) and returned-pk insertion detection differs Ã¢â‚¬â€ the 5.3 webhook handler must validate rows (full_clean/choice membership) and re-query existence instead of trusting returned pks. [backend/apps/billing/tests/test_idempotency.py]
- **Credits/status coupling** (`credits_granted` set while `pending`/`failed`/`refunded`, `reconciled_at` free-form): DB checks deferred until the 5.3 webhook write order (INSERT pending Ã¢â€ â€™ grant Ã¢â€ â€™ succeeded) is pinned; adding them now risks fighting the flow. [backend/apps/billing/models.py]

## RESOLVED by the epic-4 deferred-work pass (2026-08-09)

- ~~**Offline/hung requests leave the Reveal button spinning forever**~~ Ã¢â‚¬â€ **RESOLVED**: `HttpClient` now carries a per-instance `AbortController` (aborted on the window `offline` event, recreated on `online`, merged with caller signals via `AbortSignal.any`) Ã¢â‚¬â€ in-flight requests reject instantly with `CanceledError` instead of hanging until the 20s timeout; requests started while offline fail fast the same way (the failure window is exactly the offline window). `RevealControl.handleClick` and `useExport.create` fail fast when `navigator.onLine === false` (failure toast / generic error state, no POST, no spinner). Tests: http-client offline/online/fail-fast suites, reveal click-while-offline, use-export offline. [frontend/src/lib/api/http-client.ts, frontend/src/components/search/RevealControl.tsx, frontend/src/components/search/ExportModal.tsx, frontend/src/hooks/useExport.ts]
- ~~**Ledger table columns run together ("Balance afterReference")**~~ Ã¢â‚¬â€ **RESOLVED**: the /credits ledger table is now `table-fixed` with explicit column widths (Type 30% / Amount 12% / Date 22% / Balance after 14% / Reference 22%), `px-3` horizontal padding on every th/td, `whitespace-nowrap` on the numeric cells, and `truncate` + `title` on the reference cell (full UUID on hover). Smoke assert in credits-page.test.tsx. [frontend/src/components/credits/CreditsPage.tsx]

## RESOLVED by the epic-4 deferred-work pass (2026-08-09) Ã¢â‚¬â€ backend batch

- ~~**Format dispatch duplicated across two files**~~ Ã¢â‚¬â€ **RESOLVED**: `EXPORT_FORMATS` spec table (builder + mime + `supports_watermark`) + `build_export_file(format, rows, headers, watermark_text=None)` + `export_mime(format)` in `export_service.py`; `create_export` and `ExportDownloadView` collapse to single lookups; the watermark passes only to csv (an inconsistent watermarked-xlsx is impossible). Registry + dispatch + mime tests added. [backend/apps/exports/export_service.py, backend/apps/exports/services.py, backend/apps/exports/views.py]
- ~~**_locale/_LOCALES duplicated 4x and already diverged**~~ Ã¢â‚¬â€ **RESOLVED**: `User.effective_locale` property (guarded against `LOCALE_CHOICES`, `'en'` fallback) replaces the duplicated `_locale()`/`_LOCALES` in credits/search/exports views and the `EXPORT_CSV_HEADERS`-keyed variant in exports/services.py. Property tests added (canonical passthrough Ãƒâ€”3, unknown + empty Ã¢â€ â€™ en). [backend/apps/accounts/models.py, backend/apps/{credits,search,exports}/views.py, backend/apps/exports/services.py]
- ~~**CSV formula-injection guard misses tab/space-prefixed triggers**~~ Ã¢â‚¬â€ **RESOLVED**: `_sanitize_text` now checks the `lstrip`'ed value (`" =cmd"`, `\t=`, `\r+` variants neutralized; leading-space plain cells untouched). Tests added. [backend/apps/exports/export_service.py]
- ~~**xlsx `<t>` cells lack `xml:space="preserve"`**~~ Ã¢â‚¬â€ **RESOLVED**: `_inline_t` emits `xml:space="preserve"` when the text has leading/trailing whitespace or newlines (multi-line addresses no longer collapse on open). Tests added. [backend/apps/exports/export_service.py]

## RESOLVED by the epic-4 deferred-work pass (2026-08-09) Ã¢â‚¬â€ contracts verified

- ~~**SERIALIZABLE guard breaks under composition**~~ Ã¢â‚¬â€ **RESOLVED (verified)**: the 4.2/4.4 views call `reveal_contact`/`create_export` directly with no outer `transaction.atomic()` (re-verified in the current code); the contract stands as documented. [backend/apps/credits/services.py]
- ~~**15-credit-banner handoff**~~ Ã¢â‚¬â€ **RESOLVED by 4.3**: `CreditsWelcomeBanner` renders immediately above the checklist card (DOM-order pinned). [frontend/src/components/search/SearchPage.tsx]
- ~~**Step-2/step-3 Epic-4 event contract**~~ Ã¢â‚¬â€ **RESOLVED**: backend `ChecklistView._state` now uses EXISTS on `reveals`/`exports` (search/views.py:315-319); the 4.2 reveal + 4.5/4.6 export mutations invalidate `checklistKeys.all` on success. [backend/apps/search/views.py, frontend/src/hooks/useReveal.ts, useExport.ts]

## Deferred from: code review of story 4.4 (2026-08-08) Ã¢â‚¬â€ ALL RESOLVED by the epic-4 deferred-work pass (2026-08-09)

- ~~**Format dispatch duplicated across two files**: the csv/xlsx branch lives in `services.py` (generation pre-flight) AND `views.py` (download regeneration) Ã¢â‚¬â€ a new format means touching both plus the TextChoices/`_FORMATS`/CHECK constraint/MIME. Pending (user-decided, after the 4.5/4.6 review): a lightweight registry Ã¢â‚¬â€ `EXPORT_FORMATS` spec table (`builder` + mime + `supports_watermark`) with a single `build_export_file(format, rows, headers, watermark_text=None)` + `export_mime(format)` in `export_service.py`; both call sites collapse to one lookup. The watermark metadata matters: `build_export_csv` alone takes `watermark_text` (4.6), so a plain callable dict would break the xlsx call. Full strategy-pattern classes rejected (YAGNI until a third format lands). [backend/apps/exports/export_service.py, backend/apps/exports/services.py, backend/apps/exports/views.py]~~
- ~~**_locale/_LOCALES duplicated 4x and already diverged**: identical `_locale(user)` + `_LOCALES = frozenset({'ar','fr','en'})` copies in `credits/views.py`, `search/views.py`, `exports/views.py`, plus a variant in `exports/services.py` (guards against `EXPORT_CSV_HEADERS` keys instead of the canonical set Ã¢â‚¬â€ direct attribute access, no getattr guard). Pending: an `effective_locale` property on the accounts `User` model (checks `dict(LOCALE_CHOICES)`, falls back `'en'`; no migration Ã¢â‚¬â€ property, not a field), replacing all four call sites. [backend/apps/accounts/models.py, backend/apps/{credits,search,exports}/views.py, backend/apps/exports/services.py]~~
- ~~**CSV formula-injection guard misses tab/space-prefixed triggers**: `_FORMULA_TRIGGER_RE = ^[=+\-@]` neutralizes only cells STARTING with a trigger char; OWASP also lists `\t`/`\r`-prefixed and leading-whitespace-before-trigger variants (`" =cmd"`), which Excel may still evaluate. Pending (Low): strip leading whitespace before the trigger check and/or add `\t\r` to the regex. [backend/apps/exports/export_service.py]~~
- ~~**xlsx `<t>` cells lack `xml:space="preserve"`**: multi-line addresses / leading-trailing space values may be whitespace-collapsed by Excel on open (data fidelity, not a repair risk). Pending (Low): emit `xml:space="preserve"` when the text has leading/trailing whitespace or newlines. [backend/apps/exports/export_service.py]~~

## Deferred from: code review of story 4.1 (2026-08-07)

- ~~**SERIALIZABLE guard breaks under composition**: `reveal_contact` runs `SET TRANSACTION ISOLATION LEVEL SERIALIZABLE` as the first statement inside its `transaction.atomic()` Ã¢â‚¬â€ PG raises `ProgrammingError` if a future caller (e.g. the 4.2 reveal view) wraps the call in an OUTER atomic block or executes any query before it. Contract: call `reveal_contact` directly, never inside an outer `transaction.atomic()`. The per-user `select_for_update` lock (added in review) carries the concurrency correctness on any isolation level, so the guard is defense-in-depth Ã¢â‚¬â€ the composition hazard is a loud error, not silent corruption. [backend/apps/credits/services.py]~~ Ã¢â‚¬â€ **RESOLVED (verified 2026-08-09)**: the 4.2/4.4 views call the services directly with no outer atomic block.

## Deferred from: code review of story-3.7-checklist-card (2026-08-06)

- ~~**15-credit-banner handoff**: the checklist card AC says "appears below the 15-credit banner", but the banner does not exist yet (credit surfaces land in Epic 4). 3.7 renders the card as the FIRST child of `#results`. Epic 4 must render the 15-credit welcome banner immediately ABOVE the card (the banner slots before `#results`'s first child Ã¢â‚¬â€ the card's DOM-order test pins the position). [frontend/src/components/search/SearchPage.tsx, frontend/src/components/search/ChecklistCard.tsx]~~ Ã¢â‚¬â€ **RESOLVED by 4.3** (`CreditsWelcomeBanner` above the card).
- ~~**Step-2/step-3 Epic-4 event contract**: `ChecklistView` GET returns `step_reveal`/`step_export` hard-coded `false` (the `reveals`/`exports` tables do not exist until Epic 4). Contract for 4.x: (a) backend Ã¢â‚¬â€ extend `ChecklistView._state` with `EXISTS` clauses on `reveals`/`exports` replacing the literal False (documented in views.py:296-299; the client contract does not change); (b) frontend Ã¢â‚¬â€ the 4-2 reveal and 4-5 export mutations MUST `invalidateQueries({ queryKey: checklistKeys.all })` on success, or the card's live check-off for steps 2/3 stops working. [backend/apps/search/views.py, frontend/src/hooks/useChecklistMutations.ts]~~ Ã¢â‚¬â€ **RESOLVED (verified 2026-08-09)**: EXISTS clauses live at search/views.py:315-319; reveal/export mutations invalidate `checklistKeys.all`.

## RESOLVED by Story 3.5 (2026-08-05)

- ~~No timeout/abort on search requests~~ Ã¢â‚¬â€ **RESOLVED**: shared `timeout: 20000` in the HttpClient default config (all AD-19 services inherit) + `signal` forwarding on `searchPeople`/`searchCompanies` (queryFn abort on new submit; a hung request can no longer strand the page in loading). [frontend/src/lib/api/http-client.ts, frontend/src/lib/api/search-service.ts]
- ~~`aria-live="polite"` wraps the whole `#results` section~~ Ã¢â‚¬â€ **RESOLVED**: the polite region moved to the count/status line (`data-testid="results-status"`); the table is outside the live region; sort/page changes announce via an sr-only `role="status"` span inside it. [frontend/src/components/search/SearchPage.tsx]
- ~~AD-20 TanStack Query adoption~~ Ã¢â‚¬â€ **RESOLVED**: `QueryClientProvider` mounts in `Providers` (module-scoped client, `retry: false` + `refetchOnWindowFocus: false` Ã¢â‚¬â€ retry:false is a quota contract: a retried success would double-burn `daily_usage`); the results query is a `useQuery` consumer via the AD-19 `SearchService` methods (queryFn receives the abort signal); session stays SessionProvider-owned. The 3.4 gate test remains as the smoke guard. [frontend/src/components/providers/Providers.tsx, frontend/src/components/search/SearchPage.tsx]
- ~~`FilterSidebar` `applied` re-sync effect untested / clobbers post-submit edits~~ Ã¢â‚¬â€ **RESOLVED**: SearchPage drives `applied` with stable identity (set only on query success); the resync effect now carries a dirty-guard (`dirtyRef`) Ã¢â‚¬â€ staged edits during a query flight are never clobbered (verified by the 3.3 "keeps staged filters editable" test); chip removals stage via the new `stagedPatch` prop. [frontend/src/components/search/FilterSidebar.tsx]
- ~~Badge double-count landmine (draft.wilayas + wilayaCount)~~ Ã¢â‚¬â€ **RESOLVED**: badge = `countActiveFilters({ ...draft, wilayas: [] }) + wilayaCount` Ã¢â‚¬â€ wilayaCount is the single wilaya source while the combobox is wired (fires NOW because 3.5 drives applied). [frontend/src/components/search/FilterSidebar.tsx]
- ~~Dual combobox instances keep independent query state~~ Ã¢â‚¬â€ **RESOLVED**: `WilayaCombobox` gained optional controlled `inputValue`/`onInputValueChange`; SearchPage owns ONE shared query state fed to both aside + drawer mounts (same React element, two mounts Ã¢â‚¬â€ synchronized by construction). [frontend/src/components/search/WilayaCombobox.tsx, SearchPage.tsx]

## Deferred from: code review of story-3.5-results-table-stacked-row (2026-08-05)

- `/companies/:id` company-name links render per the AC literal but 404 until the company-detail surface lands (CompanyDetailPage is not in Epic 3's story list). No stub created Ã¢â‚¬â€ the link is forward-correct; the detail story must land before launch. [frontend/src/components/search/ResultsTable.tsx, ResultsTableStackedRow.tsx]

## RESOLVED by Story 3.4 (2026-08-05)

- ~~`elementFromPoint` jsdom polyfill returns the open drawer popup for every call site~~ Ã¢â‚¬â€ **RESOLVED**: polyfill now returns `[data-slot="combobox-content"]` first, then `[data-slot="drawer-popup"]`, else body. [frontend/src/test/mocks.ts]
- ~~Cross-stack industry parity is dev-run only~~ Ã¢â‚¬â€ **RESOLVED**: `backend/apps/search/tests/test_wilaya_parity.py` asserts industries lockstep (count, ids 1..N, name_en order) with a strict TS-array parser. [backend/apps/search/tests/test_wilaya_parity.py]
- ~~No cross-stack parity test between `backend/apps/search/data/wilayas.py` and `frontend/src/data/wilayas.ts`~~ Ã¢â‚¬â€ **RESOLVED**: the same parity module asserts wilaya lockstep (58 entries, codes 1Ã¢â‚¬â€œ58, trilingual non-blank); mutation-verified (code swap Ã¢â€ â€™ test fails). Canonical source = the frontend TS files. [backend/apps/search/tests/test_wilaya_parity.py]

## Deferred from: code review of story-3.3-filter-sidebar-component (2026-08-05)

- ~~No timeout/abort on search requests~~ Ã¢â‚¬â€ **RESOLVED by 3.5**: shared `timeout: 20000` in the HttpClient default config + `signal` forwarding on search methods. [frontend/src/lib/api/search-service.ts, frontend/src/lib/api/http-client.ts]
- ~~`FilterSidebar` `applied` re-sync effect~~ Ã¢â‚¬â€ **RESOLVED by 3.5**: `applied` driven with stable identity (set on query success) + dirty-guard resync + chips staging via `stagedPatch`; 3.6 saved-search re-runs restore `applied.wilayas` into the combobox via SearchPage. [frontend/src/components/search/FilterSidebar.tsx]
- Physical-property classes inside stock shadcn base-nova wrappers (drawer.tsx `md:text-left`, `left-`/`right-` swipe-direction variants, tooltip.tsx arrow offsets, scroll-area.tsx `border-l`) Ã¢â‚¬â€ registry defaults, dormant in 3.3 (swipe-down only, mobile only). Revisit if the drawer is reused at Ã¢â€°Â¥md in RTL or if a CSS lint gate is added; do not hand-edit registry files lightly. [frontend/src/components/ui/{drawer,tooltip,scroll-area}.tsx]
- ~~`aria-live="polite"` wraps the whole `#results` section~~ Ã¢â‚¬â€ **RESOLVED by 3.5**: the polite region moved to the count/status line; the table is outside the live region. [frontend/src/components/search/SearchPage.tsx]
- Registry-file class debt in the 3.4 combobox wrapper (physical `-ml-1`/`right-2`/`pl-1.5`/`pr-8` inside stock `combobox.tsx` ItemIndicator/ChipRemove, physical `data-[side=left|right]:` slide variants) Ã¢â‚¬â€ dormant (chips use custom remove buttons; popup sides default to bottom/start); revisit with the same caveat as the drawer debt. [frontend/src/components/ui/combobox.tsx]
- ~~AD-20 TanStack Query: gate CHECK PASSED in Story 3.4~~ Ã¢â‚¬â€ **RESOLVED by 3.5**: adoption completed Ã¢â‚¬â€ QueryClientProvider in Providers, results query is a useQuery consumer, session stays SessionProvider-owned. [frontend/src/__tests__/tanstack-query-gate.test.tsx]

## Deferred from: code review of story-3.4-wilaya-combobox (2026-08-05)

- Dual combobox instances (the sidebar renders the same `wilayaField` node in the aside AND the drawer) keep independent internal query state Ã¢â‚¬â€ a query typed in the drawer can resurface when the aside popup reopens after a surface switch; while the drawer is open there are two `role="combobox"` inputs in the DOM (the aside is `display:none`, so it stays out of the a11y tree). Lift the query state or scope per surface in 3.5/3.6. [frontend/src/components/search/WilayaCombobox.tsx, FilterSidebar.tsx]
- `elementFromPoint` jsdom polyfill ignores coordinates: with the drawer AND the combobox popup both open, every point reports the combobox popup Ã¢â‚¬â€ combobox click-outside dismissal is untestable in that combination (latent; no current test opens the combobox inside the drawer). [frontend/src/test/mocks.ts]
- Combobox popup can outlive its anchor when the viewport crosses the md breakpoint while open (orphan floating popup; real browser only Ã¢â‚¬â€ the drawer has the matchMedia-close precedent, the combobox does not). [frontend/src/components/search/WilayaCombobox.tsx]
- Badge double-count landmine: `countActiveFilters` counts `draft.wilayas` AND the sidebar badge adds `wilayaCount` Ã¢â‚¬â€ safe today (`draft.wilayas` is always `[]` while the field is wired), but when 3.6 feeds `applied.wilayas` into the draft re-sync the badge becomes 2Ãƒâ€”N. 3.6 must normalize: `wilayaCount` is the single wilaya source while the combobox is wired. [frontend/src/components/search/FilterSidebar.tsx, frontend/src/lib/api/search-service.ts]
- Pre-existing AD-8 risk in the 3.3 key `search.filters.badge` Ã¢â‚¬â€ next-intl formats `{count}` with the locale's numeral system, so the mobile Filters badge renders Arabic-Indic digits in AR. The 3.4 fix (`String()` for `wilaya_more`) covers only the new key; a numbering-system pass over interpolated counts is needed pre-launch. [frontend/messages/ar.json]

## Deferred from: code review of story-3.2-search-api-endpoints (2026-08-05)

- Rate-limit check-then-increment TOCTOU: the quota SELECT and the atomic upsert span the search query, so a concurrent burst at count = limit-1 can exceed the 30/100 cap. One-statement `INSERT ... ON CONFLICT DO UPDATE ... WHERE search_count < limit RETURNING` would require increment-before-success (conflicts with Q8) and a PG transaction + row lock; needs a Postgres-backed CI job to be exercised. Spine-documented pattern. [backend/apps/search/quota.py]
- PG keyword path exercised only via string assertions in CI (SQLite fallback runs behavior tests); real-PostgreSQL verification was done ad-hoc in this review. Add a Postgres-backed CI job that runs the search API tests against PG (deferred-work 2.2 precedent). [backend/apps/search/fts.py]

## Deferred from: code review of 3-1-search-database-schema (2026-08-04)

- `bulk_create` / `bulk_update` / `QuerySet.update()` skip the `save()` override, leaving `search_normalized` empty/stale (tsvector silently empty). Requirement for story 3.2 (search API) and Epic 6 (scraper pipeline): writers must set `search_normalized` explicitly or write per-row. [backend/apps/search/models.py]
- DailyUsage upsert via update-then-create races under concurrent rate-limited requests (both create Ã¢â€ â€™ IntegrityError). Story 3.2 must use an atomic PG `INSERT ... ON CONFLICT (user_id, date) DO UPDATE` upsert; the tested pattern is the SQLite-compatible approximation. [backend/apps/search/tests/test_daily_usage.py]
- No cross-stack parity test between `backend/apps/search/data/wilayas.py` and `frontend/src/data/wilayas.ts` (mirrored by process, not by test). Story 3.4 (wilaya combobox) should consolidate the canonical source and add a parity check.

## Deferred from: code review of 2-2-signup-free-credits (2026-08-01)

- Rate limiting on `POST /api/auth/signup/` and `POST /api/auth/resend-verification/` Ã¢â‚¬â€ anonymous email bombing of existing users + unbounded account creation. Needs a new dependency (django-ratelimit / django-axes) Ã¢â€ â€™ user approval required before prod. [backend/apps/accounts/views.py:159,225]
- `select_for_update` is a silent no-op on SQLite Ã¢â‚¬â€ the concurrent-verify double-grant protection is only exercised on Postgres; CI currently runs no backend tests. Add a Postgres-backed CI job. [backend/apps/accounts/views.py:174]
- Client routing for 401 `email_not_verified` (redirect to /verify-email) + refresh tokens minting for unverified users Ã¢â‚¬â€ belongs to story 2.3 (Login & Session Management). [backend/apps/accounts/auth.py:27]
- `SingleUseToken` rows accumulate (resend invalidates by UPDATE, never deletes) Ã¢â‚¬â€ add a daily cleanup task (expired/consumed rows) to the ops maintenance backlog.
- Verification token exposed in URL path / access logs Ã¢â‚¬â€ standard email-link tradeoff; mitigated by single-use + 24h TTL.
- stylelint globals.css debt (136 pre-existing errors in the story-1.x design-token file) Ã¢â‚¬â€ unrelated to story 2.2; `lint:css` is not in the CI verification list.

## Deferred from: code review of story 2.1 (2026-08-01)

- djoser 2.2.3 has no register/login/logout/password-reset endpoints Ã¢â‚¬â€ `include('djoser.urls')` mounts only the `users/` router. Story 2.2 (signup) and 2.4 (password reset) must plan custom endpoints; story dev notes corrected. [backend/config/urls.py:15]
- No migrate step in deploy path (docker-compose.yml django command, backend/Dockerfile gunicorn, .github/workflows/deploy.yml); AUTH_USER_MODEL switch is unsupported on an already-migrated default-user DB. [docker-compose.yml:42]
- last_active_at per-request write amplification Ã¢â‚¬â€ authenticated requests within a 1-minute window trigger a full UPDATE on the users row (auth.py:20-22); revisit interval when traffic exists.

## Deferred from: code review of 2-3-login-session-management (2026-08-01)

- "7 days" deletion grace hardcoded in frozen-account copy; backend has no shared grace constant. Story 2.6 owns the deletion-grace flow (recover action + constant). [frontend/messages/en.json]
- SessionUser cast blindly from /api/auth/me/ response; a backend field change would silently corrupt the Header. Adopt zod response parsing (AD-18 pattern) in a later story touching session data. [frontend/src/lib/api/auth-service.ts:22]
- /frozen renders the guest header (Login/Sign up links) since the probe degrades to guest there. Story 2.6 refines the frozen surface. [frontend/src/components/layout/Header.tsx:18]
